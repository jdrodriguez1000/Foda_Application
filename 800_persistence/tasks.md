# Tasks — Tareas del Proyecto

> Este archivo responde a: **¿Qué se hizo y qué falta?** Lista las tareas realizadas y las próximas a realizar.

---

## Índice
1. [Convenciones](#1-convenciones)
2. [Tareas Completadas](#2-tareas-completadas)
3. [Tareas En Progreso](#3-tareas-en-progreso)
4. [Tareas Pendientes](#4-tareas-pendientes)
5. [Backlog](#5-backlog)

---

## 1. Convenciones
- **ID:** identificador único (T-001, T-002, ...).
- **Estado:** ✅ Completada · 🔄 En progreso · ⏳ Pendiente · 🧊 Backlog.
- **Prioridad:** 🔴 Alta · 🟡 Media · 🟢 Baja.

## 2. Tareas Completadas
| ID | Tarea | Fecha | Notas |
|---|---|---|---|
| T-001 | Crear estructura `800_persistence` con archivos de seguimiento | 2026-07-01 | 5 archivos creados con índice. |
| T-003 | Crear `CLAUDE.md` con protocolos de inicio y cierre de sesión | 2026-07-01 | Incluye paso final de commit y push a Git. |
| T-004 | Inicializar repositorio Git y configurar remoto `origin` (rama `main`) | 2026-07-01 | Remoto: Foda_Application.git. |
| T-005 | Crear skills de proyecto `foda-next` y `foda-status` | 2026-07-01 | En `.claude/skills/`. Reemplazadas por T-006. |
| T-006 | Migrar protocolos de inicio/cierre de skills a subagentes (`session_starter`, `session_closer`) y eliminar skills antiguas | 2026-07-01 | Ver D-005. `session_starter` en model `haiku`, `session_closer` en model `sonnet`. |
| T-002 | Definir alcance y requerimientos del proyecto | 2026-07-01 | Alcance definido a partir de `990_documents/expected_workflow.md` y `current_state.md`, y del diseño de arquitectura. Se seguirá afinando iterativamente. |
| T-007 | Análisis y documento de diseño de arquitectura del sistema (`700_architecture/system_design.md` v0.1) | 2026-07-01 | Borrador con 16 secciones; pendiente de validación con el usuario (ver T-008). |
| T-012 | Corregir subagentes de sesión (referencias rotas a skills `foda-next`/`foda-status` ya eliminadas), eliminar duplicación CLAUDE.md↔agentes estableciendo fuente única de verdad en los agentes, y establecer invocación por frase-gatillo ("iniciemos/cerremos la sesión") | 2026-07-01 | Ver D-009 y L-006. Archivos: `CLAUDE.md`, `.claude/agents/session_starter.md`, `.claude/agents/session_closer.md`. |
| T-008 | Revisar y validar `700_architecture/system_design.md` con el usuario, sección por sección | 2026-07-01 | 16 secciones en 5 bloques, todas confirmadas. Documento actualizado de v0.1 a v0.2. Ver D-010 a D-014, A-004 (validado), L-007. |
| T-009 | Construir los 8 agentes de desarrollo SDD/TDD en `.claude/agents/` (`feature_definer`, `spec_writer`, `plan_builder`, `tdd_tester`, `tdd_coder`, `tdd_refactor`, `integration_tester`, `spec_verifier`) con los modelos y colores acordados | 2026-07-02 | Ver D-008, D-015. Renombrados `tdd_red`→`tdd_tester`, `tdd_green`→`tdd_coder`. Tools mínimas: Read, Glob, Grep, Write, Edit, Bash (sin Agent/web). |
| T-010 | Documentar la convención de `state.json` y la orquestación de la cadena SDD/TDD en `700_architecture/sdd_tdd_workflow.md` | 2026-07-02 | Ver D-008, D-015. Fuente única de verdad de la cadena SDD/TDD (v0.1). |
| T-011 | Crear la estructura de carpetas `600_features/` con una plantilla/ejemplo de feature | 2026-07-02 | `README.md` + `_template/` con esqueletos de los 4 documentos + `state.json` inicial. Sin feature de ejemplo ficticia (decisión del usuario); la primera feature real cumplirá ese rol. |
| T-016 | Reconciliar `980_guideline/` (`principles.md`, `methodology.md`) con lo ya construido: reparar `principles.md` como canon vinculante, insertar cláusula de lectura obligatoria en los 10 agentes, importar en `CLAUDE.md`, reorganizar `methodology.md` con mapa de fuentes y resolver contradicciones/citas fantasma | 2026-07-02 | Ver D-017, D-018, D-019, L-009, L-010, L-011. Sesión de gobernanza/reconciliación, sin código de aplicación. Genera T-017/T-018 como trabajo futuro (agentes runtime y plano runtime), luego canceladas por D-020. |
| T-019 | Revertir el runtime agéntico importado por la metodología: recortar `980_guideline/methodology.md` a "Metodología de Desarrollo del Motor" y formalizar la reversión al runtime determinista de `system_design.md` | 2026-07-02 | Ver D-020, D-021, D-022, L-012, L-013, L-014. El usuario reportó sentirse perdido con la complejidad agregada por la metodología importada; se contrastó contra `system_design.md` (D-006) y se decidió revertir. Se rescataron solo dos piezas de la metodología: Single Writer Rule (D-021) y rúbrica de evaluación calibrada para salidas no deterministas (D-022). Cancela T-017/T-018. |
| T-021 | Aplicar ajustes de gobernanza: vocabulario de bandas (D-029) + `feature_contract` (D-030) | 2026-07-02 | Checklist completo (6/6 ítems): (1) `700_architecture/sdd_tdd_workflow.md` v0.1→v0.2 — nueva sección §6 «Bandas y Ejes de Crecimiento». (2) `980_guideline/methodology.md` — añadido en §1 el bloque "Contratos explícitos en dos niveles (P5, D-030)" documentando `feature_contract` (adoptado) y `slice_contract` (diferido); el documento no mencionaba contratos tras el recorte de D-020, así que fue introducir el modelo, no solo reconciliar (ver L-017). (3) `600_features/README.md` — árbol de directorios y paso 2 de "Cómo arrancar" reflejan `feature_contract.md`; corregida referencia colgante `§6`→`§7` (L-017). (4) Plantilla `600_features/_template/feature_contract.md` creada. (5) `.claude/agents/feature_definer.md` — nuevo paso 2 "Escribir feature_contract.md", regla de uno por feature, pasos renumerados. (6) Retro-ajuste: creado `600_features/client_scaffold/feature_contract.md`. Ver D-029, D-030, L-017. |
| T-022 | Codificar la cadena de trazabilidad HU→CA→TSK (D-031): plantillas, agentes, `sdd_tdd_workflow.md` y retro-ajuste de `client_scaffold` | 2026-07-02 | A pedido del usuario para trazabilidad total `definition.md`→`spec.md`→`plan.md`. `definition.md` usa historias de usuario `HU-xx` (formato Connextra); `spec.md` codifica `CA-xx` enlazados a ≥1 `HU-xx` con matriz de cobertura HU→Spec; `plan.md` descompone en tareas atómicas `TSK-xx` (ID/Descripción/Entregable/Responsable/Estado/Trazabilidad→CA) con reglas de partición (un responsable, un entregable, código y test separados). El bucle TDD sigue corriendo por caso de `state.json` sin cambios de esquema; las TSK son capa de trazabilidad. Actualizadas las 3 plantillas, `feature_definer.md`, `spec_writer.md`, `plan_builder.md`, y `sdd_tdd_workflow.md` (v0.2→v0.3, regla transversal §8). Retro-ajustado `client_scaffold/tracer_bullet/`: `definition.md` (HU-01…HU-05), `spec.md` (CA-01…CA-11 + matriz), `plan.md` (nueva sección de tareas TSK-01…TSK-13, 18 casos mapeados a CA y TSK, secciones renumeradas). Ver D-031, L-018, A-009. |
| T-013 | Construir la primera feature real del sistema: `client_scaffold` (`foda client new <NAME>`), ejecutando la cadena de 8 agentes de punta a punta | 2026-07-02 | Cadena completa: feature_definer → spec_writer (GATE) → plan_builder (GATE) → bucle TDD (26 tests) → `integration_tester` (6 tests de integración, 32 passed) → `spec_verifier` (veredicto **CONFORME** a los 11 CA, `verification.md`). Primera feature en recorrer la cadena de 8 agentes de punta a punta; valida A-005. 3 hallazgos no bloqueantes: F-1 (entorno local Python 3.12.10 vs R1/D-010 que exige 3.13+), F-2 (capa CLI TSK-07 no construida, resuelto vía T-023/`client_new_cli`), F-3 (`created_at` en hora local, no UTC; T-025 backlog). Ver D-032, D-033, L-019 a L-023, A-005, A-009. |
| T-023 | Construir la feature `client_new_cli` (`foda client new <NAME>`, cierra el hallazgo F-2 de `client_scaffold`) ejecutando la cadena de 8 agentes | 2026-07-03 | Cadena SDD/TDD completa: `feature_definer` (5 HU) → `spec_writer` (10 CA, GATE APROBADO) → `plan_builder` (15 TSK, 12 casos TDD, GATE APROBADO) → bucle TDD (12/12 casos, 46 tests) → `integration_tester` (8 tests de integración nuevos: entry point, invocación real vía subprocess, integración con core real sin espías, `client.yaml` como contrato, aislamiento multi-tenant, resolución de `clients_root`, duplicado end-to-end; 14 passed en integración, 54 passed en total, sin defectos en `src/foda/cli.py`) → `spec_verifier` (veredicto **CONFORME** a los 10 CA, `verification.md`). Segunda feature en recorrer la cadena de 8 agentes de punta a punta. Decisiones vinculantes: D-034 (cadena completa), D-035 (CLI sí lleva tests propios, NC-5 prevalece), D-036 (`clients_root` vía `pyproject.toml` hacia arriba desde cwd), D-037 (patrón verde-directo estándar), D-038 (ramas `except` separadas por HU). Ver D-034 a D-038, L-024, L-025. |
| T-014 | Construir feature `client_context` (resolución de rutas, cliente nuevo vs. recurrente) ejecutando la cadena de 8 agentes | 2026-07-03 | Tercera feature en recorrer la cadena de 8 agentes de punta a punta. 3 decisiones vinculantes con el usuario (D-040 modo desde disco vía `models/latest` sin flag editable, D-041 `clients_root` como parámetro del constructor, D-042 introspección de artefactos existentes diferida a `flow_base`). Cadena completa: `feature_definer` (4 HU) → `spec_writer` (12 CA, decisiones técnicas D-043 `FileNotFoundError` como marcador vía `client.yaml`, D-044 `is_recurring` booleano, D-045 constructor directo + validación en `__init__`, GATE APROBADO) → `plan_builder` (16 TSK, 12 casos TDD, GATE APROBADO) → bucle TDD (12/12 casos: 6 con ciclo real incl. refactor que extrajo helper `_data_dir`, 6 verde-directo por D-037) → `integration_tester` (5 tests de integración: contrato con `create_client`, ciclo nuevo→recurrente, superficie para `Flow.run(ctx)`, fallo temprano, aislamiento multi-tenant) → `spec_verifier` (veredicto **CONFORME** D-046 a los 12 CA, `verification.md`). Se construyó `src/foda/core/context.py` (clase `ClientContext`). Suite final: 71 passed sin regresiones. 2 hallazgos no bloqueantes F-1 (verde-directo documentado) y F-2 (limitaciones de symlink roto y filesystem case-insensitive, registradas como A-010/A-011). Ver D-040 a D-046, L-029, L-030, A-010, A-011. |
| T-015 | Construir feature `flow_base` (abstracción `Flow`: load_inputs → validate → execute → write_outputs, consumiendo `ClientContext` vía `Flow.run(ctx)`) ejecutando la cadena de 8 agentes | 2026-07-03 | Cuarta feature en recorrer la cadena de 8 agentes de punta a punta. Materializa `system_design.md` §9: contrato base + template method, NO un flujo concreto. Cadena completa: `feature_definer` (5 HU) → `spec_writer` (13 CA, GATE APROBADO; 4 decisiones técnicas D-047 a D-050 sobre `FlowContractError`, `Artifact`, `FlowResult` y hooks/`run`; 2 GATEs de diseño D-051/D-052 sobre construcción del `FlowResult` y orden `load_inputs→validate`) → `plan_builder` (13 TSK, GATE APROBADO con caso 14 añadido por el humano) → bucle TDD (14/14 casos: 3 con ciclo real, 11 verde-directo por D-037) → `integration_tester` (4 tests de integración) → `spec_verifier` (veredicto **CONFORME** D-053 a los 13 CA, `verification.md`, sin hallazgos bloqueantes). Se construyó `src/foda/core/flow.py` (`Flow`, `Artifact`, `FlowResult`, `FlowContractError`). Suite final: 94 passed sin regresiones. Ver D-047 a D-053, L-031, L-032. |
| T-026 | Definir colaborativamente con el humano cuál es la siguiente feature a construir y su alcance | 2026-07-03 | RESUELTA por deliberación estratégica con el usuario (sin código): la siguiente feature es `onboarding` (flujo 020), descartando por ahora Discovery (núcleo LLM, T-020 pendiente), la banda `stab_1` de `flow_base` y el orquestador `foda run`. Se acordó además que `onboarding` arranca simulando la salida de Discovery con un `contract_data.json` fabricado (fixture), manteniendo la estrategia `tracer_bullet` y de escalabilidad progresiva. Se definió una hoja de ruta de 14 features de producto como brújula (no contrato cerrado). Ver D-054 a D-057, L-033, A-012. |
| T-027 | Construir la feature `onboarding` (flujo 020, banda `tracer_bullet`) ejecutando la cadena de 8 agentes | 2026-07-06 | Quinta feature en recorrer la cadena de 8 agentes de punta a punta CONFORME. Cadena completa: `feature_definer` (HU-01…HU-05) → `spec_writer` (22 CA tras añadir CA-05b, GATE APROBADO) → `plan_builder` (22 casos TDD, GATE APROBADO) → bucle TDD (22/22 casos: 13 con ciclo real red→green→refactor, 9 "verde directo" por D-037, desde el caso 15 bajo pre-autorización D-060) → `integration_tester` (5 tests de integración: end-to-end contra esperado fijo, invariante CA-12, fallo temprano `FlowContractError`, contrato de artefactos con flujo vecino, aislamiento multi-tenant; commit `9dcfdcc`) → `spec_verifier` (veredicto **CONFORME** a los 22/22 CA, `verification.md`, commit `a742a04`). Se construyó `src/foda/flows/f020_onboarding/onboarding.py`. Suite final: 121 passed (116 unit + 5 integración) sin regresiones. 1 hallazgo NO bloqueante OBS-1 (regla "`field.required` booleano" sin CA/test/implementación explícita, candidato a `stab_1`, ver T-029). Ver D-058 a D-062, L-034 a L-040, A-013. |
| T-028 | Construir la feature `flow_orchestrator` (orquestador CLI `foda run <cliente> --flow <flujo>` + `foda status <cliente>`, banda `tracer_bullet`) ejecutando la cadena de 8 agentes | 2026-07-07 | Sexta feature en recorrer la cadena de 8 agentes de punta a punta CONFORME. Cadena completa: `feature_definer` (`feature_contract.md` + `definition.md`, 5 HU, commit `c132a9b`) → `spec_writer` (14 CA-xx, GATE humano **APROBADO SIN CAMBIOS**, DS-ORQ-1 a DS-ORQ-4, commit `ed28ccd`) → `plan_builder` (20 TSK, 15 casos TDD, commit `774ba4c`) → **GATE humano del plan APROBADO SIN CAMBIOS** (valida A-014, A-015, A-016) → bucle TDD (15/15 casos: solo casos 4 y 10 con ciclo real red→green→refactor —camino feliz de `run` y de `status`—, 13 "verde directo" por D-037, ver L-041; se construyó `src/foda/orchestrator.py` con `FLOWS`/`resolve_flow` y se extendió `src/foda/cli.py` con subcomandos `run`/`status` y helper `_build_client_context`) → `integration_tester` (9 tests de integración con piezas reales: orchestrator→cli→ClientContext→Flow.run→Onboarding→disco, incluido entry point vía subprocess) → `spec_verifier` (veredicto **CONFORME** a los 14/14 CA, `verification.md`, commit `190af39`). Suite final: 145 passed (121 previos + 24 nuevos) sin regresiones. `README.md` actualizado con los comandos `run`/`status`. Sin hallazgos bloqueantes ni no bloqueantes nuevos. Ver D-062 a D-067, L-041, A-014 a A-016. |
| T-030 | Validación manual end-to-end de `flow_orchestrator` por el humano en una terminal real | 2026-07-07 | El usuario ejecutó `foda client new`, `foda status` y `foda run` sobre un cliente propio (`COMPANY_001`, Python 3.13.7, venv activado), cubriendo los 8 escenarios de `spec.md` (CA-01, CA-02, CA-04, CA-06, CA-07, CA-08, CA-09, CA-13): fallo limpio por artefacto ausente, éxito tras inyectar `contract_data.json` manualmente, cliente inexistente, flujo sin `--flow`, flujo desconocido. `map_client_data.json` verificado como correcto y determinista. Confirma en producción el veredicto CONFORME de `spec_verifier` (D-067); ningún defecto encontrado. Sin cambios de código ni de tests; el cliente de prueba fue borrado y `clients/` está en `.gitignore`. Ver L-042. |
| T-031 | Construir la feature `ingestion` (flujo 030, banda `tracer_bullet`) ejecutando la cadena de 8 agentes | 2026-07-08 | Séptima feature en recorrer la cadena de 8 agentes de punta a punta CONFORME, primera determinista tras `flow_orchestrator`. Cadena completa: `feature_definer` (HU-01…HU-06, commit `b636db4`) → `spec_writer` (CA-01…CA-21, GATE APROBADO, enmendado por DS-ING-8/D-077 y luego por DS-ING-9/D-078) → `plan_builder` (22 casos TDD, re-generado tras la enmienda) → bucle TDD (22/22 casos: casos 1, 2, 12, 13, 14, 16, 19 con ciclo real red→green→refactor —incluido el caso 16 que hizo aflorar la contradicción de spec resuelta por D-078/DS-ING-9, y el caso 19 con ROJO GENUINO por `bronze_path` hardcodeado a `None`—; los demás casos verde directo por D-037/D-060; 167 tests) → `integration_tester` (7 tests de integración contra el sistema real: `Onboarding` real + `contract_data.json` fabricado D-014, invariante silver/gold, fallo temprano `FlowContractError`, consumo por flujo vecino, inconsistencia parcial real CA-18, aislamiento multi-tenant; 174 passed, sin defectos, commit `1945729`) → `spec_verifier` (veredicto **CONFORME** a los 21/21 CA-01…CA-21, `verification.md`, commit `d8dc9bf`). Suite final: 174 passed sin regresiones. Ver D-068 a D-078, L-043 a L-050, A-017. |
| T-032 | Cablear el CLI para `ingestion`: registrar `Ingestion` en `FLOWS` de `src/foda/orchestrator.py` para habilitar `foda run <cliente> --flow ingestion`, y actualizar `README.md` | 2026-07-09 | Cambio quirúrgico de 3 archivos (`src/foda/orchestrator.py`, `README.md`, y `tests/flows/test_ingestion.py` por el fix de flakiness): `FLOWS = {"onboarding": Onboarding, "ingestion": Ingestion}`. Verificado: `resolve_flow('ingestion')` devuelve instancia de `Ingestion`. Al correr la suite se detectó y corrigió un test flaky preexistente (CA-13, determinismo byte a byte): `openpyxl` embebe timestamps en `docProps/core.xml`, el test generaba los bytes del xlsx dos veces por separado para `ctx_1`/`ctx_2`; se corrigió generándolos una sola vez e inyectándolos en ambos contextos vía el nuevo parámetro opcional `precios_xlsx` de `_build_ctx_fixture_completo`. Suite estable 174 passed (3 corridas). Cierra el hallazgo L-051. Ver L-052. |
| T-033 | Prueba humana de `ingestion` vía CLI de punta a punta | 2026-07-09 | El usuario creó clientes DEMO_A..DEMO_F (fuera de Git, `clients/` gitignored por D-006) y ejecutó manualmente: camino feliz (3 archivos ingested, success:true, copias en bronze) y 6 caminos no-felices, todos con el resultado esperado: (A) columna requerida faltante → rejected/missing_column, no copiado a bronze; (B) columna extra → rejected/unexpected_column pese a tener todas las requeridas (política estricta); (C) archivo declarado faltante → missing/missing_file; (D) archivo inesperado → unexpected_file top-level, no copiado a bronze; (E) sin `map_client_data.json` (onboarding no corrido) → `FlowContractError`, exit 1, sin reporte escrito; (F) columna opcional ausente → sigue ingested/success:true. Se detectaron 3 hallazgos de gobernanza (exit code no refleja `success=false`, `.venv` desactualizado tras nueva dependencia `openpyxl`, `ingestion` no limpia `data/bronze/` entre corridas) que originaron el ADR D-080. Ver D-080, L-053, L-054, L-055. |
| T-035 | Reflejar `success` en el exit code de `foda run` (punto 4 del ADR D-080) | 2026-07-09 | Cambio quirúrgico sobre `flow_orchestrator` ya CONFORME (no re-recorrió la cadena SDD/TDD completa; TDD acotado rojo→verde sobre la feature existente, ver L-056). RED: nuevo test `test_run_flujo_con_success_false_devuelve_1_sin_reportar_completado` en `tests/cli/test_flow_orchestrator_cli.py` (espía `Onboarding.run` devolviendo `FlowResult(success=False, outputs=[])`, afirma exit 1 y stdout sin "completado"). GREEN: en `_dispatch_run` de `src/foda/cli.py`, tras `flow.run(ctx)` sin excepción, si `result.success` es False imprime mensaje que NO dice "completado" y retorna 1; si es True, comportamiento previo intacto (exit 0). Docstrings de `cli.py` actualizados (trazabilidad a D-080/L-053). Contrato final de `foda run`: fallo duro (`FlowContractError`) → 1; `success:false` (inconsistencias blandas) → 1 (antes 0, este es el fix); `success:true` → 0. Suite: 175 passed (174+1), sin regresiones. **Pendiente de T-035**: prueba humana vía CLI (correr `ingestion` con datos inconsistentes reales y verificar exit code 1, L-051); el usuario decidió no hacerla en esta sesión. Ver D-080, L-053, L-056. |

## 3. Tareas En Progreso
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| _Ninguna._ | | | |

## 4. Tareas Pendientes
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| T-034 | Implementar el flujo de ramas por feature + merge por PR (política definida en D-079, alcance resuelto por D-081) | 🔴 Alta | Próxima tarea prioritaria acordada con el usuario. 7 ajustes: `CLAUDE.md` nueva sección de ramas; `feature_definer.md` paso 0 crear rama `feature/<nombre>` + stages nuevos; `session_closer.md` push a rama actual (`git push -u origin HEAD`) en vez de `origin main`; `session_starter.md` avisar si está en `main` con feature en progreso; `methodology.md` operacionalizar; `decisions.md` ADR (ya D-079/D-081); plantilla `state.json` con stages `human_test`/`merge_to_main`. Punto de alcance antes ABIERTO ya RESUELTO por D-081 (2026-07-09): T-034 se implementa directo sobre `main` (no en su propia rama), y el contrato queda aclarado como "automatización hasta el PR, merge a `main` es gate humano explícito". |
| T-036 | Implementar el gate de progresión entre flujos por `success` del predecesor + flag `--force` | 🟡 Media | Puntos 1-3 del ADR D-080: ningún flujo puede ejecutarse si su predecesor no terminó con `success:true` (leyendo el reporte del predecesor, p. ej. `ingestion_report.json`); si no hay OK, `foda run` del flujo siguiente falla con código 1 y mensaje claro sin escribir nada; override `--force` permite correr de todos modos dejando advertencia registrada. Excepción temporal para `discovery` (no construido, se opera a mano) y `onboarding` (solo exige la presencia de `contract_data.json`, no un "OK de discovery"). Por NC-2/NC-4 se implementa junto al primer flujo consumidor de un reporte de `ingestion` en adelante (hoy no hay consumidor). Diferida hasta que exista un primer flujo consumidor de reportes (probablemente `discovery`, aún no construido). |
| T-037 | Comando encadenado tipo `foda run <cliente> --flow onboarding→ingestion` | 🟢 Baja | Feature nueva vía SDD/TDD; depende de T-036 (gate de progresión) para tener sentido (encadenar solo si el predecesor puede fallar el gate). Diferida junto con T-036. |

## 5. Backlog
| ID | Tarea | Notas |
|---|---|---|
| T-024 | `client_scaffold` banda `stab_n`: escribir test que simule fallo de I/O e implementar el rollback best-effort DS-2.2 (caso 18) | Trabajo diferido por GATE PA-3 opción (c), ver D-032. |
| T-025 | `client_scaffold` banda `stab_n`: cambiar `created_at` de `client.yaml` de fecha local (`date.today()`) a UTC | Hallazgo no bloqueante F-3 de `spec_verifier`; CA-06 solo valida el patrón de fecha, no la zona horaria. |
| ~~T-017~~ | ~~Construir los agentes runtime del patrón A/B/C descrito en `980_guideline/` (`foda-governor`, `foda-<flujo>-planner`, `foda-<flujo>-evaluator`)~~ | **Cancelada por D-020** (2026-07-02): el runtime NO es agéntico; lo define exclusivamente `system_design.md`. |
| ~~T-018~~ | ~~Reconciliar por completo el plano runtime descrito en `980_guideline/` (`fda-*-state.json`, `install.sh`, distinción planos MOTOR/INSTANCIA) con la arquitectura ya construida~~ | **Cancelada por D-020** (2026-07-02): se descartan MOTOR/INSTANCIA y `fda-*-state.json`; no hay plano runtime que reconciliar. |
| T-020 | Diseñar las rúbricas calibradas concretas (dimensiones+pesos, few-shot, anclas) para las salidas no deterministas de Discovery y Exploration | Trabajo futuro identificado en D-022. Se hará al construir esos flujos, después de T-013/T-014/T-015. |
| T-029 | `onboarding` banda `stab_1`: codificar como CA formal la regla "`field.required` es booleano" (Comportamiento Esperado 2b de `spec.md`), con su test correspondiente | Hallazgo no bloqueante OBS-1 de `spec_verifier` (2026-07-06): la regla existe en el texto de `spec.md` pero no tiene CA/test/implementación explícita; no es no conformidad porque los 22 CA-xx (fuente de verdad) están todos cubiertos. |
