# Progress — Estado del Proyecto

> Este archivo responde a: **¿Cómo va el proyecto?** Muestra el avance general, lo realizado y lo próximo a realizar.

---

## Índice
1. [Resumen del Estado](#1-resumen-del-estado)
2. [Métricas de Avance](#2-métricas-de-avance)
3. [Lo Realizado](#3-lo-realizado)
4. [En Progreso](#4-en-progreso)
5. [Próximo a Realizar](#5-próximo-a-realizar)
6. [Bloqueos y Riesgos](#6-bloqueos-y-riesgos)
7. [Historial de Actualizaciones](#7-historial-de-actualizaciones)

---

## 1. Resumen del Estado
- **Proyecto:** Foda_Application
- **Fase actual:** T-031 (`ingestion`, flujo 030, banda `tracer_bullet`) **CERRADA/CONFORME**, séptima feature en recorrer la cadena de 8 agentes de punta a punta. Esta sesión cerró las etapas finales 7-8/8. `integration_tester` añadió `tests/integration/test_ingestion_integration.py` con 7 tests contra el sistema REAL: (1) end-to-end sobre `ClientContext` real con `map_client_data.json` de una corrida real de `Onboarding().run(ctx)` + `contract_data.json` fabricado, ejercitando el contrato multi-flujo D-014; (2) éxito no toca silver/gold; (3-4) fallo temprano → `FlowContractError` (no excepción cruda) si falta `contract_data.json` o `map_client_data.json`, sin reporte ni bronze parcial; (5) `ingestion_report.json` consumible por un flujo vecino de juguete (patrón SS8/D-014); (6) inconsistencia parcial real (CA-18): copia solo válidos a bronze, 1 rejected excluido, `success=false`; (7) aislamiento multi-tenant entre dos `ClientContext` reales. 7 passed al primer intento, sin defectos encontrados, sin cambios de código de producción; suite 174 passed (167+7) sin regresiones (commit `1945729`). `spec_verifier` recorrió los 21 CA-01…CA-21 con matriz de trazabilidad completa (21/21 cubiertos, 0 parciales, 0 sin cubrir), verificó restricciones (Python 3.13+, JSON determinista, LLM aislado, `openpyxl` autorizada, core no ampliado, enmienda D-078 verificada en CA-16), corrió la suite completa (174 passed en 1.96s) y emitió veredicto **CONFORME**, produciendo `600_features/ingestion/tracer_bullet/verification.md` (commit `d8dc9bf`). `state.json`: `status:"done"`, `current_stage:"completed"`. `ingestion` es la SÉPTIMA feature completa CONFORME de punta a punta (tras `client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`, `flow_orchestrator`). T-031 queda COMPLETADA. Sin decisiones ADR nuevas ni bloqueos en esta sesión. Ver D-068 a D-078, L-043 a L-050, A-017.
- **Fase anterior:** T-028 (`flow_orchestrator`, banda `tracer_bullet`) **CERRADA/CONFORME**, sexta feature en recorrer la cadena de 8 agentes de punta a punta. Tras el GATE humano del plan **APROBADO SIN CAMBIOS** (valida A-014, A-015, A-016), se ejecutó el bucle TDD completo (15/15 casos: solo el caso 4 —camino feliz de `run`— y el caso 10 —camino feliz de `status`— requirieron ciclo rojo→verde real con código de producción nuevo; los otros 13 nacieron "verde directo" por D-037/L-041, cubiertos por el diseño mínimo de esos dos casos). Se construyó `src/foda/orchestrator.py` (`FLOWS = {"onboarding": Onboarding}` + `resolve_flow(name)`) y se extendió `src/foda/cli.py` con los subcomandos `foda run <cliente> --flow <flujo>` y `foda status <cliente>` (helper `_build_client_context` extraído en refactor). `integration_tester` añadió 9 tests de integración con piezas reales (orchestrator→cli→ClientContext→Flow.run→Onboarding→disco, incluido el entry point `foda` vía subprocess), sin defectos de integración. `spec_verifier` emitió veredicto **CONFORME**: 14/14 CA-01…CA-14 cubiertos con evidencia real releída, `verification.md` producido, sin hallazgos bloqueantes ni no bloqueantes. Suite final: 145 passed (121 previos + 24 nuevos), sin regresiones. Se actualizó `README.md` documentando los comandos `run`/`status` (tabla-resumen de los tres comandos, modelo mental, secciones de uso, flujo típico ampliado). Ver D-062 a D-067.
- **Deliberación estratégica que originó la feature:** tras cerrar `onboarding`, el humano preguntó en qué punto puede probar/inspeccionar resultados sin esperar a los 14 flujos. Se aclaró (respaldado por `system_design.md` §5 y el rol de revisor/aprobador del DS, D-006) que NO hay que esperar: cada flujo deja un artefacto inspeccionable (`map_client_data.json` de `onboarding` lo es). Brecha identificada: hoy no se puede disparar un flujo desde terminal porque el orquestador `foda run`/`foda status` (`system_design.md` §11) fue diferido por D-054/T-026. Decisión B: la siguiente feature es **`flow_orchestrator`** (banda `tracer_bullet`), con alcance acotado a `foda run <cliente> --flow <flujo>` + `foda status <cliente>`; rangos `--from/--to`, `--pipeline` y `foda export` quedan fuera de alcance, diferidos a bandas/features posteriores.
- **Estado general:** 🟢 Sin bloqueos de gobernanza. `client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`, `flow_orchestrator` e `ingestion` conformes y cerradas (siete features completas de punta a punta); `ingestion` ya cableada al CLI y validada por prueba humana. `foda run` ya refleja `success` en su exit code (T-035, código+test; falta solo la prueba humana). El flujo de ramas por feature + merge por PR (D-079/D-081) ya está implementado (T-034) y esta sesión fue la PRIMERA en ejercerlo en la práctica. 🔄 **EN PROGRESO (rama `feature/profiling`, NO en `main`):** feature `profiling` (flujo 040, banda `tracer_bullet`, esqueleto mínimo/tracer bullet), octava feature en recorrer la cadena de 8 agentes. Cadena SDD completa (etapas 1-3, 2 GATEs humanos APROBADOS) y bucle TDD arrancado: caso 1/17 cerrado, 16/17 pendientes. T-036 (gate de progresión) ya NO está diferida: se implementa DENTRO de esta feature (Profiling es el primer flujo consumidor real de un reporte de `ingestion`). T-037 (comando encadenado) sigue diferida.
- **Vigésimo noveno cierre (nueva sesión, rama `feature/profiling`):** Se resolvió con el humano el "bloqueo" de T-036 (gate de progresión, diferida por falta de flujo consumidor de un reporte de `ingestion`): leyendo `990_documents/expected_workflow.md` se identificó que el flujo que sigue a `ingestion` en el pipeline de negocio es **Profiling (040)**, no `discovery` (que precede a `onboarding`, no lo sucede) — ver L-059. Profiling es el primer consumidor real de un reporte de `ingestion` con `success`, y por tanto el anfitrión natural de T-036. Se decidió construir un **esqueleto mínimo (tracer bullet) de `profiling`** como anfitrión concreto de T-036, en vez del flujo profiling completo (alcance acotado por NC-2/NC-4). Se ejecutó la cadena SDD/TDD sobre la rama `feature/profiling` (creada por `feature_definer`, primera vez que se ejerce D-079/D-081 en la práctica): `feature_definer` (`feature_contract.md` + `definition.md`, HU-01…HU-05, commit `01147c2`); `spec_writer` (`spec.md`, CA-01…CA-13, 4 decisiones de diseño DS-PROF-1 a DS-PROF-4 — el gate vive en `_dispatch_run` de `cli.py` no en `Flow`; `PREDECESSORS = {"profiling": "ingestion"}` en `orchestrator.py`; `profiling_report.json` mínimo con único campo vinculante `success`; `--force` como `store_true` con evaluador `evaluate_predecessor_gate`; **GATE humano APROBADO**, commit `8883a1d`); `plan_builder` (`plan.md`, 17 casos TDD ordenados unidad-Flow→evaluador-gate→CLI/`--force`; **GATE humano APROBADO**, commit `8f3285a`). Bucle TDD arrancado: caso 1/17 (CA-01, contrato de clase `Profiling(Flow)`) cerrado con ciclo completo rojo (`tdd_tester`, `ModuleNotFoundError` legítimo, commit `7c5433e`) → verde (`tdd_coder` crea `src/foda/flows/f040_profiling/{__init__,profiling}.py`, commit `7cfc521`) → refactor (`tdd_refactor`, sin cambios de código, ya mínimo, commit `5848be9`). Suite: 176 passed (175 previos + 1). Fuera de alcance explícito de esta feature: lógica de salud de datos, lectura de `bronze/`, exportables csv/xlsx, comparación contra `client_register.yaml` (diferido a `stab_1`). **Feature EN PROGRESO, NO completada**: quedan 16/17 casos pendientes (2-17), el próximo es CA-02. Rama `feature/profiling` con 6 commits, empujada a `origin` en el cierre de esta sesión. Ver D-082 a D-086, L-059, L-060.
- **Vigésimo octavo cierre (nueva sesión):** T-034 COMPLETADA: implementación del flujo de ramas por feature + merge por PR (política D-079, alcance resuelto por D-081), sesión de gobernanza/documentación sin código de producción (suite sin cambios, 175 passed). Se resolvieron con el humano (vía AskUserQuestion) 2 ambigüedades de alcance: (1) ampliar de 7 a 9 archivos, incluyendo `sdd_tdd_workflow.md` y `spec_verifier.md` para evitar inconsistencias con los stages terminales nuevos; (2) la sesión principal (no un agente) abre el PR tras `spec_verifier` CONFORME, manteniendo los 8 agentes de desarrollo simples. Modelo de flujo: `feature_definer` crea `feature/<nombre>` (paso 0) → cadena SDD/TDD corre sobre esa rama → `session_closer` empuja a `HEAD` → `spec_verifier` CONFORME deja `current_stage=human_test` (ya no cierra `completed` directamente) → la sesión principal abre el PR (`gh pr create`) → gate humano `human_test` → gate humano `merge_to_main` → feature cerrada en `main`. 9 archivos editados: `CLAUDE.md` (§3 nueva), `.claude/agents/feature_definer.md` (paso 0 + stages terminales), `.claude/agents/session_closer.md` (push a `HEAD`), `.claude/agents/session_starter.md` (paso 3, advertencia no bloqueante), `980_guideline/methodology.md` (apéndice operacionalizado), `600_features/_template/state.json` (stages `human_test`/`merge_to_main`), `.claude/agents/spec_verifier.md` (semántica de cierre ajustada), `700_architecture/sdd_tdd_workflow.md` (v0.3→v0.4), `800_persistence/decisions.md` (actualización de estado D-079/D-081 en este cierre). Validaciones: `state.json` template JSON parseable; grep de referencias colgantes sin hallazgos bloqueantes. T-034 se aplicó directo sobre `main` (D-081), no en su propia rama. Ver D-079, D-081, L-058.
- **Vigésimo séptimo cierre (nueva sesión):** T-035 COMPLETADA (código+test, prueba humana pendiente): cambio quirúrgico sobre `flow_orchestrator` ya CONFORME, con TDD acotado rojo→verde en vez de reabrir la cadena de 8 agentes (D-080 punto 4, L-056). `foda run` ahora sale 1 si `result.success` es `false` (antes salía 0 y decía "completado"); sale 0 solo si `success:true`; el fallo duro (`FlowContractError`) sigue saliendo 1 sin cambios. Suite: 175 passed (174+1), sin regresiones. Se resolvió con el humano el punto de alcance ABIERTO de T-034/D-079 mediante la enmienda **D-081**: mientras T-034 no se implemente, todo el trabajo va directo a `main` (incluida la propia T-034), y se aclaró el contrato de la futura política de ramas — la automatización lleva el trabajo hasta el PR, el merge a `main` queda como gate humano explícito. T-034 pasa a ser la próxima tarea prioritaria (prioridad sube a Alta). Ver D-080, D-081, L-053, L-056, L-057.
- **Vigésimo quinto cierre (nueva sesión, sin código, sesión de análisis/definición):** dos hallazgos/decisiones registrados sin tocar código de producción. (1) Al preparar la prueba humana de `ingestion`, se detectó que `src/foda/orchestrator.py` (`FLOWS = {"onboarding": Onboarding}`) no registra `ingestion`: `foda run <cliente> --flow ingestion` falla con "Flujo desconocido" pese al veredicto CONFORME de `spec_verifier` (D-078/L-051). Se definió (sin ejecutar) el plan de prueba humana de punta a punta: crear cliente, depositar `contract_data.json` fabricado en `020_outputs/010_discovery/`, producir `map_client_data.json` con `foda run --flow onboarding`, depositar archivos crudos en `010_inputs/030_ingestion/`, ejecutar `foda run --flow ingestion` e inspeccionar `ingestion_report.json` + `data/bronze/`. (2) Se acordó con el humano una nueva política de gobernanza (D-079, IMPLEMENTACIÓN PENDIENTE): toda feature nueva se trabaja en su propia rama `feature/<nombre>`, con merge a `main` vía Pull Request (`gh`) tras prueba humana aprobada (`gh pr merge --merge --delete-branch`), y `state.json` gana stages terminales `human_test`/`merge_to_main`. La implementación requiere 7 ajustes futuros (T-034), con un punto de alcance abierto sin resolver (si el propio cambio de gobernanza se aplica directo a `main` o en su propia rama). Se registraron las 3 próximas tareas (T-032, T-033, T-034) en ese orden. Ver D-079, L-051, A-018.
- **Validación manual humana de `flow_orchestrator` (2026-07-07):** el usuario ejecutó de punta a punta la CLI `foda` en una terminal real (Python 3.13.7, venv activado) con un cliente propio (`COMPANY_001`, distinto al `ABC`/`DEMO_ABC` de los tests), cubriendo `foda client new`, `foda status` (con y sin artefactos), `foda run` (fallo limpio por artefacto ausente, éxito tras inyectar `contract_data.json` manual, cliente inexistente, flujo sin `--flow`, flujo desconocido). Los 8 escenarios coincidieron exactamente con el comportamiento esperado en `spec.md` (CA-01, CA-02, CA-04, CA-06, CA-07, CA-08, CA-09, CA-13), sin Tracebacks ni salidas parciales, y el `map_client_data.json` producido fue verificado como correcto y determinista. Esta validación **confirma en producción** el veredicto CONFORME de `spec_verifier` (D-067); ningún defecto encontrado. Se aclaró de paso (consultando `system_design.md`) que `contract_data.json` es la salida futura del flujo `010_discovery` (aún no construido); hoy hay que inyectarlo a mano como stand-in tipo tracer bullet para poder probar `onboarding`/`flow_orchestrator`. El cliente de prueba `COMPANY_001` fue borrado por el usuario tras la prueba; `clients/` está vacío y en `.gitignore`, por lo que nada de la prueba quedó en control de versiones. Ver L-042.
- **Vigésimo sexto cierre (nueva sesión):** T-032 COMPLETADA: se cableó `ingestion` al orquestador (`src/foda/orchestrator.py`, `FLOWS = {"onboarding": Onboarding, "ingestion": Ingestion}`) y se actualizó `README.md` ("Flujos disponibles hoy"); cambio quirúrgico de 3 archivos (NC-2/NC-3), cierra el hallazgo L-051. Al correr la suite se detectó y corrigió un test flaky preexistente (`test_dos_ejecuciones..._byte_identicos`, CA-13 de `ingestion`): `openpyxl` embebe timestamps en `docProps/core.xml`, así que dos llamadas independientes a `_precios_xlsx_bytes()` generaban bytes distintos y el test comparaba orígenes diferentes; se corrigió generando los bytes una sola vez e inyectándolos en ambos `ClientContext` de prueba. Suite estable en 174 passed (3 corridas consecutivas). T-033 VERIFICADA CONFORME: el usuario ejecutó manualmente `ingestion` vía CLI de punta a punta con clientes DEMO_A..DEMO_F (fuera de Git, `clients/` gitignored), cubriendo el camino feliz y 6 caminos no-felices (columna faltante, columna extra que rechaza pese a tener las requeridas, archivo declarado faltante, archivo inesperado, ausencia de `map_client_data.json` → `FlowContractError` sin reporte, columna opcional ausente), todos con el resultado esperado. Se detectaron 3 hallazgos de gobernanza: (1) `foda run` no refleja `success=false` en su código de salida (solo el fallo duro por `FlowContractError` sale con código 1); (2) el `.venv` puede quedar desactualizado tras declarar una nueva dependencia (`openpyxl`) en `pyproject.toml`, requiriendo reinstalación editable; (3) `ingestion` no limpia `data/bronze/` entre corridas (gap de idempotencia, no manifestado en la prueba). Se acordó con el usuario una nueva decisión de gobernanza, **D-080 "Gate de progresión entre flujos por `success` del predecesor"**: ningún flujo puede ejecutarse si su predecesor no terminó con `success == true`, con override `--force`, y `foda run` debe reflejar `success` en el exit code (0 solo si OK, 1 si `success:false`), con excepción temporal para `discovery`/`onboarding`. Se registran 3 tareas nuevas derivadas del ADR (exit code, gate+`--force`, comando encadenado futuro). Ver D-080, L-052 a L-055.
- **Trigésimo cierre (nueva sesión, rama `feature/profiling`):** se avanzó el bucle TDD desde el caso 2 hasta el caso 17, cerrando el **BUCLE TDD COMPLETO de `profiling` (17/17)**, `stages.tdd.status:"done"`, `current_stage` avanza a `integration_tester`. Casos con código de producción genuino (rojo real→verde con código nuevo): caso 2 (CA-02, `Profiling.execute`/`write_outputs` en `src/foda/flows/f040_profiling/profiling.py`, reporte mínimo determinista sort_keys/indent=2/newline, sin sobreescribir `run`); caso 6 (CA-06, registro `"profiling": Profiling` en `FLOWS` de `src/foda/orchestrator.py`); caso 7 (CA-12, `PREDECESSORS = {"profiling": "ingestion"}` + esqueleto de `evaluate_predecessor_gate`, rama "sin predecesor→None"); caso 8 (CA-06, rama "success:true→None" resolviendo la ruta del predecesor vía `resolve_flow(...).produces[0].path(ctx)`, sin hardcodear, DS-PROF-2/4); caso 9 (CA-07, rama "success:false→mensaje", refactor corrigió un doble negativo confuso); caso 10 (CA-13, rama "reporte ausente→mensaje" con chequeo `report_path.exists()` antes de leer; `evaluate_predecessor_gate` queda COMPLETA en sus 4 ramas, refactor unificó ambos mensajes); caso 12 (CA-07/CA-08, wiring CLI: flag `--force` `store_true` + llamada a `evaluate_predecessor_gate` en `_dispatch_run` de `src/foda/cli.py` antes de `flow.run`, rama "mensaje sin --force→stderr+return 1"); caso 13 (CA-09/CA-10, rama override "mensaje con --force→advertencia una línea a stderr+continúa"; wiring CLI queda COMPLETO en sus 3 caminos). 8 casos "verde directo" (3, 4, 5, 11, 14, 15, 16, 17) con test específico no trivial y comportamiento ya cubierto por casos anteriores (D-037), previstos por `plan.md`. Tests nuevos: `tests/flows/test_profiling.py`, `tests/test_orchestrator.py` (extendido), `tests/cli/test_profiling_gate_cli.py` (archivo nuevo). Saneamiento incidental: docstrings desalineados corregidos en `orchestrator.py`/`cli.py`; se corrigió un error de sintaxis JSON preexistente en `600_features/profiling/tracer_bullet/state.json` (faltaba llave de cierre); deuda conocida y aceptada (duplicación deliberada de fixtures entre suites `tests/cli/`, duplicación preexistente del patrón `print stderr + return 1` en `cli.py`). Suite: 192 passed (176→192), sin regresiones. Todos los commits de la sesión (`100bbdc`...`5ec3e0b`) quedaron en `feature/profiling`, empujados a `origin` en el cierre de esta sesión. Próxima tarea: invocar `integration_tester` (TSK-31). Se mantuvo disciplina TDD estricta caso a caso pese al riesgo de verdes anticipados (NC-3/NC-5); no se hizo push intermedio (política D-079/D-081, el push lo hace `session_closer`).
- **Última actualización:** 2026-07-09

## 2. Métricas de Avance
| Métrica | Valor |
|---|---|
| Avance global | Siete features completas cerradas CONFORME de punta a punta (`client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`, `flow_orchestrator`, `ingestion`); `ingestion` ya cableada al CLI y validada por prueba humana; `foda run` ya refleja `success` en su exit code (T-035); flujo de ramas por feature + PR ya implementado (T-034); octava feature `profiling` EN PROGRESO en rama `feature/profiling` (cadena SDD completa, **bucle TDD COMPLETO 17/17 casos cerrado**, pendiente `integration_tester`/`spec_verifier`) |
| Tareas completadas | 36 |
| Tareas pendientes | 1 (T-037 comando encadenado; T-036 pasó a En Progreso dentro de `profiling`) |
| Tareas en progreso | 1 (T-038 construir feature `profiling`, incluye T-036; bucle TDD 17/17 cerrado, próximo paso `integration_tester`) |

## 3. Lo Realizado
- Creación de la estructura de persistencia (`800_persistence`) con los 5 archivos de seguimiento.
- Creación de `CLAUDE.md` con Protocolo de Inicio y Protocolo de Cierre de Sesión (incluye commit y push a Git).
- Inicialización del repositorio Git, configuración del remoto `origin` y rama `main`.
- Migración de los protocolos de inicio/cierre de skills de proyecto a subagentes: se crearon `session_starter` (model `haiku`, protocolo foda-next) y `session_closer` (model `sonnet`, protocolo foda-status) en `.claude/agents/`, y se eliminaron las skills antiguas `foda-next` y `foda-status` (junto con la carpeta `.claude/skills/`). Motivo: el frontmatter `model:` no aplica a skills invocadas inline, solo a agentes.
- La sesión principal ahora se ejecuta en Opus 4.8 (fijado como default vía `/model`).
- Se leyeron y analizaron `990_documents/expected_workflow.md` (pipeline esperado de ~14 flujos) y `990_documents/current_state.md` (situación actual del negocio Triple S / Sabbia).
- Se definió el alcance del negocio: planeación de demanda con ML, modelo objetivo Service as a Software (SaaSw), automatización 85-95%, científico de datos como revisor/aprobador; se predice demanda de productos, no ventas.
- El usuario fijó restricciones de diseño clave: CLI sin frontend, Python, determinista por defecto con LLM aislado, multi-tenant por carpeta-por-cliente en disco (sin BD), entrada YAML / salida JSON, datos crudos a capa bronze, cliente nuevo genera modelo / cliente recurrente reutiliza modelo, estructura de carpetas por flujo con prefijos numéricos.
- Se creó `700_architecture/system_design.md` (v0.1, borrador) con el diseño completo de arquitectura: contexto, principios, restricciones (R1-R9), visión general, modelo de los 14 flujos, clasificación por determinismo, estructura de carpetas, contrato de artefactos, abstracción `Flow`/`ClientContext`, capas medallion, interfaz CLI, caminos nuevo vs recurrente, multi-tenant, encapsulamiento del LLM, detalle por flujo y puntos abiertos.
- Se diseñó (a nivel conceptual, sin construir código) la cadena de 8 **agentes de desarrollo** SDD/TDD (`feature_definer`, `spec_writer`, `plan_builder`, `tdd_red`, `tdd_green`, `tdd_refactor`, `integration_tester`, `spec_verifier`), con modelos/colores asignados, gates humanos tras spec y plan, bucle red→green→refactor por caso de test, política de reintento (máx. 2) y escalamiento a humano, commit por etapa, y persistencia en disco vía `state.json` por feature (los subagentes son efímeros; la resumibilidad depende de checkpoints en disco). Se definió la distinción entre agentes de desarrollo (construyen la app) y agentes de runtime (Discovery, Ingestion, etc., que son la app). Se acordó la estructura de artefactos por feature en `600_features/<feature>/`.
- Sesión de mantenimiento de infraestructura de protocolos (T-012, D-009, L-006): se corrigieron referencias colgantes a las skills `foda-next`/`foda-status` (ya eliminadas en T-006) dentro de `.claude/agents/session_starter.md` y `.claude/agents/session_closer.md`; se eliminó la duplicación del protocolo entre `CLAUDE.md` y los agentes, dejando el detalle paso a paso solo en los agentes (fuente única de verdad) y reduciendo `CLAUDE.md` §1/§2 a una política corta que delega e invoca por frase-gatillo ("iniciemos la sesión" / "cerremos la sesión"). No hubo cambios de código de aplicación ni de arquitectura.
- T-008 completada: se revisó y validó `700_architecture/system_design.md` sección por sección con el usuario (16 secciones, 5 bloques temáticos). Cambios aplicados: (1) R1 ahora exige Python 3.13+; (2) `models/` pasa a ser versionado (subcarpetas por versión + puntero `latest`) en vez de un único `best_model.pkl`; (3) los exports/descargables (csv/xlsx) se guardan dentro de `020_outputs/<flujo>/`, sin carpeta `exports/` separada; (4) tabla de contratos de artefactos ajustada al modelo versionado y se añadió nota de dependencias multi-flujo (un flujo puede requerir artefactos de más de un flujo atrás); (5) proveedor LLM por defecto fijado explícitamente como la API de Anthropic (Claude), tras la abstracción `src/foda/llm/`. Documento actualizado de v0.1 a v0.2. Ver D-010 a D-014, A-004 (validado).
- T-009 completada: se construyeron los 8 agentes de desarrollo SDD/TDD en `.claude/agents/` (`feature_definer`, `spec_writer`, `plan_builder`, `tdd_tester`, `tdd_coder`, `tdd_refactor`, `integration_tester`, `spec_verifier`), cada uno con frontmatter (name, description "arranca en frío", model, color) y `tools: Read, Glob, Grep, Write, Edit, Bash` (sin Agent ni web; la orquestación la hace la sesión principal). Renombrado respecto a D-008: `tdd_red` → `tdd_tester`, `tdd_green` → `tdd_coder` (ver D-015). Los agentes validan gate/etapa previa vía `state.json`, hacen commit por etapa sin push, y separan artefactos SDD (`600_features/<feature>/`) de código/tests (`src/foda/`, `tests/`).
- T-010 completada: se creó `700_architecture/sdd_tdd_workflow.md` (v0.1) como fuente única de verdad de la cadena SDD/TDD: distinción desarrollo vs runtime, tabla de los 8 agentes, diagrama de orquestación con gates y bucle TDD, protocolo de gates humanos, convención completa de `state.json` (esquema, estados, matriz lectura/escritura, transiciones), reglas transversales, artefactos por feature, reanudación y bloqueos.
- T-011 completada: se creó `600_features/` con `README.md` (qué vive allí, cómo arrancar una feature, enlace a sdd_tdd_workflow.md) y `_template/` con esqueletos de `definition.md`, `spec.md`, `plan.md`, `verification.md` y `state.json` inicial canónico. No se creó una feature de ejemplo ficticia (decisión aprobada por el usuario): la primera feature real cumplirá ese rol y validará A-005.
- Se acordó con el usuario cuál será la primera feature real (T-013): `client_scaffold` (`foda client new <NAME>`), elegida entre 3 candidatas (`client_context`, `client_scaffold`, `flow_base`) siguiendo un orden de construcción abajo-hacia-arriba. Se definió su alcance in/out y se resolvieron 3 decisiones de alcance (fallar si el cliente ya existe, sin `--force` por ahora; nombre de cliente con patrón seguro exigido, sin normalización; entregar core `create_client(...)` primero, con capa CLI fina encima). No se escribió código ni artefactos de feature todavía, solo se acordó el plan. Ver D-016.
- T-016 completada: sesión de gobernanza/reconciliación documental (sin código de aplicación). El usuario introdujo `980_guideline/methodology.md` y `980_guideline/principles.md`, que describían una arquitectura runtime más ambiciosa (patrón Governor/Planificador/Evaluator, planos MOTOR/INSTANCIA, bandas Tracer Bullet) totalmente ausente de lo construido, además de duplicaciones y contradicciones internas. Se reparó `principles.md` como canon vinculante (P1-P8, E1-E12, NC-1...NC-6) y se insertó en los 10 agentes de `.claude/agents/` una cláusula que obliga a leerlo primero; se importó en `CLAUDE.md` §0 vía `@980_guideline/principles.md`. Se reorganizó `methodology.md` con un mapa de fuentes por tema (elimina duplicación con `system_design.md` y `sdd_tdd_workflow.md`) y se corrigieron sus inconsistencias internas. Se formalizaron dos citas fantasma como ADR reales: D-017 (protocolo de construcción por celda dimensionado por banda) y D-018 (invariante de independencia de tres contextos frescos + orden test-first RED-GREEN-REFACTOR). Se resolvió el choque entre la carpetería step-céntrica de D-017 y la feature-céntrica `600_features/` ya en uso: D-019 adopta `600_features/<feature>/<banda>/` (banda como subcarpeta, banda por defecto `tracer_bullet`), actualizando `sdd_tdd_workflow.md`, `600_features/README.md`, `600_features/_template/state.json` y las rutas de artefacto de los 8 agentes. Quedó registrado (erróneamente, ver T-019) como trabajo futuro construir los agentes runtime y reconciliar el plano runtime completo. Ver D-017, D-018, D-019, L-009, L-010, L-011.
- T-019 completada: sesión de reversión crítica del runtime agéntico (sin código de aplicación). Tras T-016, el usuario reportó sentirse perdido y percibió la metodología importada como complejidad agregada. Se volvió a `700_architecture/system_design.md` (D-006) y se confirmó que el runtime ya estaba resuelto ahí de forma simple: código Python determinista, multi-tenant (`clients/<NAME>/`), orquestador = CLI, LLM aislado solo en Discovery/Exploration. La metodología importada (de otro harness) había introducido un runtime agéntico (patrón A/B/C: governor/planner/evaluator por flujo) y planos MOTOR/INSTANCIA externos (`fda-*`, `install.sh`, `fda-*-state.json`) que contradicen ese diseño y violan E4/NC-2, además de citar D-005/D-009/D-010 con significados que no coinciden con `decisions.md`. Se decidió (D-020, aprobada explícitamente por el usuario) que el runtime lo define exclusivamente `system_design.md` y NO es agéntico: se descartan del harness el patrón A/B/C-runtime, los agentes foda-governor/planner/evaluator, MOTOR/INSTANCIA, install.sh y fda-*-state.json. Los únicos agentes de IA son los 8 de desarrollo (D-008). Se rescataron solo dos piezas de la metodología: la Single Writer Rule como norma de persistencia (D-021, sin adoptar los 3 archivos fda-*) y una rúbrica de evaluación calibrada limitada a salidas NO deterministas — documentos de Discovery, propuesta de features de Exploration, evaluación temprana de calidad ML (D-022); el código determinista sigue con tests + veredicto binario de `spec_verifier`. Se reescribió `980_guideline/methodology.md` de ~350 líneas a una "Metodología de Desarrollo del Motor" enfocada (§0 Propósito+Mapa de fuentes, §1 Ciclo SDD+TDD, §2 Gates, §3 Persistencia de desarrollo, §4 Evaluación, §5 Evolución/Mínima Complejidad, Apéndice de Estándares), eliminando todo el contenido A/B/C-runtime, MOTOR/INSTANCIA y las citas fantasma. T-017/T-018 quedan canceladas; nueva tarea de backlog T-020 (diseñar rúbricas concretas al construir Discovery/Exploration). `principles.md`, la cláusula vinculante en los 10 agentes y la carpetería por banda (D-017/D-018/D-019) se conservan sin cambios (son de desarrollo). T-013 sigue pendiente sin empezar. Ver D-020, D-021, D-022, L-012, L-013, L-014.
- T-013 arrancada: se ejecutaron las 3 primeras etapas de la cadena SDD/TDD sobre `client_scaffold`. (1) `feature_definer` creó `600_features/client_scaffold/tracer_bullet/definition.md` y `state.json` (commit `453a386`); concretó las capas medallion `data/{bronze,silver,gold}/` + `models/`, verificado como canónico contra `system_design.md` §7/§10 (no fue decisión silenciosa, ver L-015). (2) `spec_writer` creó `spec.md` (commit `bb3b60a`) resolviendo DS-1 (patrón de nombre, D-023), DS-2 (atomicidad por validación-primero + limpieza best-effort, D-024) y DS-3 (ubicación/firma del core y tipos de error, D-025), con 11 criterios de aceptación test-mappables; **GATE humano APROBADO**, aceptando explícitamente como límites conocidos de la banda `tracer_bullet` los filesystems case-insensitive (A-007) y los nombres reservados de Windows (A-008). (3) `plan_builder` creó `plan.md` con 10 pasos y 18 casos TDD ordenados (commit `0c2d682`); **GATE humano APROBADO**, confirmando adoptar PyYAML (PA-1, D-026), hacer el bootstrap del paquete `foda` dentro de esta feature en vez de como tarea separada (PA-2, D-027), e implementar el caso 18 (rollback de filesystem) sin test en esta banda (PA-3, D-028). La sesión se SUSPENDIÓ antes de arrancar el bucle TDD. T-013 pasa de Pendiente a En Progreso. Ver D-023 a D-028, L-015, A-007, A-008.
- Conversación de gobernanza posterior al cierre anterior (sin código ni docs editados): el usuario expuso su vocabulario habitual de bandas (tracer_bullet / estabilización / MVP / evolución / final) y se detectó que mezclaba dos ejes ortogonales: madurez de UNA feature (vertical) vs. crecimiento del PRODUCTO agregando features (horizontal). Se acordó D-029 (bandas verticales `tracer_bullet → stab_n`; MVP/Final = hitos de producto emergentes, no bandas; evolución = features nuevas con su propio tracer_bullet; solo se adopta el vocabulario ahora, la maquinaria de hitos queda para después, E4/NC-2) y D-030 (`feature_contract` obligatorio a nivel feature, creado por `feature_definer`, materializa P5; `slice_contract` por celda queda diferido, con nota abierta de posible fusión futura con `spec.md`; `client_scaffold` queda temporalmente no-conforme por no tener `feature_contract`, pendiente de retro-ajuste). Se creó la tarea T-021 (alta prioridad) para aplicar ambas decisiones en `sdd_tdd_workflow.md`, `methodology.md`, `600_features/README.md`, la plantilla `feature_contract.md`, `feature_definer.md` y el retro-ajuste de `client_scaffold`. T-013 permanece EN PROGRESO, suspendida antes del bucle TDD. Ver D-029, D-030.
- T-021 completada (ítems #2-#6, cierra la tarea): (2) `980_guideline/methodology.md` — añadido en §1 el bloque "Contratos explícitos en dos niveles (P5, D-030)" (el documento no mencionaba contratos tras el recorte de D-020, así que la "reconciliación" fue introducir el modelo, L-017). (3) `600_features/README.md` — árbol de directorios y "Cómo arrancar" reflejan `feature_contract.md`; corregida referencia colgante `§6`→`§7`. (4) Creada plantilla `600_features/_template/feature_contract.md`. (5) `.claude/agents/feature_definer.md` — nuevo paso "Escribir feature_contract.md", regla de uno por feature, pasos renumerados. (6) Creado `600_features/client_scaffold/feature_contract.md` (retro-ajuste). Ver D-029, D-030, L-017.
- T-022 completada (nueva, dentro de la misma sesión): a pedido del usuario, se codificó la cadena de trazabilidad `definition.md`→`spec.md`→`plan.md` (D-031). `definition.md` usa historias de usuario `HU-xx` (Connextra); `spec.md` codifica `CA-xx` enlazados a ≥1 `HU-xx` con matriz de cobertura HU→Spec; `plan.md` descompone en tareas atómicas `TSK-xx` (ID/Descripción/Entregable/Responsable/Estado/Trazabilidad→CA), con reglas de partición (un responsable, un entregable, código y test separados; responsable ∈ {tdd_tester, tdd_coder, tdd_refactor, integration_tester, humano}; el responsable es el único escritor de su campo Estado, alineado con la Single Writer Rule D-021). El bucle TDD sigue corriendo por caso de `state.json`, sin cambios de esquema ni de mecanismo (cambio mínimo, NC-2/NC-3): las TSK son una capa de trazabilidad pura. Se actualizaron las 3 plantillas, `feature_definer.md`, `spec_writer.md`, `plan_builder.md` y `sdd_tdd_workflow.md` (v0.2→v0.3, regla transversal §8). Se retro-ajustó `client_scaffold/tracer_bullet/`: `definition.md` (HU-01…HU-05), `spec.md` (CA-01…CA-11 + matriz), `plan.md` (nueva sección de tareas TSK-01…TSK-13, 18 casos mapeados a CA y TSK). El contenido sustantivo de los 18 casos no cambió. Ver D-031, L-018, A-009.
- Nueva sesión: el humano re-confirmó el GATE del `plan.md` retro-ajustado de `client_scaffold` (A-009 pasa a Validado). Se ejecutó el bucle TDD completo sobre los casos 1-17 del plan con la terna `tdd_tester`→`tdd_coder`→`tdd_refactor` (5 casos "verde directo": 3, 5, 8, 15, 17, documentados sin forzar rojos artificiales, L-020), cerrando con 26 tests en verde y sin regresiones. Se hizo el bootstrap del paquete `foda` (`pyproject.toml` con src-layout y pytest `pythonpath=["src"]`, `.gitignore`) y se construyó `src/foda/core/scaffold.py` (`create_client(name, clients_root) -> Path` + `_validate_name`) implementando la validación completa de DS-1 (7 reglas consolidadas en una whitelist `re.compile(r"[A-Za-z0-9_-]{1,64}")`, L-021), la creación del árbol de scaffold (`010_inputs/020_outputs/data/{bronze,silver,gold}/models`) y la escritura de `client.yaml`. Se resolvió el GATE humano PA-3 sobre el caso 18 (rollback best-effort DS-2.2): el humano eligió posponerlo a una banda `stab_n` futura (D-032, refina D-028); tracer_bullet cierra solo con la estrategia validación-primero (DS-2.1). `state.json` quedó con `tdd.status:"done"` y `current_stage` a nivel feature en `"integration_tester"`. Esta es la primera ejecución de punta a punta del bucle TDD de la cadena de 8 agentes, validando en gran medida A-005. Ver D-032, L-019, L-020, L-021, A-005, A-009.
- T-013 CERRADA: `integration_tester` sobre `client_scaffold` añadió `tests/integration/test_client_scaffold_integration.py` con 6 tests (árbol vs `system_design.md` §7, aislamiento multi-tenant §13, `client.yaml` como contrato consumible por un lector externo, fallo temprano si no existe cliente, creación secuencial sin estado compartido, colisión con archivo preexistente), dejando `Flow.run`/`ClientContext`/CLI fuera de alcance documentado; suite 32 passed (26 unit + 6 integración). `spec_verifier` recorrió los 11 CA de `spec.md`, produjo `600_features/client_scaffold/tracer_bullet/verification.md` con matriz de trazabilidad CA→test y emitió veredicto **CONFORME**; registró 3 hallazgos no bloqueantes (F-1 desalineación de entorno Python 3.12.10 local vs R1/D-010 que exige 3.13+, F-2 capa CLI TSK-07 no construida, F-3 `created_at` usa fecha local en vez de UTC). El humano validó la evidencia corriendo la suite por su cuenta (32 passed confirmado).
- Nueva feature `client_new_cli` (T-023, cierra el hallazgo F-2 de `client_scaffold`): tras 3 decisiones vinculantes con el humano (D-034 cadena SDD/TDD completa, D-035 la CLI sí lleva tests propios pese al No-Objetivo previo de `client_scaffold` porque NC-5 "sin excepción" prevalece, D-036 resolución de `clients_root` buscando `pyproject.toml` hacia arriba desde el cwd), se ejecutaron `feature_definer` (5 HU), `spec_writer` (10 CA, GATE humano APROBADO; resolvió sin marcador→código 1, `clients/` creado por la CLI con mkdir idempotente, `argparse` de stdlib, convención de códigos 0/1/2, `[project.scripts]`) y `plan_builder` (15 TSK, 12 casos TDD, GATE humano APROBADO). El humano decidió SUSPENDER antes de arrancar el bucle TDD; `state.json` de `client_new_cli` queda `status:"suspended"` con bloque de reanudación (invocar `tdd_tester` con el caso 1).
- Séptimo cierre (nueva sesión): se reanudó `client_new_cli` y se ejecutó el **bucle TDD completo** de sus 12 casos (`tdd_tester`→`tdd_coder`→`tdd_refactor`). 5 casos con ciclo rojo→verde real: caso 1 (camino feliz, crea `src/foda/cli.py` con `main`, parser argparse, `_find_project_root` que busca `pyproject.toml` hacia arriba desde el cwd por D-036, delega en `create_client`); caso 7 (sin `pyproject.toml` → mensaje a stderr + `return 1`, DS-CLI-1); caso 8 (NAME inválido → `try/except ValueError`, mensaje a stderr + `return 1`; el `tdd_coder` corrigió un defecto mecánico objetivo del test — aserción sobre `clients/..` que pathlib resolvía siempre a un directorio existente, L-025 — y se extrajo `_protect_dash_prefixed_name` en el refactor para manejar NAME con guion inicial); caso 9 (duplicado → `except FileExistsError`, mensaje distinto a ValueError por exigencia de la spec, D-038); caso 12 (entry point, `[project.scripts]` en `pyproject.toml`, cierre del bucle TDD). 7 casos verde-directo (2, 3, 4, 5, 6, 10, 11) documentados como cobertura de regresión sin forzar rojo artificial, reutilizando el patrón de L-020 ahora consolidado como práctica estándar del harness (D-037, L-024). Suite final: 46 passed sin regresiones. `state.json`: `stages.tdd.status:"done"`, 12/12 casos `done`, `current_stage:"integration_tester"`. Avance global sube de 72% a 78%. Ver D-037, D-038, L-024, L-025.
- Novena sesión del día (nueva sesión, sin código de features nuevas): sesión de **verificación de uso real de la CLI y corrección de entorno**. El usuario ejecutó `foda client new DEMO_ABC` tras `pip install -e .` y obtuvo `foda: The term 'foda' is not recognized`. Diagnóstico: el `.venv` existente estaba creado con Python 3.12.10 (no 3.13+, R1/D-010), por lo que `pip install -e .` fallaba con `Package 'foda' requires a different Python` y el comando `foda` nunca llegaba a instalarse; esto **confirma en la práctica** el hallazgo F-1 de `spec_verifier` sobre `client_scaffold` (D-033). Se recreó el `.venv` con Python 3.13.7 (`py -3.13 -m venv .venv`) y se reinstaló el paquete (`pip install -e .`, instala PyYAML 6.0.3 y `foda` 0.1.0, crea `foda.exe`). Verificación end-to-end exitosa: `foda client new DEMO_ABC` creó `clients/DEMO_ABC/client.yaml` e imprimió la ruta — primera vez que la CLI se ejecuta de punta a punta en un entorno real, fuera de la suite de tests (refuerza A-005, ver L-027). Se creó `README.md` en la raíz del proyecto (nuevo, no existía) documentando requisitos (Python 3.13+ obligatorio, con advertencia del error de versión), configuración inicial del entorno virtual paso a paso, uso de la CLI (`foda client new <NOMBRE>`, tabla de códigos de salida 0/1/2), uso diario del venv, sección "¿Cuándo reinstalar `foda`?" y cómo correr los tests (ver L-028). Se decidió no versionar la carpeta `clients/` (datos de runtime multi-tenant en disco, D-006): se añadió `clients/` y `.venv/` a `.gitignore` (D-039); el cliente de prueba `clients/DEMO_ABC/` permanece en disco a pedido explícito del usuario, pero fuera de Git. Sin decisiones ADR de diseño de producto nuevas; A-005 reforzado por evidencia práctica adicional. Avance global sin cambio (82%): sesión de verificación/documentación, sin construcción de feature nueva.
- Décima sesión (nueva sesión): T-014 (`client_context`), tercera feature en el orden abajo-hacia-arriba (D-016), construida y CERRADA/CONFORME de punta a punta. Se acordaron con el usuario 3 decisiones vinculantes para la banda `tracer_bullet` (D-040 modo nuevo/recurrente inferido del disco vía `models/latest`, sin flag editable en `client.yaml`; D-041 `clients_root` como parámetro del constructor, igual que `create_client`, sin que el core resuelva `pyproject.toml`; D-042 introspección de "qué artefactos existen" diferida a `flow_base`/T-015). Se ejecutó `feature_definer` (HU-01…HU-04), `spec_writer` (12 CA, 3 decisiones técnicas D-043 `FileNotFoundError` como marcador de existencia vía `client.yaml`, D-044 `is_recurring` booleano `== (models/latest).exists()`, D-045 constructor directo con validación en `__init__` y rutas como propiedades de solo lectura; GATE humano APROBADO) y `plan_builder` (16 TSK, 12 casos TDD; GATE humano APROBADO). Bucle TDD completo (`tdd_tester`→`tdd_coder`→`tdd_refactor`): 6 casos con ciclo real (construcción+name/root, inputs/outputs_dir, bronze/silver/gold_dir con refactor real que extrajo `_data_dir`, models_dir, `is_recurring` en falso, `FileNotFoundError` si no existe) y 6 casos verde-directo documentados (D-037), incluida una corrección de estado `state.json` "red"→"done" en el caso 10. `integration_tester` añadió 5 tests de integración (contrato de rutas vs. `create_client`, ciclo nuevo→recurrente end-to-end, superficie suficiente para `Flow.run(ctx)` con función local `_run_fake_flow`, fallo temprano con mensaje claro, aislamiento multi-tenant). `spec_verifier` emitió veredicto **CONFORME** (D-046) a los 12 CA con matriz de trazabilidad completa, registrando 2 hallazgos no bloqueantes: F-1 (patrón "verde directo" en 6/12 casos, D-037) y F-2 (limitaciones aceptadas: symlink `models/latest` roto se evalúa como inexistente ⇒ NUEVO, y filesystems case-insensitive heredan semántica del FS sin normalización; registradas como A-010/A-011). Suite final: 71 passed (12 unit nuevos + 5 integración nuevos + 54 preexistentes), sin regresiones; no se tocó código de producción previo (`create_client`/`scaffold.py` intactos, NC-3). Se aclaró con el usuario la distinción de dominio entre "nuevo vs. recurrente" (¿existe modelo?) y "reanudar donde se dejó" (¿qué pasos ya se hicieron?), quedando esta última fuera de alcance de `client_context` (diferida a `flow_base`, D-042). Avance global sube de 82% a 86%. Próxima tarea prioritaria: T-015 (`flow_base`). Ver D-040 a D-046, L-029, L-030, A-010, A-011.
- Duodécima sesión (nueva sesión, deliberación estratégica sin código): T-026 RESUELTA. Se deliberó con el usuario en términos de negocio las 3 opciones sobre la mesa (primer flujo concreto, `stab_1` de `flow_base`, orquestador `foda run`) y se decidió (D-054) que la próxima feature es `onboarding` (flujo 020), descartando por ahora Discovery (flujo 010) por ser núcleo LLM no determinista con T-020 (rúbricas) pendiente. Se acordó (D-055) que `onboarding` arranca simulando la salida de Discovery con un `contract_data.json` fabricado (fixture realista), dejando como supuesto provisional (A-012) que esa forma fija el contrato futuro de Discovery. Se confirmaron (D-056, D-057) la estrategia `tracer_bullet` y la estrategia de escalabilidad progresiva (arrancar con el caso más simple, endurecer jerarquías completas en bandas posteriores, apoyado en `system_design.md` §2.6/§15). Se acordó una hoja de ruta de 14 features de producto como brújula (no contrato cerrado): onboarding, ingestion, profiling, cleaning, derivation, discovery, exploration, featuring, modelling, inferences, simulation, reporting, monitoring, alerting; más infraestructura pendiente (foda_run, foda_status, foda_export, llm_client). Nueva tarea T-027 (alta prioridad): iniciar `onboarding` invocando `feature_definer`. Avance global sin cambio (90%): sesión de decisión estratégica, sin construcción. Ver D-054 a D-057, L-033, A-012.
- Decimoctavo cierre (nueva sesión): se inició T-028 (`flow_orchestrator`, banda `tracer_bullet`), sexta feature en recorrer la cadena de 8 agentes, ejecutando sus 3 primeras etapas. (1) `feature_definer` creó `600_features/flow_orchestrator/feature_contract.md` (nivel feature) y `600_features/flow_orchestrator/tracer_bullet/definition.md` con 5 HU-01…HU-05, e inicializó `state.json` (commit `c132a9b`). Alcance dentro: `foda run <cliente> --flow <flujo>` (ejecuta un flujo delegando en `Flow.run(ctx)` de `flow_base` y `ClientContext` de `client_context`) y `foda status <cliente>` (introspecciona en disco vía `Artifact.exists` qué artefactos `requires`/`produces` existen). Fuera de alcance, diferido explícitamente: `--from/--to`, `--pipeline`, `foda export`, y cualquier manifiesto de estado separado (descartado por D-021, Single Writer Rule — el estado es el disco). (2) `spec_writer` creó `spec.md` con 14 CA-01…CA-14 trazados a las 5 HU (commit `ed28ccd`), resolviendo 3 puntos abiertos con decisiones de diseño aprobadas sin cambios por el humano en el GATE: **DS-ORQ-1** módulo nuevo `src/foda/orchestrator.py` con registro literal `FLOWS = {"onboarding": Onboarding}` y `resolve_flow(name)`, fuera de `core/` para no invertir dependencias core→flujos (se descartó descubrimiento dinámico, sobre-diseño E4/NC-2). **DS-ORQ-2** `ValueError` para flujo desconocido (mismo patrón que `client_new_cli`). **DS-ORQ-3** `foda status` en texto plano determinista (una sección por flujo; por artefacto: rol, nombre, `[presente]`/`[ausente]`, ruta relativa), se descartó JSON por no haber consumidor de máquina en esta banda. **DS-ORQ-4** orden de validación flujo→cliente→ejecución; códigos de salida 0/1/2 alineados con `client_new_cli`; `run`/`status` NO crean `clients/` (a diferencia de `client new`). **GATE humano: el usuario APROBÓ la spec sin cambios.** (3) `plan_builder` creó `plan.md` con 20 TSK-01…TSK-20 y pobló `state.json` con 15 casos TDD ordenados (fundamental→complejo) en `tdd.cases`, trazados a CA-01…CA-14 (commit `774ba4c`). Enfoque: slice vertical mínimo que cablea piezas ya CONFORMES sin reimplementar lógica; tests nuevos en `tests/test_orchestrator.py` (unit del registro) y `tests/cli/test_flow_orchestrator_cli.py` (CLI independiente de la suite de `client_new_cli`, CA-14). `state.json` queda con `awaiting_approval:true`, `gate:human`, `current_stage:"plan_builder"`. **GATE humano del plan PENDIENTE**: la sesión se cerró en este punto, antes de arrancar el bucle TDD, con 3 puntos abiertos sugeridos por `plan_builder` para la revisión del plan (registrados como A-014, A-015, A-016). Suite de tests sin cambios (121 en verde); aún no se escribió código de producción ni tests de esta feature. Próxima tarea: tras el OK humano del plan, invocar `tdd_tester` con el caso 1 (`resolve_flow("onboarding")` devuelve instancia de `Onboarding`, CA-10). Ver D-062 a D-066, A-014 a A-016.
- Décimo noveno cierre (nueva sesión): T-028 (`flow_orchestrator`) CERRADA/CONFORME, cerrando las etapas 4-8/8 de la cadena SDD/TDD tras el GATE humano del plan (APROBADO SIN CAMBIOS, valida A-014/A-015/A-016). Bucle TDD completo (`tdd_tester`→`tdd_coder`→`tdd_refactor`) sobre los 15 casos: solo el caso 4 (camino feliz de `foda run`, construye `src/foda/orchestrator.py` con `FLOWS`/`resolve_flow` y `src/foda/cli.py` con subcomando `run`) y el caso 10 (camino feliz de `foda status`, subcomando `status`) requirieron ciclo rojo→verde real; los otros 13 casos nacieron "verde directo" (D-037) porque el diseño mínimo de esos dos casos ya satisfacía los criterios de error (flujo desconocido, cliente inexistente, `FlowContractError`, delegación en `flow.run`, extensibilidad vía `FakeFlow`/A-015, no-creación de `clients/`) — ver L-041. Refactors reales: caso 10 extrajo el helper `_build_client_context` (elimina duplicación entre `run` y `status`); TSK-20 (refactor final de banda) factorizó fixtures de test y actualizó el docstring de módulo de `cli.py`. `integration_tester` añadió `tests/integration/test_flow_orchestrator_integration.py` con 9 tests end-to-end con piezas REALES (orchestrator→cli→ClientContext→Flow.run→Onboarding→disco, incluido el entry point `foda` vía subprocess), sin defectos de integración, sin tocar `src/`. `spec_verifier` recorrió los 14 CA-01…CA-14 de `spec.md` releyendo cada test citado y emitió veredicto **CONFORME** (D-067) en `600_features/flow_orchestrator/tracer_bullet/verification.md`, sin hallazgos bloqueantes ni no bloqueantes. Se actualizó `README.md` (nuevo, ya no v0 sin comandos de orquestación) documentando `foda run <cliente> --flow <flujo>` y `foda status <cliente>` con descripción, uso, cuándo usarlos, salida real, códigos de salida, tabla-resumen de los tres comandos y flujo típico ampliado (crear→status→run→status). Suite final: 145 passed (121 previos + 24 nuevos: 15 unit/CLI + 9 integración), sin regresiones. `flow_orchestrator` es la SEXTA feature completa CONFORME de punta a punta del proyecto (tras `client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`); T-028 queda COMPLETADA. Alcance diferido documentado en la feature: `--from/--to`, `--pipeline`, `foda export`. Avance global sube a 100% del alcance de infraestructura+orquestador construido hasta ahora; queda pendiente decidir con el humano la siguiente feature de negocio del backlog de flujos deterministas. Ver D-062 a D-067, L-041.
- Vigésimo cierre (nueva sesión, sin código nuevo): **validación manual end-to-end de `flow_orchestrator` por el humano**, en una terminal real (Python 3.13.7, venv activado), ejercitando la CLI `foda` completa con un cliente propio (`COMPANY_001`) distinto a los fixtures de test. Se cubrieron los 8 escenarios de `spec.md`: creación de cliente, `foda status` antes/después de artefactos, `foda run` con fallo limpio por artefacto ausente (CA-06, sin Traceback ni salida parcial), éxito tras inyectar manualmente un `contract_data.json` válido (CA-01/CA-02), verificación manual de que el `map_client_data.json` resultante es correcto y determinista, cliente inexistente (CA-09), `run` sin `--flow` (CA-13, error de argparse código 2) y flujo desconocido (CA-04). Todos los resultados coincidieron con el comportamiento esperado; ningún defecto encontrado. Esta prueba **confirma en producción**, desde una terminal real y con datos de cliente personalizados, el veredicto CONFORME emitido por `spec_verifier` (D-067) sobre `flow_orchestrator`. Se aclaró (consultando `system_design.md`) que `contract_data.json` es la salida futura del flujo `010_discovery` (aún no construido, solo existe `f020_onboarding`); hoy hay que inyectarlo a mano como stand-in tipo tracer bullet, y el usuario decidió guardar su copia de ejemplo fuera del repositorio (coherente con `clients/` en `.gitignore`, D-039). El cliente de prueba `COMPANY_001` fue borrado tras la validación; el working tree quedó limpio, sin cambios de código ni de tests que commitear. Ver L-042.
- Vigésimo primer cierre (nueva sesión): T-031 (`ingestion`, flujo 030) ARRANCADA, séptima feature de la cadena, elegida sobre `discovery` para consolidar terreno determinista (D-068). Etapas 1-3/8 completadas: `feature_definer` (HU-01…HU-06, commit `b636db4`), `spec_writer` (CA-01…CA-21, GATE APROBADO, commit `942d4a3`) y `plan_builder` (22 casos TDD, commit `6d4b81b`, completando el commit tras un corte de sesión, L-043). En el GATE humano del plan, el humano pidió (DS-ING-8/D-077) un reparto de responsabilidad entre `contract_data.json` (fuente del conjunto de archivos esperados) y `map_client_data.json` (fuente de las columnas esperadas por archivo), sin chequeo de coherencia entre ambos, reemplazando la decisión previa de "solo el mapa"; esto obligó a reabrir `spec_writer` (spec enmendada, commit `baf8c38`) y re-ejecutar `plan_builder` (re-plan, commit `981e3bf`), preservando el orden/numeración de los 22 casos (L-044). Se arrancó el bucle TDD: caso 1/22 (CA-14) cerrado con ciclo rojo→verde→refactor completo (`tdd_tester` creó el esqueleto `Ingestion(Flow)` y el test, commit `cb8dbfd`; `tdd_coder` implementó `load_inputs`/`execute`/`write_outputs` mínimos que parsean contrato+mapa, leen un dataset separado por coma y escriben `ingestion_report.json` determinista, verde en 1er intento, commit `c8481a0`; `tdd_refactor` decidió no tocar código, ya mínimo, y solo cerró el caso en `state.json`, commit `52b5fa6`). A pedido explícito del humano, la sesión se detiene tras el caso 1; quedan 21/22 casos pendientes, retoma en `tdd_tester` con el caso 2 (CA-20). Suite final: 146 passed (145 previos + 1 nuevo), sin regresiones. Avance global: séptima feature en progreso (no se expresa como % único, ver §2). Ver D-068 a D-077, L-043 a L-045, A-017.
- Décimo séptimo cierre (nueva sesión): T-027 (`onboarding`) CERRADA/CONFORME, cerrando las 2 etapas finales de la cadena SDD/TDD. `integration_tester` añadió `tests/integration/test_onboarding_integration.py` con 5 tests: (1) end-to-end sobre un `ClientContext` real comparando `map_client_data.json` contra un esperado fijo derivado a mano de `spec.md` (no contra la propia implementación); (2) invariante CA-12: `run` exitoso no toca bronze/silver/gold del cliente real; (3) fallo temprano con `FlowContractError` claro si falta `contract_data.json`, sin salida parcial; (4) contrato de artefactos entre flujos (§8): un flujo vecino de juguete declara como `require` el `map_client_data.json` de Onboarding y lo consume sin fallar; (5) aislamiento multi-tenant entre dos clientes reales. Suite: 121 passed (116 previos + 5 nuevos), sin regresiones; no fue necesario tocar `src/foda/flows/f020_onboarding/onboarding.py` (commit `9dcfdcc`). `spec_verifier` creó `verification.md` con matriz de trazabilidad y emitió veredicto **CONFORME**: 22/22 CA cubiertos, cero parciales, cero sin cubrir; in scope/out of scope y restricciones de `definition.md` respetados; 1 hallazgo NO bloqueante OBS-1 (regla "`field.required` es booleano" del Comportamiento Esperado 2b de `spec.md` sin CA/test/implementación explícita, candidato a `stab_1`; commit `a742a04`). `onboarding` es la QUINTA feature completa CONFORME de punta a punta; T-027 queda COMPLETADA. Deliberación estratégica posterior con el humano (sin código): se aclaró que no hay que esperar a los 14 flujos para que el humano pueda probar/inspeccionar (cada flujo deja un artefacto revisable, `system_design.md` §5, D-006/D-014/E6); se identificó que hoy no se puede disparar un flujo desde terminal porque el orquestador `foda run`/`foda status` (§11) fue diferido (D-054/T-026). Decisión B: la siguiente feature es **`flow_orchestrator`**, banda `tracer_bullet` acotada a `foda run <cliente> --flow <flujo>` + `foda status <cliente>` (rangos `--from/--to`, `--pipeline` y `foda export` diferidos). Avance global sube de 98% a 99%. Nueva tarea T-028 (alta prioridad): iniciar `flow_orchestrator` invocando `feature_definer`. Ver D-061, D-062, L-040.
- Vigésimo segundo cierre (nueva sesión): se avanzó el bucle TDD de `ingestion` de 2/22 a **11/22 casos cerrados** (casos 3-11). Ciclo completo rojo→verde→refactor en los casos 4 (CA-02, `_detect_separator`/`_read_delimited`), 6 (CA-04, `_read_xlsx`/`_read_file` + dependencia `openpyxl` en `pyproject.toml`, TSK-01), 8 (CA-11, `_copy_bytes`/`self._bronze_copies`) y 11 (CA-06, verificación de existencia de archivo declarado antes de leerlo, helpers `_missing_file_entry`/`_ingested_file_entry`); verde directo (D-037/D-060) en los casos 3 (CA-01), 5 (CA-03), 7 (CA-15, con fixture completo DS-ING-7), 9 (CA-12) y 10 (CA-05). Durante el refactor del caso 6 se corrigió además una coma faltante preexistente en `state.json` (JSON inválido de una sesión anterior). Suite final: 156 passed (147 previos + 9 nuevos), sin regresiones. Todos los commits de esta sesión estaban locales hasta el cierre. Quedan 11/22 casos pendientes; retoma en `tdd_tester` con el caso 12 (CA-07). Ver D-068 a D-077, L-043 a L-047, A-017.
- Vigésimo tercer cierre (nueva sesión): bucle TDD de `ingestion` COMPLETADO, de 11/22 a **22/22 casos cerrados** (casos 12-22), cerrando las etapas 4-6/8 de la cadena. Ciclo completo rojo→verde→refactor en los casos 12 (CA-07, `unexpected_files`/`unexpected_file`), 13 (CA-08, `missing_column`, refactor consolidó `_read_file` en una sola pasada), 14 (CA-09, `unexpected_column`, simetría `_missing_required_columns`/`_unexpected_columns`), 16 (CA-16, lista top-level `inconsistencies[]`, helper `_top_level_inconsistencies`) y 19 (CA-18, inconsistencia parcial, ROJO GENUINO: `bronze_path` estaba hardcodeado a `None`, corregido con helper `_bronze_relative_path`). Verde directo (D-037/D-060) en los casos 15 (CA-10), 17 (CA-17), 18 (CA-19), 20 (CA-13, determinismo), 21 y 22 (CA-21, `FlowContractError` por ausencia de `contract_data.json`/`map_client_data.json`). **Decisión de gobernanza D-078/DS-ING-9** (enmienda de DS-ING-2): el caso 16 hizo aflorar una contradicción real de spec (vocabulario cerrado incluía `unexpected_file` sin esquema que lo alojara); `tdd_tester` se detuvo (NC-1/NC-6), el humano decidió materializar una lista top-level `inconsistencies[]` con `{type, detail}`; se enmendó `spec.md` (DS-ING-2, DS-ING-9, CA-16) antes de continuar, saldando la deuda documentada de los casos 11/12. Suite final: 167 passed (156 previos + 11 nuevos), sin regresiones. `state.json`: `stages.tdd.status:"done"`. Próxima tarea: invocar `integration_tester` (etapa 7/8), luego `spec_verifier` (etapa 8/8). Ver D-078, L-048, L-049.
- Vigésimo cuarto cierre (nueva sesión): T-031 (`ingestion`) CERRADA/CONFORME, cerrando las etapas finales 7-8/8 de la cadena. `integration_tester` añadió `tests/integration/test_ingestion_integration.py` con 7 tests contra el sistema REAL (no fixtures aislados): end-to-end sobre `ClientContext` real con `map_client_data.json` de una corrida real de `Onboarding` + `contract_data.json` fabricado (contrato multi-flujo D-014); invariante de que el éxito no toca silver/gold; fallo temprano → `FlowContractError` claro (no excepción cruda) si falta `contract_data.json` o `map_client_data.json` por separado; `ingestion_report.json` consumible por un flujo vecino de juguete (patrón SS8); inconsistencia parcial real (CA-18, copia solo válidos a bronze, 1 rejected excluido); aislamiento multi-tenant entre dos `ClientContext` reales. 7 passed al primer intento, sin defectos, sin cambios de código de producción; suite 174 passed (167+7) sin regresiones (commit `1945729`). `spec_verifier` recorrió los 21 CA-01…CA-21 con matriz de trazabilidad 21/21 cubiertos (0 parciales, 0 sin cubrir), verificó restricciones (Python 3.13+, JSON determinista, LLM aislado, `openpyxl` autorizada, core no ampliado NC-3, enmienda D-078 verificada en CA-16), corrió la suite completa (174 passed) y emitió veredicto **CONFORME**, produciendo `600_features/ingestion/tracer_bullet/verification.md` (nuevo, commit `d8dc9bf`). `state.json`: `status:"done"`, `current_stage:"completed"`. `ingestion` es la SÉPTIMA feature completa CONFORME de punta a punta (tras `client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`, `flow_orchestrator`). T-031 queda COMPLETADA. Sin decisiones ADR nuevas ni bloqueos. Ver D-068 a D-078, L-050.

## 4. En Progreso
- _Ninguna._ `ingestion` (T-031/T-032/T-033) cerrada CONFORME, cableada al CLI y validada por prueba humana de punta a punta; T-035 (exit code de `foda run`) completada a nivel código/test; sin tareas en progreso.

## 5. Próximo a Realizar
- (Alta prioridad) T-034: implementar el flujo de ramas por feature + merge por PR (D-079, alcance resuelto por D-081) — 7 ajustes pendientes en `CLAUDE.md`, `feature_definer.md`, `session_closer.md`, `session_starter.md`, `methodology.md`, plantilla `state.json`; se implementa directo sobre `main` (punto de alcance ya resuelto por D-081), con el contrato "automatización hasta el PR, merge humano" como guía de diseño.
- Prueba humana pendiente de T-035 (correr `ingestion` con datos inconsistentes vía CLI y verificar exit code 1); el usuario decidió no hacerla en la sesión que cerró T-035.
- (Media prioridad) T-036: implementar el gate de progresión entre flujos por `success` del predecesor + flag `--force` (puntos 1-3 del ADR D-080), diferida hasta que exista un primer flujo consumidor de un reporte de `ingestion` en adelante (no hay consumidor todavía, probablemente `discovery`).
- (Baja prioridad, backlog) T-037: comando encadenado tipo `foda run <cliente> --flow onboarding→ingestion`; depende de T-036 (gate de progresión).
- Decidir con el humano la siguiente feature de negocio a construir (ver punto siguiente).
- Al iniciar la siguiente feature, tener presente el caso `client_register` (comparación de datos reales contra el registro de Discovery) diferido a banda futura (A-017), y las variantes diferidas a `stab_1` de `ingestion`: medios database/api (D-069), validación de tipos de columna (D-072), archivo de una sola columna sin separador detectable, kinds de dataset no ejercitados, combinaciones separador×extensión redundantes.
- Tras `ingestion`, decidir con el humano la siguiente feature de negocio del bloque determinista firme (profiling, cleaning, derivation, featuring, inferences, simulation, reporting, monitoring, alerting); discovery, exploration (LLM) y modelling (ML) quedan sujetas a decisiones futuras (T-020 pendiente para sus rúbricas).
- Para cualquier prueba manual futura de `onboarding`/`flow_orchestrator` antes de que exista `discovery`: se necesita un `contract_data.json` válido; usar como referencia canónica el fixture `_contrato_valido` de `tests/cli/test_flow_orchestrator_cli.py` o `tests/flows/test_onboarding.py` (ver L-042).
- Instalar el paquete (`pip install -e .`) en cualquier entorno de desarrollo nuevo requiere primero un `.venv` creado con Python 3.13+ (ver README.md, L-027); documentado, sin acción pendiente salvo recordarlo a futuros colaboradores.
- T-024 (backlog): banda `stab_n` de `client_scaffold` — test + implementación del rollback best-effort DS-2.2 (caso 18 diferido por D-032).
- T-025 (backlog, de F-3): evaluar mover `created_at` de `client.yaml` a UTC en una banda de estabilización futura.
- T-029 (backlog, de OBS-1): evaluar codificar como CA formal la regla "`field.required` es booleano" en una banda `stab_1` de `onboarding`, con su test correspondiente.
- Validar en una sesión nueva que el import `@980_guideline/principles.md` en `CLAUDE.md` efectivamente carga el archivo en el contexto de la sesión principal (A-006).

## 6. Bloqueos y Riesgos
- _Ningún bloqueo de gobernanza activo._ `ingestion` cerró CONFORME de punta a punta (8/8 etapas SDD/TDD), ya cableada al CLI y validada por prueba humana; no quedan checkpoints SDD/TDD pendientes.
- **Riesgo resuelto:** `foda run` ya refleja `success=false` en su código de salida (T-035, D-080 punto 4); queda pendiente solo la prueba humana de confirmación.
- **Riesgo identificado (no bloqueante):** `ingestion` no limpia `data/bronze/` entre corridas (gap de idempotencia); no se ha manifestado en pruebas con clientes frescos, pero queda anotado.
- **Gobernanza implementada:** la política de ramas por feature + PR (D-079/D-081) ya está aplicada (T-034, 2026-07-09); a partir de la próxima feature nueva, se trabajará en `feature/<nombre>` con merge por PR y gates humanos `human_test`/`merge_to_main`. El gate de progresión entre flujos por `success` (D-080, puntos 1-3) sigue pendiente de implementar (T-036), diferido hasta que exista un flujo consumidor.
- (El riesgo previo de complejidad runtime agéntica no reconciliada fue resuelto por D-020: T-017/T-018 canceladas.)

## 7. Historial de Actualizaciones
| Fecha | Cambio |
|---|---|
| 2026-07-01 | Creación inicial del archivo. |
| 2026-07-01 | Cierre de sesión: andamiaje del proyecto (persistencia, CLAUDE.md, Git, skills). |
| 2026-07-01 | Sesión de verificación: sin trabajo nuevo, sigue a la espera de que el usuario defina el alcance del proyecto (T-002). |
| 2026-07-01 | Migración de protocolos de skills a subagentes (`session_starter`, `session_closer`); eliminación de skills antiguas `foda-next`/`foda-status`; sesión principal fijada en Opus 4.8. Sigue pendiente T-002. |
| 2026-07-01 | T-002 completada: alcance definido a partir de `990_documents/expected_workflow.md` y `current_state.md`. Se creó `700_architecture/system_design.md` v0.1 con el diseño de arquitectura del sistema (14 flujos, determinismo por defecto, LLM aislado, multi-tenant en disco, CLI en Python). Pendiente validación del documento con el usuario antes de iniciar desarrollo. |
| 2026-07-01 | Sesión de diseño (sin código): se acordó la cadena de 8 agentes de desarrollo SDD/TDD y su orquestación (gates humanos, bucle TDD, checkpoint en disco vía `state.json`, `600_features/`). Registradas T-009, T-010, T-011 como pendientes; T-008 sigue vigente. Ver D-008, L-005, A-005. |
| 2026-07-01 | Sesión de mantenimiento (T-012): corregidas referencias colgantes a skills eliminadas en `session_starter`/`session_closer`; eliminada duplicación del protocolo entre `CLAUDE.md` y los agentes (fuente única de verdad en los agentes); invocación por frase-gatillo. Sin cambios de código ni de arquitectura. Ver D-009, L-006. |
| 2026-07-01 | T-008 completada: `700_architecture/system_design.md` validado sección por sección con el usuario (16 secciones, 5 bloques) y actualizado de v0.1 a v0.2. Cambios: Python 3.13+ (R1), modelos ML versionados con puntero `latest`, exports dentro de `020_outputs/<flujo>/`, contratos de artefactos ajustados + nota de dependencias multi-flujo, LLM por defecto = API de Anthropic (Claude). A-004 queda validado. Desbloqueada la construcción incremental (T-009 en adelante). Ver D-010 a D-014, L-007. |
| 2026-07-02 | T-009, T-010 y T-011 completadas: construidos los 8 agentes de desarrollo SDD/TDD en `.claude/agents/`, documentada la cadena en `700_architecture/sdd_tdd_workflow.md` (v0.1), y creada la estructura `600_features/` con plantilla. Renombrado `tdd_red`→`tdd_tester`, `tdd_green`→`tdd_coder` respecto a D-008 (ver D-015). Avance global sube de 35% a 48%. Nueva tarea pendiente T-013: construir la primera feature real (valida A-005). Ver D-015, L-008. |
| 2026-07-02 | Sesión corta de continuación: se acordó con el usuario que la primera feature real (T-013) será `client_scaffold` (`foda client new`), con orden de construcción abajo-hacia-arriba (client_scaffold → client_context → flow_base → flujos). Alcance in/out definido y 3 decisiones de alcance resueltas. Sin código ni artefactos de feature aún. T-014 y T-015 registradas en backlog. Avance global sube de 48% a 50%. Ver D-016. |
| 2026-07-02 | T-016 completada: sesión de gobernanza/reconciliación (sin código de aplicación). Se repararon y reconciliaron `980_guideline/principles.md` (canon vinculante P1-P8/E1-E12/NC-1...NC-6) y `980_guideline/methodology.md` (mapa de fuentes, eliminación de duplicaciones y contradicciones) con lo ya construido. Se insertó cláusula vinculante en los 10 agentes y se importó `principles.md` en `CLAUDE.md` §0. Se formalizaron D-017 y D-018 (citas fantasma del `methodology.md`) y se resolvió el choque de carpetería con D-019 (`600_features/<feature>/<banda>/`), actualizando `sdd_tdd_workflow.md`, `600_features/README.md`, `_template/state.json` y las rutas de los 8 agentes. T-013 sigue pendiente sin empezar. Nuevas tareas T-017/T-018 en backlog (agentes runtime y plano runtime). Avance global sube de 50% a 52%. Ver D-017, D-018, D-019, L-009, L-010, L-011. |
| 2026-07-02 | T-019 completada: reversión crítica del runtime agéntico importado por la metodología (sin código de aplicación). El usuario reportó sentirse perdido con la complejidad agregada; se contrastó contra `system_design.md` (D-006, ya resolvía el runtime como determinista) y se decidió (D-020, aprobada por el usuario) que el runtime NO es agéntico: se descartan el patrón A/B/C-runtime, foda-governor/planner/evaluator, MOTOR/INSTANCIA, install.sh y fda-*-state.json. `methodology.md` recortado a "Metodología de Desarrollo del Motor". Se rescataron solo dos piezas: Single Writer Rule (D-021) y rúbrica de evaluación calibrada limitada a salidas no deterministas (D-022). T-017/T-018 canceladas; nueva tarea de backlog T-020. `principles.md` y la carpetería por banda se conservan. T-013 sigue pendiente. Avance global sube de 52% a 54%. Ver D-020, D-021, D-022, L-012, L-013, L-014. |
| 2026-07-02 | T-013 arrancada: ejecutadas las 3 primeras etapas de la cadena SDD/TDD sobre `client_scaffold` (feature_definer, spec_writer, plan_builder), con ambos GATEs humanos (tras spec y tras plan) APROBADOS. Se resolvieron DS-1/DS-2/DS-3 (D-023 a D-025) y se confirmaron PA-1/PA-2/PA-3 (D-026 a D-028): adoptar PyYAML, bootstrap del paquete `foda` dentro de esta feature, caso TDD 18 sin test en tracer_bullet. Se verificó que las capas medallion del `feature_definer` son canónicas contra `system_design.md` (L-015). Sesión suspendida antes del bucle TDD; T-013 pasa de Pendiente a En Progreso. Avance global sube de 54% a 58%. Ver D-023 a D-028, L-015, A-007, A-008. |
| 2026-07-02 | Segundo cierre de la misma sesión: conversación de gobernanza posterior (sin código ni docs editados). Se acordaron D-029 (vocabulario de bandas: eje vertical `tracer_bullet → stab_n` por feature; MVP/Final = hitos de producto emergentes, no bandas; evolución = features nuevas) y D-030 (contratos en dos niveles: `feature_contract` obligatorio por feature, creado por `feature_definer`; `slice_contract` por celda diferido, con nota de posible fusión futura con `spec.md`). Se creó T-021 (alta prioridad) para aplicar ambas decisiones en la documentación y retro-ajustar `client_scaffold` con su `feature_contract`, ANTES de reanudar el bucle TDD de T-013. Avance global sin cambio (58%): sesión de gobernanza sin construcción. Ver D-029, D-030. |
| 2026-07-02 | Tercer cierre de la misma sesión: T-021 arrancada, ítem #1/6 completado. `700_architecture/sdd_tdd_workflow.md` actualizado de v0.1 a v0.2: nueva sección §6 «Bandas y Ejes de Crecimiento» que formaliza D-029 (eje vertical `tracer_bullet → stab_n`, eje horizontal = hitos de producto emergentes MVP/Final sin carpetas, evolución = features nuevas, alcance de adopción limitado al vocabulario vertical por E4/NC-2), renumeración de secciones §6-§9 → §7-§10 y ajuste de índice/referencias cruzadas/menciones de banda. Cambio quirúrgico (NC-3), sin tocar la lógica de la cadena de agentes. T-013 sigue suspendida a la espera del cierre de T-021 (quedan ítems #2-#6). Avance global sin cambio (58%). |
| 2026-07-02 | Cuarto cierre de la misma sesión: T-021 COMPLETADA (ítems #2-#6): `methodology.md` §1 con el modelo de contratos en dos niveles (feature_contract/slice_contract, L-017); `600_features/README.md` refleja feature_contract y corrige referencia colgante §6→§7; plantilla `_template/feature_contract.md` creada; `feature_definer.md` responsable de crearlo; retro-ajuste `600_features/client_scaffold/feature_contract.md`. Nueva tarea T-022 COMPLETADA en la misma sesión: trazabilidad codificada HU→CA→TSK (D-031) — plantillas de `definition.md`/`spec.md`/`plan.md`, agentes `feature_definer`/`spec_writer`/`plan_builder`, `sdd_tdd_workflow.md` v0.2→v0.3 (regla transversal §8), y retro-ajuste completo de `client_scaffold/tracer_bullet/` (HU-01…HU-05, CA-01…CA-11 + matriz, TSK-01…TSK-13). El bucle TDD no cambió de mecanismo (sigue por caso de `state.json`). T-013 queda en progreso, pendiente re-confirmar el GATE de plan retro-ajustado con el humano (A-009) antes del caso TDD #1. Avance global sube de 58% a 62%. Ver D-031, L-017, L-018, A-009. |
| 2026-07-02 | Quinto cierre (nueva sesión): el humano re-confirmó el GATE de plan retro-ajustado (A-009 validado). Se ejecutó el bucle TDD de `client_scaffold` de punta a punta sobre los casos 1-17 (tdd_tester→tdd_coder→tdd_refactor por caso, 5 casos "verde directo" documentados, L-020), dejando 26 tests en verde sin regresiones: bootstrap del paquete `foda` (`pyproject.toml`, src-layout), `src/foda/core/scaffold.py` con `create_client`+`_validate_name` (validación DS-1 completa vía whitelist consolidada, L-021), y `tests/core/test_scaffold.py`. GATE PA-3 resuelto por el humano con la opción (c): el caso 18 (rollback best-effort DS-2.2) queda DIFERIDO a una banda `stab_n` futura, refinando D-028 (D-032); tracer_bullet cierra con la estrategia validación-primero. Primera validación sustancial de la viabilidad de la cadena SDD/TDD de 8 agentes (A-005). Nuevas tareas: T-023 (cablear CLI, pendiente) y T-024 (banda stab_n del caso 18, backlog). Avance global sube de 62% a 68%. Ver D-032, L-019, L-020, L-021, A-005, A-009. |
| 2026-07-02 | Sexto cierre (nueva sesión): T-013 CERRADA. `integration_tester` añadió 6 tests de integración (árbol vs `system_design.md` §7, aislamiento multi-tenant §13, `client.yaml` como contrato consumible, fallo temprano sin cliente, creación secuencial sin estado compartido, colisión con archivo preexistente); suite 32 passed (26 unit + 6 integración). `spec_verifier` emitió veredicto **CONFORME** a los 11 CA con matriz de trazabilidad en `verification.md`, y registró 3 hallazgos no bloqueantes: F-1 (entorno local Python 3.12.10 vs R1/D-010 que exige 3.13+; `pyproject.toml` ya declara `>=3.13` correctamente), F-2 (capa CLI `foda client new` TSK-07 no construida, in-scope de la definición pero fuera de los CA verificados), F-3 (`created_at` usa `date.today()` local en vez de UTC, CA-06 solo valida el patrón). El humano validó la evidencia corriendo la suite por su cuenta (32 passed confirmado). F-2 dio origen a la nueva feature `client_new_cli` (T-023): se ejecutó su cadena SDD/TDD hasta `plan_builder` con 3 decisiones vinculantes (D-A cadena completa, D-B la CLI sí lleva tests pese al No-Objetivo previo de `client_scaffold` — NC-5 prevalece, D-C resolución de `clients_root` buscando `pyproject.toml` hacia arriba desde el cwd); ambos GATEs (spec: 10 CA; plan: 15 TSK y 12 casos TDD) fueron APROBADOS por el humano tal cual. La sesión se SUSPENDIÓ antes de arrancar el bucle TDD por decisión del usuario; `state.json` de `client_new_cli` queda `status:"suspended"`, `current_stage:"tdd"`, con bloque `suspension` (resume_at: invocar `tdd_tester` con el caso 1). Avance global sube de 68% a 72%. Ver D-033 a D-036, L-022, L-023, A-006 (nota). |
| 2026-07-02 | Séptimo cierre (nueva sesión): bucle TDD de `client_new_cli` COMPLETADO (12/12 casos). 5 casos con ciclo rojo→verde real (1 camino feliz/tracer bullet de `src/foda/cli.py`; 7 sin `pyproject.toml`; 8 NAME inválido con corrección de defecto mecánico de test, L-025; 9 duplicado; 12 entry point `[project.scripts]`); 7 casos verde-directo documentados como regresión (2,3,4,5,6,10,11), consolidando el patrón de L-020 como práctica estándar (D-037, L-024). Decisión de diseño D-038: ramas `except ValueError`/`except FileExistsError` separadas por exigencia de mensajes distintos (CA-07 vs CA-08). Suite: 46 passed. `state.json` con `tdd.status:"done"`, `current_stage:"integration_tester"`. Avance global sube de 72% a 78%. Ver D-037, D-038, L-024, L-025. |
| 2026-07-03 | Octavo cierre (nueva sesión): T-023 (`client_new_cli`) CERRADA y CONFORME. `integration_tester` añadió 8 tests de integración (`tests/integration/test_client_new_cli_integration.py`): consistencia del entry point declarado en `pyproject.toml`, invocación real del CLI vía subprocess (caminos éxito/error), integración de `main()` con el core real de `client_scaffold` sin espías, `client.yaml` como contrato consumible por flujo vecino, aislamiento multi-tenant vía CLI, resolución real de `clients_root`, duplicado end-to-end; sin defectos encontrados en `src/foda/cli.py`; suite de integración 14 passed (8 nuevos + 6 de `client_scaffold`), suite completa 54 passed sin regresiones. `spec_verifier` recorrió los 10 CA (CA-01…CA-10, D-034 a D-036) con evidencia completa, verificó restricciones (Python 3.13+, cero dependencias nuevas, resolución D-C sin asumir cwd, entry point declarado) y emitió veredicto **CONFORME**, generando `verification.md`. `state.json`: feature `status:"done"`, `current_stage:"completed"`. No surgieron decisiones ADR nuevas ni cambios de rumbo. Avance global sube de 78% a 82%. |
| 2026-07-03 | Novena sesión del día (sin código de feature nueva): verificación de uso real de la CLI fuera de tests. El `.venv` local estaba en Python 3.12.10, confirmando en la práctica el hallazgo F-1 (D-033): `pip install -e .` fallaba silenciosamente y `foda` no se instalaba. Se recreó el venv con Python 3.13.7 y se reinstaló con éxito; `foda client new DEMO_ABC` funcionó de punta a punta por primera vez en entorno real. Se creó `README.md` (requisitos, setup de entorno, uso de la CLI, tabla de códigos de salida, cuándo reinstalar, cómo testear). D-039: `clients/` y `.venv/` añadidos a `.gitignore` (datos de runtime multi-tenant, no se versionan); `clients/DEMO_ABC/` queda en disco sin versionar, a pedido del usuario. Avance global sin cambio (82%). Ver D-039, L-027, L-028. |
| 2026-07-03 | Décima sesión (nueva sesión): T-014 (`client_context`) CERRADA/CONFORME. Tercera feature en recorrer la cadena de 8 agentes de punta a punta. 3 decisiones vinculantes acordadas con el usuario (D-040 modo desde disco vía `models/latest`, D-041 `clients_root` como parámetro, D-042 introspección de artefactos diferida a `flow_base`). Cadena completa: `feature_definer` (4 HU) → `spec_writer` (12 CA, 3 decisiones técnicas D-043 a D-045, GATE APROBADO) → `plan_builder` (16 TSK, 12 casos TDD, GATE APROBADO) → bucle TDD (12/12 casos, 6 con ciclo real incl. refactor que extrajo `_data_dir`, 6 verde-directo por D-037) → `integration_tester` (5 tests de integración) → `spec_verifier` (veredicto **CONFORME** D-046, 2 hallazgos no bloqueantes F-1/F-2, registrados como A-010/A-011). Se construyó `src/foda/core/context.py` (clase `ClientContext`). Suite final: 71 passed sin regresiones. Avance global sube de 82% a 86%. Próxima tarea prioritaria: T-015 (`flow_base`). Ver D-040 a D-046, L-029, L-030, A-010, A-011. |
| 2026-07-03 | Undécima sesión (nueva sesión): T-015 (`flow_base`) CERRADA/CONFORME. Cuarta feature en recorrer la cadena de 8 agentes de punta a punta; materializa `system_design.md` §9 (contrato base + template method que heredarán todos los flujos concretos). Cadena completa: `feature_definer` (5 HU) → GATE de spec APROBADO (12→13 CA tras discutir el diseño, 4 decisiones técnicas D-047 a D-050: `FlowContractError` propia, `Artifact(name, base, relative)` declarativo, `FlowResult` mínimo, hooks base + `run` template method no sobreescribible; más 2 GATEs de diseño D-051 `execute()` construye el `FlowResult` y D-052 orden `load_inputs→validate`) → `plan_builder` (13 TSK, GATE de plan APROBADO con el humano pidiendo añadir el caso 14 —guarda `ValueError` de `Artifact.base` inválida— para respetar NC-5) → bucle TDD (14/14 casos: 3 con ciclo real —`Artifact`, `Flow`+`FlowResult`+`run`, `FlowContractError`+`validate`—, 11 verde-directo por D-037) → `integration_tester` (4 tests de integración) → `spec_verifier` (veredicto **CONFORME** D-053 a los 13 CA, sin hallazgos bloqueantes). Se construyó `src/foda/core/flow.py` (`Flow`, `Artifact`, `FlowResult`, `FlowContractError`), reutilizando `ClientContext` sin reimplementarlo. Suite final: 94 passed sin regresiones. Avance global sube de 86% a 90%. Próxima tarea: definir colaborativamente con el humano la siguiente feature a construir (primer flujo concreto vs. banda `stab_1` de `flow_base` vs. orquestador `foda run`); ninguna arrancada aún. Ver D-047 a D-053, L-031, L-032. |
| 2026-07-03 | Duodécima sesión (nueva sesión, deliberación estratégica sin código): T-026 RESUELTA. Se decidió (D-054) que la próxima feature a construir es `onboarding` (flujo 020), no Discovery, por ser este último de núcleo LLM no determinista con T-020 pendiente. Se acordó (D-055) simular la salida de Discovery con un `contract_data.json` fabricado para alimentar `onboarding` (contrato provisional, A-012), y se confirmaron (D-056/D-057) las estrategias `tracer_bullet` y de escalabilidad progresiva. Se registró una hoja de ruta de 14 features de producto como brújula. Nueva tarea T-027 (alta prioridad): iniciar `onboarding` con `feature_definer`. Avance global sin cambio (90%). Ver D-054 a D-057, L-033, A-012. |
| 2026-07-03 | Décimo tercer cierre (nueva sesión): T-027 (`onboarding`) ARRANCADA, etapas 1-3/8 completadas. Se consensuó con el humano, punto por punto (NC-6), la forma del `contract_data.json` fixture: jerarquías dinámicas (`levels` como fuente de verdad de la profundidad, no fija en 4), miembros planos con claves exactas de sus `levels`, `historical_data.datasets` con esquema por dataset (`fields`: `name`/`type`/`required`/`maps_to`) separando tipo de dataset/cobertura temporal/periodicidad, **Modelo B** de mapeo columna→nivel vía `maps_to` declarado en el contrato (D-058), vocabularios controlados cerrados que admiten datasets futuros (inventario, órdenes de compra, etc., más allá del fixture ventas+inventario) porque se predice demanda no ventas. Se confirmó la ubicación de artefactos (`contract_data.json` en `020_outputs/010_discovery/`, `map_client_data.json` en `020_outputs/020_onboarding/`, resoluble con `Flow`/`Artifact` sin ampliar `ClientContext`) y que Onboarding no toca bronze/datos reales (responsabilidad de Ingestion/030). Cadena ejecutada con 3 GATEs humanos APROBADOS: `feature_definer` (HU-01…HU-05, commit `33211d2`), `spec_writer` (22 CA tras añadir CA-05b a pedido del humano para profundidad >4 niveles, DS-ONB-1..5, commit), `plan_builder` (22 casos TDD, commit `4f4736e`). `state.json` avanzado a `current_stage:"tdd"`, `stages.tdd.status:"ready"`, `next_case:1` (cambio incluido en el commit de cierre de esta sesión). Avance global sube de 90% a 92%. Próxima tarea: arrancar el bucle TDD con `tdd_tester` caso 1 (CA-01). Ver D-058, D-059, L-034, A-013. |
| 2026-07-06 | Décimo cuarto cierre (nueva sesión): primera vuelta completa del bucle TDD de `onboarding` — caso 1/22 (CA-01, happy path end-to-end mínimo) cerrado con las 3 fases red→green→refactor. RED (`tdd_tester`, commit `9654f5f`): test `tests/flows/test_onboarding.py` + esqueleto importable `src/foda/flows/f020_onboarding/onboarding.py` (clase `Onboarding(Flow)` con `name`/`requires`/`produces`, sin lógica); falla por `NotImplementedError` de `Flow.execute()` (94 passed, 1 failed). GREEN (`tdd_coder`, commit `2b1a34e`): implementados los 4 hooks (`load_inputs`, `validate` delega en `super()`, `execute` deriva mapa mínimo y devuelve `FlowResult`, `write_outputs` escribe JSON), sin adelantar lógica de casos posteriores; 95 passed sin regresiones. REFACTOR (`tdd_refactor`, commit `612fbb9`): sin cambios en producción (ya mínima, NC-2/NC-3); único ajuste, mover `import json` al encabezado del test; 95 passed, caso 1 `done`. `state.json`: `stages.tdd.next_case:2`, `current_stage:"tdd_tester"` (bucle vuelve a `tdd_tester` para el caso 2 = CA-11). Avance global sube de 92% a 93%. Próxima tarea: invocar `tdd_tester` con el caso 2. Sin nuevas decisiones ADR ni supuestos; lección menor sobre la mecánica del bucle TDD también validada para `onboarding`. |
| 2026-07-06 | Décimo quinto cierre (nueva sesión): gran avance del bucle TDD de `onboarding`, casos 2 a 18 cerrados (17 casos en una sesión). 10 casos con ciclo real red→green→refactor: caso 3/4/5 (CA-02/03/04, jerarquías product/geography, refactor extrajo helper `_hierarchy`), caso 6/7/8 (CA-06/07/08, sección `datasets`, refactor extrajo helper `_dataset`), caso 10 (CA-10, `totals`), caso 16/17/18 (CA-14/15/16, primeras 3 reglas de validación de contenido en `validate()`: `levels` no vacío, claves de miembro == `levels` con helper `_validate_hierarchy`, `maps_to` a nivel existente con helper `_validate_maps_to`). 7 casos "verde directo" (D-037) con GATE humano: caso 2 (CA-11), 9 (CA-09), 11 (CA-05, depth=3), 12 (CA-05b, depth=5 incl. sku), 13 (CA-13, determinismo byte a byte), 14 (CA-12, aislamiento bronze/silver/gold). Nueva decisión D-060: el humano PRE-AUTORIZÓ, a partir del caso 14, el cierre automático de futuros casos "verde directo" sin pedir aprobación individual (solo reportarlos); primer caso cerrado bajo esta pre-autorización: caso 15 (CA-20, contract_data ausente → `FlowContractError`). Suite final: 112 tests en verde, sin regresiones. `state.json`: `stages.tdd.next_case:19`, `current_stage:"tdd_tester"`. Avance global sube de 93% a 96%. Próxima tarea: invocar `tdd_tester` con el caso 19 (CA-17, enums inválidos), luego casos 20-22, `integration_tester` y `spec_verifier`. Ver D-060, L-037. |
| 2026-07-06 | Décimo sexto cierre (nueva sesión): bucle TDD de `onboarding` COMPLETADO (22/22 casos), cerrando el bucle TDD completo de la feature. Casos 19-21 con ciclo real red→green→refactor: caso 19 (CA-17, enum inválido en `type`/`kind`/`source_medium`/`periodicity` → `FlowContractError`, helper `_validate_enums` refactorizado a `_check_enum(label, value, allowed)` para eliminar duplicación de los 4 bloques); caso 20 (CA-18, fecha no `YYYY-MM-DD` o `period_start > period_end` → `FlowContractError`, helpers `_parse_date`/`_validate_dates`, se implementaron ambas sub-reglas de CA-18 aunque el test rojo solo verificaba el rango invertido); caso 21 (CA-19, `field.name` duplicado dentro de un dataset → `FlowContractError`, helper `_validate_field_names`). Caso 22 (CA-21, el fallo ocurre en `validate` antes de `execute`/`write_outputs`, sin output parcial) cerrado como **verde directo** bajo pre-autorización D-060 (precedente casos 2, 9, 11-15): el comportamiento ya estaba garantizado estructuralmente por `Flow.run()` (template method) + la validación de contenido de los casos 16-21; test nuevo instrumenta los hooks y verifica que `execute`/`write_outputs` nunca se invocan. Suite final: 116 tests en verde, sin regresiones. `state.json`: `stages.tdd.status:"done"`, `next_case:null`, `current_stage:"integration_tester"`. Avance global sube de 96% a 98%. Se confirmó nuevamente que el patrón "verde directo" (D-037/D-060) escala bien (A-013), sin bloqueos activos. Próxima tarea: invocar `integration_tester` para `onboarding`, luego `spec_verifier`. |
| 2026-07-06 | Décimo séptimo cierre (nueva sesión): T-027 (`onboarding`) CERRADA/CONFORME, quinta feature en recorrer la cadena de 8 agentes de punta a punta. `integration_tester` añadió 5 tests de integración nuevos (end-to-end contra esperado fijo, invariante CA-12, fallo temprano `FlowContractError` sin `contract_data.json`, contrato de artefactos con flujo vecino de juguete, aislamiento multi-tenant; commit `9dcfdcc`); suite 121 passed (116+5), sin regresiones, sin tocar código de producción. `spec_verifier` produjo `verification.md` con matriz de trazabilidad completa y emitió veredicto **CONFORME** a los 22/22 CA (commit `a742a04`), registrando el hallazgo NO bloqueante OBS-1 (regla "`field.required` booleano" del Comportamiento Esperado 2b de `spec.md` sin CA/test/implementación explícita, candidato a `stab_1`; ver D-061, nueva tarea backlog T-029). Deliberación estratégica posterior con el humano (sin código): se aclaró que no hace falta esperar a los 14 flujos para que el humano inspeccione resultados (cada flujo deja un artefacto revisable, D-006/E6); se decidió (D-062) que la siguiente feature es **`flow_orchestrator`** (CLI `foda run <cliente> --flow <flujo>` + `foda status <cliente>`, banda `tracer_bullet`, con rangos/`--pipeline`/`export` diferidos). Avance global sube de 98% a 99%. Nueva tarea T-028 (alta prioridad): iniciar `flow_orchestrator` con `feature_definer`. Ver D-061, D-062, L-040. |
| 2026-07-06 | Decimoctavo cierre (nueva sesión): T-028 (`flow_orchestrator`) ARRANCADA, etapas 1-3/8 completadas. `feature_definer` creó `feature_contract.md` + `definition.md` (5 HU, commit `c132a9b`). `spec_writer` creó `spec.md` (14 CA, GATE humano **APROBADO SIN CAMBIOS**, commit `ed28ccd`), resolviendo DS-ORQ-1 (módulo `orchestrator.py` con registro literal `FLOWS`/`resolve_flow`, fuera de `core/`), DS-ORQ-2 (`ValueError` para flujo desconocido), DS-ORQ-3 (`foda status` en texto plano determinista), DS-ORQ-4 (orden de validación flujo→cliente→ejecución, códigos 0/1/2, sin crear `clients/`). `plan_builder` creó `plan.md` (20 TSK, 15 casos TDD, commit `774ba4c`), dejando 3 puntos abiertos para el GATE (A-014, A-015, A-016). **GATE humano del plan queda PENDIENTE**: la sesión se cerró antes de arrancar el bucle TDD. Avance global sin cambio (99%): pasa de "siguiente feature decidida" a "en progreso, etapas 1-3/8". Próxima tarea: obtener el GATE humano del plan e invocar `tdd_tester` con el caso 1. Ver D-062 a D-066, A-014 a A-016. |
| 2026-07-07 | Decimonoveno cierre (nueva sesión): T-028 (`flow_orchestrator`) CERRADA/CONFORME, completando las etapas 4-8/8 tras el GATE humano del plan **APROBADO SIN CAMBIOS** (valida A-014, A-015, A-016). Bucle TDD completo (15/15 casos): solo el caso 4 (camino feliz de `run`) y el caso 10 (camino feliz de `status`) requirieron ciclo rojo→verde real, construyendo `src/foda/orchestrator.py` (`FLOWS`/`resolve_flow`) y extendiendo `src/foda/cli.py` con `run`/`status`; los otros 13 casos nacieron "verde directo" (D-037, ver L-041). Refactors reales: helper `_build_client_context` (caso 10) y refactor final de banda TSK-20 (fixtures de test + docstring de `cli.py`). `integration_tester` añadió 9 tests de integración con piezas reales (incluido el entry point `foda` vía subprocess), sin defectos. `spec_verifier` emitió veredicto **CONFORME** (D-067) a los 14/14 CA, `verification.md` producido, sin hallazgos. Suite final: 145 passed (121+24), sin regresiones. `README.md` actualizado con los comandos `run`/`status`. `flow_orchestrator` es la SEXTA feature completa CONFORME de punta a punta del proyecto. Avance global sube de 99% a 100% del alcance de infraestructura+orquestador construido hasta ahora; queda pendiente decidir con el humano la siguiente feature de negocio del backlog de flujos deterministas. Ver D-062 a D-067, L-041. |
| 2026-07-07 | Vigésimo cierre (nueva sesión, sin código nuevo): validación manual end-to-end de `flow_orchestrator` ejecutada por el humano en una terminal real (Python 3.13.7), con cliente propio `COMPANY_001`, cubriendo los 8 escenarios de `spec.md` (creación de cliente, `status` con/sin artefactos, `run` con fallo limpio por artefacto ausente y éxito tras contrato inyectado manualmente, cliente inexistente, `--flow` ausente, flujo desconocido); `map_client_data.json` verificado como correcto y determinista. Confirma en producción el veredicto CONFORME de `spec_verifier` (D-067); sin defectos. Se aclaró que `contract_data.json` es la salida futura de `010_discovery` (aún no construido); hoy se inyecta a mano como stand-in tracer-bullet. Working tree limpio, sin cambios de código/tests. Avance global sin cambio (100% de la infraestructura construida hasta ahora). Ver L-042. |
| 2026-07-07 | Vigésimo primer cierre (nueva sesión): T-031 (`ingestion`, flujo 030) ARRANCADA, séptima feature de la cadena, elegida sobre `discovery` para consolidar terreno determinista (D-068), con alcance CSV/TXT (`,`/`;`/`\|`) + XLSX en tracer_bullet (D-069). Etapas 1-3/8 completadas: `feature_definer` (HU-01…HU-06), `spec_writer` (CA-01…CA-21, GATE APROBADO) y `plan_builder` (22 casos TDD). En el GATE humano del plan, el humano pidió un reparto de responsabilidad entre `contract_data.json` (archivos esperados) y `map_client_data.json` (columnas esperadas), sin chequeo de coherencia entre ambos (DS-ING-8/D-077), lo que obligó a reabrir `spec_writer` (spec enmendada) y re-ejecutar `plan_builder` (re-plan), preservando el orden de los 22 casos (L-044). Se cerró el caso 1/22 del bucle TDD (CA-14, ciclo rojo→verde→refactor completo: esqueleto `Ingestion(Flow)`, luego `load_inputs`/`execute`/`write_outputs` mínimos, `ingestion_report.json` determinista). A pedido explícito del humano, la sesión se detiene tras el caso 1; quedan 21/22 casos pendientes. Suite final: 146 passed sin regresiones. Ver D-068 a D-077, L-043 a L-045, A-017. |
| 2026-07-07 | Vigésimo segundo cierre (nueva sesión): a pedido explícito del humano ("trabaja solo en el caso 2 completo"), se cerró el caso 2/22 del bucle TDD de `ingestion` (CA-20: `Ingestion` hereda `Flow`, declara `requires`/`produces` y completa las 4 fases sin sobreescribir `run`). `tdd_tester` detectó un gap real (rojo legítimo): `Ingestion` no declaraba explícitamente el hook `validate`, dependía del heredado implícitamente, contradiciendo "Interfaces/Firmas Públicas" de `spec.md`/TSK-03 (commit `34593c8`). `tdd_coder` añadió `validate(self, ctx)` delegando en `super().validate(ctx)`, verde al primer intento, sin alterar `run` (commit `ea3e9c0`). `tdd_refactor` no tocó código (ya mínimo, NC-2/NC-3) y cerró el caso en `state.json` (commit `903fcae`). Suite final: 147 passed (146+1), sin regresiones. Quedan 20/22 casos pendientes; retoma en `tdd_tester` con el caso 3 (CA-01). Ver L-046. |
| 2026-07-08 | Vigésimo tercer cierre (nueva sesión): bucle TDD de `ingestion` COMPLETADO (22/22 casos), cerrando las etapas 4-6/8 de la cadena. Casos 12-15 completan el vocabulario de inconsistencias (`unexpected_file`, `missing_column`, `unexpected_column`, y confirmación de que columna no-required ausente no es inconsistencia). Caso 16 (CA-16) hizo aflorar una contradicción real de spec entre el vocabulario cerrado y el esquema DS-ING-2; `tdd_tester` se detuvo (NC-1/NC-6) y el humano decidió la enmienda **D-078/DS-ING-9**: se añade al reporte una lista top-level `inconsistencies[]` que agrega todas las inconsistencias del run con `{type, detail}`, saldando la deuda de los casos 11/12. Casos 17-18 (summary/success) verde directo. Caso 19 (CA-18, inconsistencia parcial) fue ROJO GENUINO: se detectó que `bronze_path` estaba hardcodeado a `None` para todos los archivos, corregido con el helper `_bronze_relative_path`. Casos 20-22 (determinismo, `FlowContractError` por artefactos ausentes) verde directo. Suite final: 167 passed (156 previos + 11 nuevos), sin regresiones. `state.json`: `stages.tdd.status:"done"`. Avance global: bucle TDD de la séptima feature completo; falta `integration_tester` y `spec_verifier`. Ver D-078, L-048, L-049. |
| 2026-07-08 | Vigésimo cuarto cierre (nueva sesión): T-031 (`ingestion`) CERRADA/CONFORME, etapas finales 7-8/8. `integration_tester` añadió 7 tests de integración contra el sistema real; `spec_verifier` emitió veredicto **CONFORME** a los 21/21 CA (`verification.md`, commit `d8dc9bf`). Suite final: 174 passed sin regresiones. `ingestion` es la SÉPTIMA feature completa CONFORME de punta a punta. Sin decisiones ADR nuevas ni bloqueos. Ver D-068 a D-078, L-050. |
| 2026-07-08 | Vigésimo quinto cierre (nueva sesión, sin código, sesión de análisis/definición): se detectó que `ingestion` (CONFORME) NO está cableada al orquestador (`FLOWS` de `src/foda/orchestrator.py` solo registra `onboarding`), por lo que no es ejecutable vía `foda run --flow ingestion` pese al veredicto CONFORME (L-051). Se definió (sin ejecutar) el plan de prueba humana de `ingestion` vía CLI. Se acordó con el humano la nueva política de gobernanza D-079 (rama por feature `feature/<nombre>` + merge por PR vía `gh` tras prueba humana, stages `human_test`/`merge_to_main` en `state.json`), estado Aceptada con IMPLEMENTACIÓN PENDIENTE (T-034, 7 ajustes futuros, punto de alcance abierto sin resolver). Se registraron 3 tareas pendientes en orden: T-032 (cablear CLI), T-033 (prueba humana), T-034 (implementar flujo de ramas). Se añade A-018 (pendiente). Ver D-079, L-051, A-018. |
| 2026-07-08 | Vigésimo cuarto cierre (nueva sesión): T-031 (`ingestion`) CERRADA/CONFORME, completando las etapas finales 7-8/8. `integration_tester` añadió 7 tests de integración contra el sistema real (`tests/integration/test_ingestion_integration.py`): end-to-end con `Onboarding` real + `contract_data.json` fabricado (D-014), invariante de no tocar silver/gold, fallo temprano `FlowContractError` por artefactos ausentes, consumo por flujo vecino de juguete, inconsistencia parcial real (CA-18), aislamiento multi-tenant; 7 passed sin defectos, suite 174 passed (167+7) sin regresiones (commit `1945729`). `spec_verifier` recorrió los 21 CA-01…CA-21 con matriz de trazabilidad completa (21/21 cubiertos), emitió veredicto **CONFORME**, produjo `verification.md` (commit `d8dc9bf`). `ingestion` es la SÉPTIMA feature completa CONFORME de punta a punta del proyecto. T-031 queda COMPLETADA. Sin bloqueos activos. Próxima tarea: decidir con el humano la siguiente feature de negocio del backlog determinista. Ver D-068 a D-078, L-050. |
| 2026-07-09 | Vigésimo sexto cierre (nueva sesión): T-032 COMPLETADA (`ingestion` cableada a `FLOWS` del orquestador + `README.md`, cierra L-051); se corrigió un test flaky preexistente de `ingestion` (CA-13, timestamps de `openpyxl` en fixtures xlsx, ver L-052); suite estable 174 passed. T-033 VERIFICADA CONFORME (prueba humana de `ingestion` vía CLI de punta a punta, camino feliz + 6 caminos no-felices con el resultado esperado). Se acordó con el humano el ADR **D-080** (gate de progresión entre flujos por `success` del predecesor + `--force`, y corrección del exit code de `foda run`); implementación pendiente salvo el punto del exit code, que queda como tarea aislada de alta prioridad. Se registraron 3 hallazgos de gobernanza (exit code no refleja `success=false`, `.venv` desactualizado tras nueva dependencia, `ingestion` no limpia bronze entre corridas) y 3 tareas nuevas. Ver D-080, L-052 a L-055. |
| 2026-07-09 | Vigésimo séptimo cierre (nueva sesión): T-035 COMPLETADA (código+test; prueba humana pendiente). Cambio quirúrgico sobre `flow_orchestrator` (ya CONFORME): TDD acotado rojo→verde directo sobre la feature existente, sin reabrir la cadena de 8 agentes (D-080 punto 4, L-056). Nuevo test en `tests/cli/test_flow_orchestrator_cli.py` que espía `Onboarding.run` devolviendo `FlowResult(success=False, outputs=[])` y afirma exit 1 sin "completado" en stdout; cambio mínimo en `_dispatch_run` de `src/foda/cli.py` (si `success` es `False` tras `flow.run(ctx)` sin excepción, imprime mensaje sin "completado" y retorna 1; si `True`, comportamiento previo intacto). Docstrings de `cli.py` actualizados. Suite: 175 passed (174+1), sin regresiones. Se resolvió con el humano el punto de alcance ABIERTO de T-034/D-079 mediante la enmienda **D-081**: mientras T-034 no se implemente, todo el trabajo (incluida la propia T-034) va directo a `main`; se aclaró el contrato de la futura política de ramas (automatización hasta el PR, merge a `main` como gate humano explícito). T-034 pasa a ser la próxima tarea prioritaria (prioridad Alta). Ver D-080, D-081, L-053, L-056, L-057. |
| 2026-07-09 | Vigésimo octavo cierre (nueva sesión): T-034 COMPLETADA — implementado el flujo de ramas por feature + merge por PR (D-079, alcance resuelto por D-081). Sesión de gobernanza/documentación, sin código de producción (suite sin cambios, 175 passed). Se resolvieron con el humano (AskUserQuestion) 2 ambigüedades de alcance: ampliar de 7 a 9 archivos (incluyendo `sdd_tdd_workflow.md` y `spec_verifier.md` por coherencia con los stages terminales) y que la sesión principal —no un agente— abre el PR tras `spec_verifier` CONFORME. Modelo: `feature_definer` crea `feature/<nombre>` → cadena SDD/TDD corre sobre la rama → `session_closer` empuja a `HEAD` → `spec_verifier` CONFORME deja `current_stage=human_test` (feature `status=in_progress`) → sesión principal abre PR → gates humanos `human_test`/`merge_to_main` → feature cerrada en `main`. 9 archivos editados: `CLAUDE.md` §3, `feature_definer.md`, `session_closer.md`, `session_starter.md`, `methodology.md`, `600_features/_template/state.json`, `spec_verifier.md`, `sdd_tdd_workflow.md` (v0.3→v0.4), `decisions.md` (actualización de D-079/D-081). T-034 se aplicó directo sobre `main` (D-081), sin rama propia. Avance: 36 tareas completadas, 2 pendientes (T-036, T-037), 0 en progreso. Ver D-079, D-081, L-058. |
