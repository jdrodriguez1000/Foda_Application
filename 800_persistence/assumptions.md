# Assumptions — Supuestos del Proyecto

> Este archivo registra los **supuestos** del proyecto e indica si están **validados o no**.

---

## Índice
1. [Cómo Registrar un Supuesto](#1-cómo-registrar-un-supuesto)
2. [Supuestos Validados](#2-supuestos-validados)
3. [Supuestos Pendientes de Validar](#3-supuestos-pendientes-de-validar)
4. [Supuestos Invalidados](#4-supuestos-invalidados)

---

## 1. Cómo Registrar un Supuesto
Cada supuesto incluye: **ID**, **descripción**, **estado** (✅ Validado · ⏳ Pendiente · ❌ Invalidado), **impacto si es falso** y **evidencia/validación**.

## 2. Supuestos Validados
| ID | Supuesto | Impacto si es falso | Evidencia |
|---|---|---|---|
| A-002 | Hay acceso/credenciales para hacer push al remoto en GitHub | Falla el cierre de sesión (push) | Push exitoso en commit 2dd82ac y en sesiones posteriores. |
| A-003 | La rama principal del proyecto es `main` | Push a rama incorrecta | Confirmado: push exitoso a `origin/main` en múltiples sesiones. |
| A-001 | El alcance del proyecto será definido por el usuario a continuación | Retraso en el arranque | El usuario definió el alcance en la sesión 2026-07-01 a partir de `990_documents/expected_workflow.md` y `current_state.md`, y de las restricciones de diseño que estableció. |
| A-004 | El documento `700_architecture/system_design.md` refleja correctamente la intención del usuario | Rediseño o retrabajo en la fase de construcción | Confirmado: el usuario revisó y validó las 16 secciones sección por sección (5 bloques temáticos) en la sesión 2026-07-01, con 5 ajustes puntuales (ver D-010 a D-014). Documento actualizado a v0.2. |
| A-009 | El retro-ajuste de trazabilidad (D-031) sobre `plan.md` de `client_scaffold` no alteró el contenido sustantivo de los 18 casos TDD ya aprobados | Si el retro-ajuste hubiera introducido un cambio de fondo no detectado, se habría arrancado el bucle TDD sobre un plan distinto al aprobado originalmente | El humano re-confirmó explícitamente el GATE del `plan.md` retro-ajustado al inicio de esta sesión (registrado en `state.json`, `plan_builder: approved:true`). El bucle TDD se ejecutó sobre ese plan sin incidentes. |
| A-005 | El diseño de la cadena SDD/TDD de 8 agentes de desarrollo (D-008, D-015) es viable con las capacidades de subagentes de Claude Code | Habría que rediseñar la orquestación, el checkpointing o la cadena de agentes | `client_scaffold` recorrió la cadena completa (feature_definer → spec_writer → plan_builder → tdd_tester/tdd_coder/tdd_refactor → integration_tester → spec_verifier) con veredicto final **CONFORME** (D-033), 32 tests en verde, sin bloqueos de orquestación ni pérdida de checkpoint entre etapas. |
| A-013 | El esquema completo de `contract_data.json` acordado con el humano (D-058: jerarquías dinámicas, datasets con esquema propio, Modelo B de `maps_to`, vocabularios controlados) es suficiente para soportar el fixture ventas+inventario del tracer_bullet de `onboarding` sin necesitar ajustes durante el bucle TDD | Si el bucle TDD revelara un caso no cubierto por el esquema acordado, habría que reabrir el GATE de spec y volver a consultar al humano antes de continuar | El bucle TDD de los 22 casos (T-027) se cerró completo (116 tests en verde) sin reabrir el GATE de spec ni ajustar el esquema del contrato. |
| A-014 | La granularidad de los casos TDD 1-3 de `flow_orchestrator` (unit del registro `resolve_flow`, separados de los casos de la CLI `foda run`/`foda status`) propuesta por `plan_builder` es la partición correcta y no requiere ajuste en el GATE humano del plan | Si el GATE detectara que la separación registro-vs-CLI es artificial o insuficiente, habría que reordenar/fusionar casos antes de arrancar el bucle TDD | El humano aprobó el plan SIN CAMBIOS; los casos 1-3 se cerraron tal como estaban particionados (casos 2 y 3 verde-directo), sin necesitar reordenar ni fusionar. |
| A-015 | Usar un `FakeFlow`/flujo falso de test para cubrir HU-05 de `flow_orchestrator` (dado que `onboarding` es el único flujo real registrado hoy) es una estrategia de test válida para el caso 14 del plan, sin necesitar un segundo flujo real | Si el GATE humano rechazara el uso de un flujo falso (p. ej. exigiendo un segundo flujo real para probar el registro con más de una entrada), habría que reescribir el caso 14 o esperar a tener un segundo flujo de negocio construido | El humano aprobó el plan SIN CAMBIOS; el caso 14 se cerró usando `FakeFlow` inyectado vía `monkeypatch.setitem(FLOWS, ...)`, confirmando extensibilidad del registro sin fricción. |
| A-016 | Sembrar un `contract_data.json` mínimo válido que pase `Onboarding.validate` (reutilizando el fixture ya validado en `onboarding`, A-013) es suficiente para ejercitar el camino feliz de `foda run`/`foda status` en los casos 4/5/6/11 del plan de `flow_orchestrator`, sin necesitar variantes adicionales del fixture | Si el camino feliz de `foda run` requiriera un fixture distinto o más elaborado que el ya usado en `onboarding`, habría que ajustar el plan antes o durante el bucle TDD | Los casos 4, 5, 6 y 11 se cerraron reutilizando el fixture validado de `onboarding` sin ajustes ni variantes adicionales. |

## 3. Supuestos Pendientes de Validar
| ID | Supuesto | Impacto si es falso | Cómo validar |
|---|---|---|---|
| A-017 | El caso "verde directo" de `ingestion` que compara datos reales contra el registro de Discovery (`client_register`) puede diferirse por completo a una banda futura sin invalidar el diseño de `contract_data.json`/`map_client_data.json` (D-077) acordado para esta banda | Si al construir `discovery` (010) resultara que `ingestion` necesita ya en `tracer_bullet` esa comparación, habría que reabrir el GATE de spec/plan de `ingestion` para añadir el caso | Se validará al construir la feature `discovery`: verificar que el reparto de responsabilidad contrato/mapa (D-077) sigue siendo suficiente sin el caso `client_register`. |
| A-012 | El `contract_data.json` simulado que se defina para alimentar el tracer_bullet de `onboarding` (D-055) tiene la forma que Discovery deberá producir en el futuro (contrato provisional entre features) | Si Discovery, al construirse, produjera una forma distinta de `contract_data.json`, habría que retrabajar `onboarding` para consumir la forma real | Se validará cuando se construya la feature `discovery`: verificar que su salida coincide con la forma acordada y documentada al definir `onboarding` (primera sub-tarea de `feature_definer` en T-027). |
| A-006 | El import `@980_guideline/principles.md` añadido en `CLAUDE.md` §0 efectivamente carga el archivo en el contexto de la sesión principal al reiniciar Claude Code | Los P1-P8/E1-E12/NC-1...NC-6 no llegarían a la sesión principal de forma automática; habría que depender solo de que el usuario o el agente lean el archivo manualmente | Validar empíricamente en una sesión nueva (tras reiniciar) comprobando si el contenido de `principles.md` aparece disponible sin pedirlo explícitamente. |
| A-007 | En la banda `tracer_bullet` de `client_scaffold` no se filtran diferencias de nombre de cliente en filesystems case-insensitive (p. ej. en Windows, `ABC` y `abc` se tratan como el mismo directorio aunque el patrón DS-1/D-023 sea case-sensitive) | Un usuario podría intentar crear `abc` tras ya existir `ABC` y obtener un comportamiento de filesystem no controlado por la validación de duplicado, en vez de un error claro de `client_scaffold` | Limitación conocida aceptada explícitamente por el humano en el GATE de `spec.md`; se endurecerá en una banda posterior si se decide soportarlo. |
| A-008 | En la banda `tracer_bullet` de `client_scaffold` no se filtran nombres reservados de Windows (CON, NUL, PRN, AUX, COM1...9, LPT1...9, etc.) como nombre de cliente inválido | Un usuario en Windows podría pasar un nombre reservado del SO y obtener un error de filesystem críptico en vez de un `ValueError` claro de validación de nombre (DS-1/D-023) | Limitación conocida aceptada explícitamente por el humano en el GATE de `spec.md`; se endurecerá en una banda posterior si se decide soportarlo. |
| A-010 | En la banda `tracer_bullet` de `client_context`, un symlink roto en `models/latest` se evalúa como inexistente (`is_recurring == False`, ⇒ NUEVO) en vez de reportar un estado de error distinto | Un cliente cuyo puntero `models/latest` esté roto (p. ej. tras borrar manualmente una versión de modelo) sería tratado silenciosamente como NUEVO, pudiendo re-entrenar en vez de alertar sobre el estado inconsistente | Limitación conocida documentada como hallazgo F-2 de `spec_verifier` (D-046); se endurecerá en una banda posterior si algún flujo consumidor (`flow_base`) lo requiere. |
| A-011 | En la banda `tracer_bullet` de `client_context`, el comportamiento en filesystems case-insensitive no se normaliza explícitamente: `ClientContext` hereda la semántica del filesystem subyacente sin lógica propia adicional | En un filesystem case-insensitive, dos nombres de cliente que difieran solo en mayúsculas/minúsculas podrían resolver al mismo `ClientContext` de forma no controlada por la clase | Limitación conocida documentada como hallazgo F-2 de `spec_verifier` (D-046), análoga a A-007 de `client_scaffold`; se endurecerá en una banda posterior si se decide soportarlo. |
> **Nota (2026-07-01):** sesión de validación de `system_design.md` (T-008): A-004 pasa a Validado. A-005 sigue pendiente, se validará al construir T-009/T-010.
> **Nota (2026-07-02):** T-009/T-010/T-011 completadas (andamiaje: 8 agentes + `sdd_tdd_workflow.md` + plantilla `600_features/`). A-005 sigue Pendiente; ahora está más cerca de validarse porque solo falta ejecutar la cadena sobre la primera feature real (T-013).
> **Nota (2026-07-02):** se acordó el alcance de la primera feature real, `client_scaffold` (ver D-016), pero aún no se invocó la cadena de agentes. A-005 sigue Pendiente hasta ejecutarla de punta a punta.
> **Nota (2026-07-02):** sesión de gobernanza/reconciliación de `980_guideline/` (T-016): se añadió A-006 (pendiente) sobre la carga efectiva del import de `principles.md` en `CLAUDE.md`. A-005 sigue Pendiente sin cambios; no se trabajó en T-013 esta sesión.
> **Nota (2026-07-02):** sesión de reversión del runtime agéntico (T-019, D-020/021/022): sin cambios en A-005 ni A-006 (siguen Pendientes); no se trabajó en T-013. La sesión fue de gobernanza/documentación (recorte de `methodology.md`), sin código de aplicación.
> **Nota (2026-07-02):** sesión de construcción de `client_scaffold` (T-013, etapas 1-3 de la cadena SDD/TDD): feature_definer, spec_writer y plan_builder completados; GATE humano aprobado tras spec y tras plan. Se añaden A-007 y A-008 (limitaciones conocidas de la banda `tracer_bullet`, aceptadas explícitamente por el humano). A-005 sigue Pendiente: aún falta ejecutar el bucle TDD (tdd_tester/tdd_coder/tdd_refactor), integration_tester y spec_verifier para validarla de punta a punta. A-006 sigue Pendiente sin cambios.
> **Nota (2026-07-02):** T-021 completada (D-029/D-030 aplicadas a la documentación y a `client_scaffold`) y T-022 completada (trazabilidad codificada HU→CA→TSK, D-031), incluyendo el retro-ajuste de `plan.md` de `client_scaffold`. Se añade A-009 (pendiente): el GATE de plan debe re-confirmarse formalmente con el humano antes de reanudar el bucle TDD, aunque el contenido de los 18 casos no cambió. A-005 y A-006 siguen Pendientes sin cambios.
> **Nota (2026-07-02):** el humano re-confirmó el GATE de plan retro-ajustado (A-009 pasa a Validado, movido a §2). Se ejecutó el bucle TDD de `client_scaffold` de punta a punta sobre los casos 1-17 (26 tests en verde, sin regresiones) y se resolvió el GATE PA-3 con el caso 18 diferido (D-032). **A-005 queda validado en gran medida**: la cadena SDD/TDD demostró ser viable para producir una feature con suite verde a través de las etapas feature_definer→spec_writer→plan_builder→tdd_tester/tdd_coder/tdd_refactor; se considerará plenamente validada al completar también `integration_tester` y `spec_verifier` sobre esta misma feature. A-006 sigue Pendiente sin cambios.
> **Nota (2026-07-02):** `integration_tester` y `spec_verifier` completados sobre `client_scaffold`, con veredicto CONFORME (D-033). **A-005 pasa a Validado** (movido a §2): la cadena de 8 agentes recorrió una feature real de punta a punta con éxito. Se acordó con el humano el alcance de la nueva feature `client_new_cli` (D-034 a D-036) y se ejecutó su cadena hasta `plan_builder` (ambos GATEs APROBADOS), quedando SUSPENDIDA antes del bucle TDD por decisión del humano. A-006 sigue Pendiente sin cambios; A-007/A-008 (limitaciones conocidas de `client_scaffold`) permanecen Pendientes, ahora heredadas por la feature ya cerrada, sin nueva evidencia de validación.
> **Nota (2026-07-02):** se reanudó `client_new_cli` y se ejecutó su bucle TDD completo (12/12 casos, 46 tests passed), reforzando adicionalmente A-005 (segunda feature que recorre con éxito el bucle TDD de la cadena de 8 agentes, aunque A-005 ya estaba Validado desde el cierre de `client_scaffold`). Se formalizó D-037, que consolida el patrón "verde directo" (usado ya en dos features distintas, ver L-020/L-024) como práctica estándar del harness, no como caso aislado. A-006, A-007, A-008 siguen Pendientes sin cambios. Pendiente `integration_tester` y `spec_verifier` para cerrar `client_new_cli`.
> **Nota (2026-07-03):** `integration_tester` y `spec_verifier` completados sobre `client_new_cli`, con veredicto CONFORME. A-005 queda reforzado una tercera vez (segunda feature cerrada de punta a punta por la cadena de 8 agentes, sin retrabajo de la implementación al pasar por integración/verificación, ver L-026). No surgieron supuestos nuevos en esta sesión. A-006, A-007, A-008 siguen Pendientes sin cambios; A-007/A-008 son limitaciones conocidas específicas de `client_scaffold` que no aplican a `client_new_cli` (la CLI no introduce nueva lógica de nombre de cliente, delega en el core ya validado).
> **Nota (2026-07-03, sesión de verificación de entorno):** se confirmó en la práctica el hallazgo F-1 de `spec_verifier` sobre `client_scaffold` (D-033): el `.venv` local estaba en Python 3.12.10 y `pip install -e .` fallaba silenciosamente sin instalar el comando `foda`. Se recreó el `.venv` con Python 3.13.7 y `foda client new DEMO_ABC` funcionó de punta a punta en un entorno real por primera vez, fuera de la suite de tests — refuerzo adicional de A-005 (ya Validado) por evidencia de uso real, no solo de tests automatizados. A-006, A-007, A-008 siguen Pendientes sin cambios. No se registran supuestos nuevos.
> **Nota (2026-07-03, sesión de construcción de `client_context`):** T-014 cerrada CONFORME, tercera feature en recorrer la cadena de 8 agentes de punta a punta (refuerza adicionalmente A-005, ya Validado). Se añaden A-010 y A-011 (limitaciones conocidas de la banda `tracer_bullet` de `client_context`, aceptadas vía hallazgo F-2 de `spec_verifier`, D-046), análogas a A-007/A-008 de `client_scaffold`. A-006, A-007, A-008 siguen Pendientes sin cambios.
> **Nota (2026-07-03, sesión de construcción de `flow_base`):** T-015 cerrada CONFORME, cuarta feature en recorrer la cadena de 8 agentes de punta a punta (refuerza adicionalmente A-005, ya Validado, ahora sobre una abstracción base en vez de una feature de negocio concreta). No se identificaron supuestos nuevos ni limitaciones conocidas adicionales en esta sesión (`spec_verifier` no registró hallazgos, D-053). A-006, A-007, A-008, A-010, A-011 siguen Pendientes sin cambios.
> **Nota (2026-07-03, sesión de deliberación estratégica T-026):** sin código ni tests nuevos. Se decidió la próxima feature (`onboarding`, D-054) y su estrategia de entrada simulada (D-055). Se añade A-012 (pendiente): el `contract_data.json` simulado fija un contrato provisional que Discovery deberá cumplir a futuro; se valida solo cuando se construya `discovery`. A-006, A-007, A-008, A-010, A-011 siguen Pendientes sin cambios.
> **Nota (2026-07-03, sesión de arranque de `onboarding`, T-027 etapas 1-3/8):** se consensuó con el humano el contrato `contract_data.json` (D-058, D-059) y se completaron `feature_definer`→`spec_writer`→`plan_builder` con los 3 GATEs humanos APROBADOS. Se añade A-013 (pendiente): el esquema de `contract_data.json` acordado es suficiente para el fixture del tracer_bullet, a validar durante el bucle TDD de 22 casos (aún no arrancado). A-012 sigue Pendiente sin cambios (Discovery no se ha construido). A-006, A-007, A-008, A-010, A-011 siguen Pendientes sin cambios.
> **Nota (2026-07-06, sesión de arranque del bucle TDD de `onboarding`, caso 1/22):** se cerró el caso 1 (CA-01) con ciclo completo red→green→refactor, sin incidentes ni ambigüedades (ver L-036). No hay evidencia aún que valide o invalide A-013 (esquema suficiente para el fixture completo): el caso 1 solo ejerció el camino feliz mínimo, no las jerarquías dinámicas ni los datasets; A-013 sigue Pendiente, a validar conforme avancen los 21 casos restantes. Sin cambios en A-006, A-007, A-008, A-010, A-011, A-012.
> **Nota (2026-07-06, sesión de avance masivo del bucle TDD de `onboarding`, casos 2-18/22):** se cerraron 17 casos adicionales (jerarquías dinámicas de profundidad 3/4/5, datasets con fields/maps_to, totals, determinismo byte a byte, aislamiento bronze/silver/gold, y las primeras 3 reglas de validación de contenido: levels no vacío, claves de miembro, maps_to a nivel existente), 112 tests en verde sin regresiones. **A-013 avanza sustancialmente hacia su validación**: el esquema de `contract_data.json` (D-058) soportó sin ajustes todos estos casos, incluyendo profundidad variable (3, 4 y 5 niveles) y el Modelo B de `maps_to`; sigue Pendiente formalmente hasta cerrar los 4 casos restantes (19-22, todos de validación de contenido: enums, fechas/rango, name duplicado, fallo temprano en validate) sin necesitar reabrir el GATE de spec. Nueva decisión D-060 (pre-autorización de cierre automático de verdes directos) no crea supuesto nuevo. Sin cambios en A-006, A-007, A-008, A-010, A-011, A-012.
> **Nota (2026-07-06, sesión de cierre del bucle TDD de `onboarding`, casos 19-22/22):** se cerraron los 4 casos restantes (enums inválidos, fechas/rango, name duplicado, fallo temprano en validate sin output parcial), 116 tests en verde sin regresiones, sin reabrir el GATE de spec. **A-013 queda VALIDADO** (movido a §2): el esquema completo de `contract_data.json` (D-058) soportó los 22 casos del tracer_bullet sin necesitar ajustes de diseño. El patrón "verde directo" (D-037/D-060) se confirmó una vez más al cerrar el caso 22 sin fricción (ver L-039). Sin cambios en A-006, A-007, A-008, A-010, A-011, A-012. Pendiente `integration_tester` y `spec_verifier` para cerrar `onboarding` de punta a punta.
> **Nota (2026-07-06, sesión de cierre de `onboarding` con `integration_tester`/`spec_verifier`, y decisión de la siguiente feature):** `onboarding` cerrada CONFORME (D-061), quinta feature en recorrer la cadena de 8 agentes de punta a punta (refuerza adicionalmente A-005, ya Validado, sobre una feature de negocio nueva construida encima de `flow_base`/`client_context`). No se añaden supuestos nuevos sobre el esquema del contrato (A-013 ya Validado). Se decidió (D-062) construir a continuación `flow_orchestrator`; no se registran supuestos nuevos sobre esa decisión en esta sesión (quedará a cargo de `feature_definer` en la próxima). A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios.
> **Nota (2026-07-06, sesión de arranque de `flow_orchestrator`, etapas 1-3/8):** se ejecutaron `feature_definer` (5 HU), `spec_writer` (14 CA, GATE humano APROBADO sin cambios, resolviendo DS-ORQ-1 a DS-ORQ-4) y `plan_builder` (20 TSK, 15 casos TDD). Se añaden A-014, A-015 y A-016 (pendientes): puntos abiertos que `plan_builder` deja explícitos para la revisión del GATE humano del plan antes de arrancar el bucle TDD. El GATE humano del plan queda PENDIENTE al cierre de esta sesión (no se aprobó todavía); la sesión se cerró antes de invocar `tdd_tester`. A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios.
> **Nota (2026-07-07, sesión de cierre de `flow_orchestrator`, etapas 4-8/8):** el humano aprobó el GATE del plan sin cambios, validando explícitamente A-014, A-015 y A-016 (los 3 pasan a §2 Validados). Se ejecutó el bucle TDD completo (15/15 casos, 13 verde-directo por D-037, ver L-041), `integration_tester` (9 tests de integración con piezas reales) y `spec_verifier` (veredicto CONFORME, D-067, 14/14 CA). `flow_orchestrator` es la sexta feature cerrada de punta a punta (refuerza adicionalmente A-005, ya Validado). No se registran supuestos nuevos. A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios.

> **Nota (2026-07-07, sesión de validación manual de `flow_orchestrator`):** el humano ejecutó la CLI `foda` de punta a punta en una terminal real con un cliente propio (`COMPANY_001`), cubriendo los 8 escenarios de `spec.md` sin encontrar defectos. **Refuerza adicionalmente A-005** (ya Validado) con evidencia de uso real en producción, más allá de la suite automatizada y de `client_new_cli`/`DEMO_ABC` (L-027). Se confirmó que `contract_data.json` es la salida futura de `010_discovery` (aún no construido), sin crear un supuesto nuevo (ya cubierto por A-012, pendiente). A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios. Ver L-042.

> **Nota (2026-07-07, sesión de arranque de `ingestion`, etapas 1-3/8 + caso 1/22 del bucle TDD):** se ejecutaron `feature_definer`, `spec_writer` (enmendado por DS-ING-8, D-077) y `plan_builder` (re-plan), y se cerró el caso 1/22 del bucle TDD (red→green→refactor, 146 tests en verde). Se añade A-017 (pendiente): el caso `client_register` (comparación contra el registro de Discovery) queda diferido sin invalidar D-077, a validar cuando se construya `discovery`. A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios. Quedan 21/22 casos del bucle TDD de `ingestion` pendientes. Ver L-043, L-044, L-045, D-068 a D-077.
> **Nota (2026-07-07, sesión de cierre del caso 2/22 del bucle TDD de `ingestion`):** a pedido explícito del humano se cerró el caso 2 (CA-20, red→green→refactor: gap real detectado —hook `validate` no declarado explícitamente—, 147 tests en verde). No se registran supuestos nuevos ni cambios de estado en los existentes; A-017 sigue Pendiente sin nueva evidencia (el caso 2 no tocó la relación contrato/mapa de D-077). A-006, A-007, A-008, A-010, A-011, A-012 siguen Pendientes sin cambios. Quedan 20/22 casos del bucle TDD de `ingestion` pendientes. Ver L-046.

## 4. Supuestos Invalidados
| ID | Supuesto | Motivo | Fecha |
|---|---|---|---|
| — | _Sin registros aún._ | — | — |
