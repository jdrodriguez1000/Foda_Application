# Lessons — Lecciones Aprendidas

> Este archivo registra las **lecciones aprendidas** durante el proyecto para no repetir errores y reforzar lo que funciona.

---

## Índice
1. [Cómo Registrar una Lección](#1-cómo-registrar-una-lección)
2. [Lecciones Técnicas](#2-lecciones-técnicas)
3. [Lecciones de Proceso](#3-lecciones-de-proceso)
4. [Lecciones de Producto/Negocio](#4-lecciones-de-productonegocio)

---

## 1. Cómo Registrar una Lección
Cada lección incluye: **ID**, **contexto** (qué pasó), **lección** (qué aprendimos) y **acción** (qué haremos distinto).

## 2. Lecciones Técnicas
| ID | Contexto | Lección | Acción |
|---|---|---|---|
| L-002 | Se invocó `foda-status` con frontmatter `model: sonnet` esperando que corriera en Sonnet, pero la sesión estaba en Haiku 4.5. | El frontmatter `model:` solo aplica a agentes/subagentes; las skills invocadas inline con slash command corren siempre en el modelo de la sesión activa. | Para fijar el modelo por protocolo, usar subagentes (herramienta Agent) en vez de skills inline. |
| L-003 | Al migrar los protocolos a subagentes, el cierre de sesión no tenía acceso al historial de la conversación. | Los subagentes arrancan en frío (sin historial); el protocolo de cierre depende de que la sesión principal le entregue un resumen completo de lo trabajado. | El agente `session_closer` exige recibir el resumen de sesión en el prompt antes de actualizar los 5 archivos. |

## 3. Lecciones de Proceso
| ID | Contexto | Lección | Acción |
|---|---|---|---|
| L-001 | Arranque del proyecto | Definir persistencia y protocolos desde el inicio da trazabilidad y continuidad entre sesiones. | Mantener disciplina: ejecutar `foda-next` al iniciar y `foda-status` al cerrar. |
| L-004 | Se diseñó `700_architecture/system_design.md` definiendo primero principios (determinista por defecto, LLM aislado, artefactos como contrato entre flujos) antes de codificar. | Definir principios de diseño antes de codificar produce una arquitectura testeable y reproducible. | Mantener el documento de diseño como referencia obligatoria antes de iniciar cualquier construcción de código. |
| L-005 | Se diseñó una cadena de 8 agentes de desarrollo SDD/TDD; los subagentes de Claude Code son efímeros (arranque en frío) y no se pueden pausar/reanudar como procesos vivos. | La resumibilidad de un flujo multi-etapa entre sesiones se logra con checkpointing a disco (artefactos + `state.json`), no con estado en memoria del agente; cada etapa debe ser atómica para poder reanudar limpiamente. | Diseñar todo flujo largo orquestado por subagentes con persistencia en disco por etapa y re-invocación desde el último checkpoint. |
| L-006 | Al migrar de skills a subagentes (T-006/D-005) quedó una referencia colgante a las skills `foda-next`/`foda-status` ya eliminadas, mencionada dentro del `description` e "Instrucción principal" de `session_starter.md`/`session_closer.md`. Además se detectó que los cambios hechos en `CLAUDE.md` durante una sesión no se recargan en la sesión en curso (se lee solo al arrancar). | Mantener una única fuente de verdad por artefacto evita desincronización entre documentos que describen el mismo protocolo; los cambios a `CLAUDE.md` solo surten efecto pleno a partir de la siguiente sesión. | El detalle paso a paso del protocolo de sesión vive únicamente en `.claude/agents/session_starter.md` y `session_closer.md`; `CLAUDE.md` solo delega e invoca por frase-gatillo. Ver D-009. |
| L-007 | Se validó `700_architecture/system_design.md` (16 secciones) con el usuario en una sola sesión. | Agrupar las secciones en bloques temáticos afines y hacer preguntas concretas al final de cada bloque (en vez de sección por sección o el documento completo de una vez) permitió validar un documento largo de forma eficiente, con feedback puntual y accionable. | Para futuras revisiones de documentos extensos con el usuario, organizar la revisión en bloques temáticos con checkpoints de confirmación. |
| L-008 | Al construir los 8 agentes de desarrollo (T-009) se definió `state.json` inline y de forma repetida en cada uno de los 8 archivos de agente, duplicando la convención 8 veces. | Duplicar una convención compartida en múltiples archivos autosuficientes crea riesgo de desincronización (p. ej. si cambia el esquema de `state.json`, hay que tocar 8 archivos). Consolidarla en un único documento de referencia (`700_architecture/sdd_tdd_workflow.md`, T-010) reduce ese riesgo, aunque los agentes siguen sin referenciarlo explícitamente. | Punto abierto: evaluar que los agentes de desarrollo referencien `sdd_tdd_workflow.md` en vez de repetir la convención de `state.json` inline. |
| L-009 | El usuario entregó `980_guideline/methodology.md` y `principles.md`, que duplicaban parcialmente contenido ya presente en `system_design.md` y `sdd_tdd_workflow.md` (los 14 flujos/medallion, los P/E), y contenían citas a decisiones inexistentes (D-021, D-029). | Cuando llega documentación de guía nueva que se solapa con documentos ya existentes, conviene primero construir un mapa de "fuente única de verdad" por tema (qué documento es dueño de qué contenido) antes de editar ninguno, para no propagar la duplicación ni dejar citas fantasma sin resolver. | Se añadió a `methodology.md` una sección "Mapa de fuentes" (§0) que asigna cada tema a su dueño canónico; las citas fantasma se formalizaron como ADR reales (D-017, D-018) en vez de dejarlas colgando. |
| L-010 | Se necesitaba que los 10 agentes de `.claude/agents/` (que arrancan en frío) se comportaran según `980_guideline/principles.md`, pero un import `@` en `CLAUDE.md` solo se resuelve de forma garantizada en la sesión principal, no en subagentes invocados vía Task/Agent. | La vía robusta para vincular un subagente efímero a una guía de comportamiento es una cláusula explícita "lee X primero" dentro de la definición del propio agente (mecanismo independiente de versión de Claude Code), no confiar en que la herencia de `CLAUDE.md`/imports `@` llegue a los subagentes. | Se insertó la misma cláusula vinculante bajo el H1 de los 10 agentes, referenciando `980_guideline/principles.md` como fuente única (sin copiar su contenido). |
| L-011 | Al reconciliar la carpetería step-céntrica de D-017 (`703/705/710/720/<banda>/<flujo>/`) contra la feature-céntrica `600_features/<feature>/` ya en uso, se optó por insertar `<banda>` como subcarpeta y aplicar `replace_all` de la subcadena de ruta única `600_features/<feature>/` en los 8 agentes. | Un `replace_all` sobre una subcadena de ruta suficientemente única (que no aparece en ningún otro contexto del archivo) es una forma segura y limpia de propagar un cambio estructural repetido en varios archivos; conviene verificar después por grep que no queden rutas sin banda ni duplicaciones `<banda>/<banda>/`. | Verificado por grep tras el cambio: sin dobles `<banda>/<banda>/` ni rutas `600_features/<feature>/` residuales sin banda en los 8 agentes. |
| L-012 | La reconciliación de T-016 adoptó "hacia arriba" el modelo runtime agéntico (patrón A/B/C, MOTOR/INSTANCIA) descrito en `980_guideline/methodology.md`, un documento importado de otro harness, sin contrastarlo primero contra `system_design.md` (D-006), que ya había resuelto el runtime como código determinista. El usuario reportó sentirse perdido y percibió la metodología como complejidad agregada. | Documentación de guía importada de otro proyecto/harness puede traer un modelo de solución (aquí: runtime agéntico) que contradice el diseño propio ya acordado. Al reconciliar una guía nueva contra lo existente, hay que contrastarla SIEMPRE contra la fuente de verdad del proyecto (`system_design.md`) antes de adoptar sus conceptos, en vez de reconciliar "hacia arriba" hacia la guía importada. | Se revirtió con D-020: el runtime lo define exclusivamente `system_design.md` (no agéntico); `methodology.md` se recortó a metodología de desarrollo. Ante una guía nueva, verificar primero si contradice al diseño de producto ya acordado. |
| L-013 | Las citas a "D-005/D-009/D-010" dentro de `methodology.md` (resueltas en T-016 como D-017/D-018) tenían significados que no coincidían con las decisiones reales de `decisions.md` (esos IDs correspondían a otras decisiones ya tomadas: skills→subagentes, versión Python, etc.). | Cuando un documento importado cita IDs de decisión (D-xxx) cuyo significado no coincide con el `decisions.md` real del proyecto, es una señal de alerta fuerte de que el documento fue importado de otro proyecto/harness sin reconciliar, y merece revisión crítica completa antes de adoptar su contenido, no solo renumerar las citas. | Al recortar `methodology.md` (D-020) se verificó que ninguna cita remanente apunte a un ID con significado distinto al de `decisions.md`. |
| L-014 | Al decidir qué rescatar de la metodología descartada (persistencia y evaluación), se evaluó adoptar sus 3 archivos de estado de runtime y una rúbrica de evaluación general, pero se concluyó que el estado de runtime ya vive en los artefactos bajo `clients/<NAME>/` (redundante) y que una rúbrica aplicada al código determinista sería overkill (los tests ya son objetivos). | Al rescatar valor de una guía que se descarta en general, conviene dimensionar cada pieza rescatada a la necesidad real: aplicar una rúbrica de evaluación calibrada solo donde la salida es no determinista (LLM/ML), no al código determinista. Es el espejo del principio "LLM solo donde aporta". | D-022 limita la rúbrica calibrada a las salidas de Discovery/Exploration y a la evaluación temprana de ML; el código sigue con tests + veredicto binario. D-021 rescata solo la Single Writer Rule de la persistencia de la metodología. |
| L-015 | El `feature_definer` de `client_scaffold` concretó `data/{bronze,silver,gold}/` + `models/` en la estructura de carpetas, mientras que D-016 (alcance acordado con el usuario) solo mencionaba `data/` de forma plana. Antes de asumir que era una desviación silenciosa del alcance, se contrastó contra `700_architecture/system_design.md` §7 y §10, que sí definen las capas medallion bronze/silver/gold y `models/` como parte canónica del diseño. | Cuando un artefacto de un agente parece añadir algo no explícito en la decisión de alcance más reciente (D-016), hay que verificar contra el documento de diseño canónico (`system_design.md`) antes de tratarlo como decisión silenciosa (NC-6): puede ser una elección ya validada por una fuente de verdad anterior, no una invención del agente. | La elección del `feature_definer` quedó validada sin necesidad de nueva decisión ni de detener la cadena; se documenta la verificación en vez de una ADR nueva, porque no hubo elección nueva, solo confirmación de una ya existente. |
| L-016 | El usuario aportó su vocabulario habitual de bandas (tracer_bullet / estabilización / MVP / evolución / final) para nombrar el crecimiento del trabajo, pero al analizarlo se vio que mezclaba dos ejes ortogonales: profundizar UNA feature (vertical: tracer_bullet→stab_n) vs. hacer crecer el PRODUCTO agregando features (horizontal: MVP/evolución/final). | El vocabulario de un colaborador puede mezclar ejes ortogonales (madurez de feature vs. crecimiento de producto); conviene separarlos explícitamente antes de adoptarlo, en vez de forzar todos los términos a una sola taxonomía de carpetas. | D-029 separa los dos ejes: solo el eje vertical (`tracer_bullet`, `stab_n`) se materializa como subcarpeta bajo la feature; MVP/Final/evolución quedan como conceptos de roadmap de producto, documentados pero sin maquinaria de carpetas todavía (E4/NC-2). |
| L-017 | Al aplicar T-021 (ítem #2), se encontró que `980_guideline/methodology.md` no mencionaba ya ningún modelo de contratos: el recorte de D-020 (T-019, "Metodología de Desarrollo del Motor") había eliminado también la mención a `slice_contract` junto con el resto del contenido runtime descartado. Además, la renumeración de secciones del ítem #1 (T-021) dejó una referencia colgante `sdd_tdd_workflow.md §6` que debía ser `§7`. | Un recorte agresivo de documentación (aunque justificado, como D-020) puede arrastrar consigo conceptos que sí seguían vigentes (el `slice_contract` diferido, no descartado); y renumerar secciones en un documento sin revisar las referencias cruzadas que otros documentos le hacen deja citas rotas. "Reconciliar" a veces significa introducir el modelo por primera vez, no solo alinear dos versiones existentes. | Al recortar documentación, distinguir explícitamente qué conceptos se descartan de cuáles solo se difieren; al renumerar secciones, hacer un grep de las referencias cruzadas (`§N`) en otros documentos antes de dar el cambio por cerrado. |
| L-018 | El usuario pidió trazabilidad total `definition.md`→`spec.md`→`plan.md`, lo que tocaba las 3 plantillas, los 3 agentes de la primera mitad de la cadena y el documento de orquestación a la vez; existía el riesgo de que la nueva capa de tareas (`TSK-xx`) chocara con el mecanismo ya construido del bucle TDD por "caso" en `state.json`. | Antes de tocar múltiples artefactos por una exigencia transversal nueva (trazabilidad), conviene resolver primero por decisión explícita CON el usuario cómo la nueva capa convive con el mecanismo existente (aquí: tareas = capa de trazabilidad, el bucle sigue por caso, sin tocar el schema de `state.json`), en vez de rediseñar el mecanismo para acomodar la nueva exigencia. | D-031 mantiene el bucle TDD intacto (mismo `state.json`, mismos `tdd.cases`) y añade `TSK-xx` como capa de trazabilidad pura, con reglas de partición (un responsable/un entregable) que evitan pisar la Single Writer Rule (D-021). |
| L-019 | Al arrancar el bucle TDD de `client_scaffold` en este entorno Windows, el launcher `py -V:3.13` no resolvió el intérprete ("No suitable Python runtime found"), aunque Python 3.13 estaba instalado. | En este entorno el launcher `py` no es fiable para fijar versión; hay que usar la ruta absoluta del intérprete. `pyproject.toml` con `pythonpath=["src"]` (pytest) hace reproducibles los imports del paquete `foda` sin necesidad de instalación editable (`pip install -e .`). | Usar `C:\Users\USUARIO\AppData\Local\Programs\Python\Python313\python.exe` de forma explícita en vez de `py -V:3.13` en este entorno; mantener `pythonpath=["src"]` en la config de pytest del proyecto. |
| L-020 | Durante el bucle TDD de `client_scaffold`, varios casos (3, 5, 8, 15, 17) llegaron "verde directo": el comportamiento exigido por el caso ya estaba cubierto por una implementación mínima de un caso anterior, sin necesitar código de producción nuevo. | Forzar un rojo artificial cuando el comportamiento ya está cubierto viola NC-2 (simplicidad) y no aporta valor; lo correcto es documentar explícitamente por qué el caso nace verde (NC-6, sin decisión silenciosa) y conservar igualmente el test como cobertura de regresión. | En el bucle TDD, cuando un caso resulta verde directo, registrar en `state.json`/commit la razón concreta (qué caso anterior ya lo cubre) y mantener el test igual, en vez de omitirlo o de inventar un fallo. |
| L-021 | La validación de nombre de cliente (DS-1, D-023) fue creciendo caso a caso (vacío, espacios, guiones iniciales, separadores de ruta, "."/"..", caracteres no permitidos, longitud) con una guarda por regla; consolidar la whitelist demasiado pronto habría obligado a repetir el refactor varias veces. | Conviene posponer la consolidación de una validación compuesta hasta tener TODAS sus reglas representadas por al menos un caso rojo→verde, en vez de generalizar apenas aparecen 2-3 reglas parecidas (evita abstracciones prematuras, NC-2) y luego refactorizar de una sola vez verificando cada regla existente sigue cubierta (NC-3). | En `_validate_name` de `client_scaffold`, la consolidación se hizo en dos pasos: tras el caso 14 (whitelist de caracteres `[A-Za-z0-9_-]+`, eliminando 3 guardas redundantes) y tras el caso 16 (fusión final con el límite de longitud en un solo `re.compile(r"[A-Za-z0-9_-]{1,64}")`), quedando 3 guardas totales (vacío, guion/underscore inicial, fullmatch) para las 7 reglas de DS-1. |
| L-022 | Para invocar el paquete `foda` fuera de la suite de `pytest` (verificación manual del humano, y ahora la futura CLI), el import falló porque el proyecto usa src-layout sin instalación editable; se requiere `PYTHONPATH=src` (o `pip install -e .`) para que `import foda` resuelva. | `pythonpath=["src"]` en la config de pytest (ver L-019) solo cubre las corridas de test; cualquier invocación fuera de pytest (script suelto, REPL, futura CLI) necesita su propio mecanismo de resolución de imports. | Al construir la capa CLI de `client_new_cli`, declarar `[project.scripts]` en `pyproject.toml` (entry point) en vez de depender de `PYTHONPATH` manual, para que `foda` sea invocable de forma reproducible una vez instalado el paquete. |
| L-023 | Al decidir el alcance de la nueva feature `client_new_cli`, existía tensión entre el No-Objetivo de `client_scaffold` (que dejaba la capa CLI explícitamente sin tests en su banda, D-025/D-028) y NC-5 ("toda tarea tiene un test que la respalda... sin excepción"). Se consultó al humano en vez de asumir. | Cuando una decisión de alcance de una feature anterior (aquí, "la CLI queda fuera de los tests de esta banda") entra en tensión con una Norma de Comportamiento vinculante de `principles.md` al construir la feature que sí cubre ese alcance, la norma prevalece salvo decisión explícita en contrario del humano; no se puede resolver la tensión por inercia documental. | D-035: `client_new_cli` SÍ lleva tests propios de la CLI (12 casos TDD), aceptado explícitamente por el humano en el GATE de plan; NC-5 prevalece sobre el No-Objetivo previo de `client_scaffold`. |

| L-024 | En el bucle TDD de `client_new_cli`, 7 de sus 12 casos (2, 3, 4, 5, 6, 10, 11) llegaron "verde directo" porque el tracer bullet del caso 1 (el flujo completo cableado de una vez) o el comportamiento por defecto de `argparse` ya cubrían el comportamiento exigido — el mismo patrón observado en `client_scaffold` (L-020), ahora repetido en una segunda feature. | Cuando el mismo patrón de "verde directo, sin forzar rojo artificial" se repite en una segunda feature independiente, deja de ser una excepción puntual y pasa a ser una práctica estándar del harness SDD/TDD: el criterio no es "cuántos casos nacen verdes" sino "documentar explícitamente la razón (NC-6) y conservar el test como regresión", cualquiera sea la feature. | Se formaliza D-037: en cualquier feature del harness, un caso que pasa en verde de inmediato se cierra como `done` con `tdd_note` explicando qué caso/comportamiento previo ya lo cubre, sin invocar `tdd_coder`/`tdd_refactor` para ese caso. |
| L-025 | El caso 8 de `client_new_cli` (NAME inválido `".."`) tenía una aserción de test que comprobaba que `clients/..` NO existiera, pero `pathlib` resuelve esa ruta al propio `tmp_path` (que siempre existe), haciendo la aserción imposible de pasar sin importar la implementación. | Un test recién escrito por `tdd_tester` puede tener un defecto mecánico objetivo (no una ambigüedad de diseño) que lo hace incorrecto independientemente del código de producción. El `tdd_coder` está autorizado a corregir ese tipo de defecto objetivo del test, siempre que lo documente, sin necesitar re-arrancar el ciclo desde `tdd_tester`. | En el refactor del caso 8, se reescribió la aserción para verificar que el nombre inválido no aparece entre las entradas de `clients_root`, documentando el motivo del cambio en el commit. Distinguir siempre "defecto objetivo del test" (corregible por `tdd_coder`) de "ambigüedad de diseño" (requiere detenerse y consultar, NC-1/NC-6). |
| L-026 | Al cerrar `client_new_cli` con `integration_tester` y `spec_verifier`, la implementación de `src/foda/cli.py` construida durante el bucle TDD unitario (con monkeypatch/espías sobre el core) pasó sin cambios los 8 tests de integración (subprocess real, core real, entry point real) y los 10 CA de la spec. | La suite unitaria (con dobles de prueba) y la suite de integración (proceso real, dependencias reales) son complementarias, no redundantes: la de integración valida específicamente lo que la unitaria no puede cubrir por diseño (el entry point declarado en `pyproject.toml`, el árbol de archivos real end-to-end). Cuando ambas pasan sin retrabajo, es una señal fuerte de que el diseño interno (core aislado + CLI fina, D-025) fue el correcto. | Mantener siempre una capa de integración con procesos/dependencias reales además de la suite unitaria con dobles, incluso cuando la suite unitaria ya es exhaustiva; no tratarla como paso burocrático. |
| L-027 | El usuario intentó usar la CLI en su máquina (`pip install -e .` seguido de `foda client new DEMO_ABC`) y obtuvo `foda: The term 'foda' is not recognized`, sin ningún mensaje de error visible de `pip`. El `.venv` existente había sido creado con Python 3.12.10, pero `pyproject.toml` exige `>=3.13` (R1/D-010); `pip install -e .` fallaba con `Package 'foda' requires a different Python` y el entry point nunca se instalaba. | Un hallazgo no bloqueante documentado en `verification.md` (F-1: entorno local en versión de Python incorrecta) puede materializarse como un fallo de uso real, silencioso desde la perspectiva del usuario final (el error de `pip` no aparece si no se mira la salida completa). Verificar la versión del intérprete usada para crear el `.venv` es un paso de diagnóstico de primera línea ante "comando no reconocido" tras una instalación editable. | Se recreó el `.venv` con `py -3.13 -m venv .venv` y se documentó el requisito + advertencia explícita en `README.md` §Requisitos, para que futuros colaboradores no repitan el fallo. |
| L-028 | El proyecto no tenía ningún `README.md` en la raíz pese a tener ya dos features cerradas y una CLI instalable; el usuario tuvo que preguntar cómo ejecutar `foda client new`. | La documentación de arranque (`README.md`) es un artefacto de primera necesidad tan pronto como existe un comando instalable, no un lujo a posponer indefinidamente: sin ella, cada nuevo colaborador (o el propio usuario en una máquina nueva) repite por prueba y error la configuración de entorno ya resuelta antes. | Se creó `README.md` con requisitos, setup de entorno paso a paso, uso de la CLI, tabla de códigos de salida, uso diario del venv, cuándo reinstalar y cómo correr los tests; se mantendrá actualizado a medida que se agreguen comandos/flujos nuevos. |
| L-029 | En el bucle TDD de `client_context`, 6 de sus 12 casos llegaron "verde directo" (D-037) porque `create_client` ya crea las 6 carpetas del árbol y porque `is_recurring` quedó bien definido desde su primer caso rojo→verde. | El patrón "verde directo" (D-037) escala especialmente bien cuando una feature se construye incrementalmente ENCIMA de otra ya existente (aquí, `client_context` sobre `create_client`): varios criterios de aceptación quedan cubiertos como efecto colateral del trabajo previo, y forzar rojos artificiales para "simular" TDD puro violaría NC-2 sin aportar valor. | Al planificar el bucle TDD de una feature que depende de otra ya cerrada, anticipar que una fracción significativa de sus casos puede nacer verde; documentar la razón concreta (qué caso/feature previa ya lo cubre) en vez de omitir el test o inventar un fallo. |
| L-030 | En el caso 10 de `client_context` (verde-directo), el `state.json` quedó registrado con estado `"red"` en vez de `"done"` tras cerrarlo; el descuadre se detectó y corrigió recién en la invocación del caso 11 siguiente. | Los casos "verde directo" (D-037) se saltan la invocación normal de `tdd_coder`/`tdd_refactor`, que es donde normalmente se actualiza el estado del caso en `state.json`; eso los deja más expuestos a que el estado quede desincronizado si no se actualiza explícitamente al cerrarlos. | Al cerrar un caso "verde directo", verificar explícitamente que su entrada en `state.json` quede en `"done"` (no solo documentar la razón en `tdd_note`), en la misma operación que lo cierra, sin esperar a la siguiente etapa para detectarlo. |
| L-031 | En el bucle TDD de `flow_base`, 11 de sus 14 casos (casi el 80%) llegaron "verde directo" (D-037): los 3 casos con ciclo rojo→verde real (`Artifact`, `Flow`/`FlowResult`/`run`, `FlowContractError`/`validate`) cubrieron de una vez la mayoría de las aserciones de comportamiento del resto del plan. | El patrón "verde directo" escala aún más en abstracciones BASE (un contrato + template method) que en features de negocio concretas: muchos criterios de aceptación de una abstracción son en realidad aserciones sobre un comportamiento ya fijado por su primer caso rojo→verde (la forma del contrato), no comportamiento nuevo por construir. | Al planificar el bucle TDD de una feature que es una abstracción/contrato base (no un caso de uso concreto), anticipar una fracción aún mayor de casos "verde directo" que en features de negocio; seguir documentando la razón concreta caso por caso (D-037/NC-6) sin tratarlo como anomalía. |
| L-032 | En el GATE humano del `plan.md` de `flow_base`, el usuario pidió AÑADIR un caso 14 (guarda `ValueError` de `Artifact.base` inválida, DS-FLOW-2) que el `plan_builder` no había incluido, pese a que la decisión DS-FLOW-2 (`Artifact` declarativo con `ValueError` en base desconocida) ya estaba aprobada en el GATE de spec. | Un GATE humano no es solo aprobar/rechazar el plan tal cual se presenta: el humano puede detectar que una decisión ya tomada en la spec (aquí, el `ValueError` de DS-FLOW-2) carece de caso de test en el plan, y exigir su cobertura antes de aprobar, en aplicación directa de NC-5 ("toda tarea tiene un test... sin excepción"). | El `plan_builder` añadió el caso 14 al final de `tdd.cases` junto con su TSK-20 correspondiente, sin renumerar los 13 casos ya aprobados; el bucle TDD lo cerró como parte del ciclo normal (verde-directo, cubierto por el `Artifact` ya construido en el caso 1). |

| L-033 | Al resolver T-026 (qué feature construir a continuación), la opción obvia por orden de pipeline era Discovery (flujo 010, el primero), pero Discovery es uno de los dos únicos flujos con núcleo LLM (D-006/D-013) y depende de una rúbrica de evaluación aún no diseñada (T-020, D-022). El usuario propuso arrancar `onboarding` simulando la salida de Discovery con un `contract_data.json` fabricado. | El orden "natural" de un pipeline de negocio no siempre coincide con el orden de construcción más seguro para el harness: conviene primero validar la abstracción base (`flow_base`) con un flujo determinista real, aunque no sea el primero del pipeline, y usar un fixture/maniquí para simular la salida de un flujo aún no construido en vez de bloquearse esperando a construirlo. | Se registró D-054 (elegir `onboarding` en vez de `discovery`) y D-055 (fixture de `contract_data.json`), dejando explícito que ese fixture fija un contrato provisional con Discovery (A-012) a validar cuando se construya ese flujo. |
| L-034 | Al diseñar el contrato `contract_data.json` de `onboarding`, la primera duda del humano fue si las columnas de un archivo de datos siempre se llaman igual que los niveles de la jerarquía (p. ej. columna `sede` == nivel `sede`); en la práctica los nombres de columna de un cliente real pueden diferir de los nombres de nivel. | Un contrato de datos entre flujos deterministas no debe asumir que el mapeo estructura↔archivo se puede inferir por coincidencia de nombres: cuando existe ambigüedad real de nombres, el mapeo debe declararse explícitamente en el propio contrato (aquí, `maps_to` por columna) en vez de que el flujo consumidor lo adivine con heurísticas frágiles. | Se adoptó el Modelo B (D-058): cada columna de `fields` declara su `maps_to` (`"product.<level>"`/`"geography.<level>"`/`"time"`/`"measure"`/`null`); `onboarding` consume el mapeo declarado, nunca lo infiere. |
| L-035 | El caso límite de "profundidad dinámica de jerarquía" (¿qué pasa si la jerarquía tiene más o menos niveles que el fixture de referencia?) se había cubierto inicialmente en el plan solo con un caso de REDUCCIÓN (3 niveles en vez de 4); el humano detectó en el GATE que faltaba probar el sentido contrario. | Un caso límite de "cardinalidad variable" (aquí, profundidad de jerarquía) debe probarse en ambas direcciones —menos y más elementos que el caso de referencia— no solo en la dirección más obvia (reducción); probar solo un sentido deja una asimetría de cobertura que puede ocultar un hardcode no detectado (p. ej. un límite superior fijo en 4). | Se añadió CA-05b (profundidad >4, 5 niveles, sin hardcode) junto al CA-05 ya existente (profundidad <4) en `spec.md`/`plan.md` de `onboarding`, a pedido explícito del humano antes de aprobar el GATE de spec. |
| L-036 | El primer caso TDD (CA-01) de `onboarding` recorrió red→green→refactor sin fricción: el esqueleto mínimo importable creado en la fase red (clase `Onboarding(Flow)` con `name`/`requires`/`produces` declarados, sin lógica) permitió que `tdd_coder` implementara los 4 hooks del template method directamente en verde, y que `tdd_refactor` no tuviera que tocar producción (ya mínima). | Cuando la fase red deja un esqueleto de producción importable y bien tipado (aunque sin lógica), el ciclo green→refactor tiende a salir limpio sin iteraciones extra; la mecánica del bucle TDD de 3 fases queda validada también para una quinta feature (`onboarding`), reforzando A-005 sobre una feature de negocio nueva construida encima de `flow_base`. | Mantener la práctica de que `tdd_tester` deje siempre un esqueleto mínimo importable (clase/función con firma correcta, sin `NotImplementedError` salvo el heredado del contrato base) al escribir el test rojo, en vez de solo el test. |
| L-037 | En la sesión de avance masivo del bucle TDD de `onboarding` (casos 2-18), 7 de 17 casos llegaron "verde directo" (2, 9, 11, 12, 13, 14, 15), una proporción alta similar a la observada en `flow_base` (L-031). El diseño mínimo de casos tempranos —`depth = len(levels)` genérico (caso 3-4), serialización determinista desde el caso 1 (`sort_keys=True`), delegación de `validate()` en `Flow.validate` base— ya satisfacía criterios de aceptación de casos muy posteriores del plan (profundidad variable, determinismo, existencia de contrato). | Cuando el diseño de los primeros casos de una feature es deliberadamente genérico-pero-mínimo (sin hardcodes, reutilizando abstracciones base ya CONFORMES como `Flow.validate`), la proporción de casos "verde directo" en el resto del plan tiende a ser alta; esto valida la estrategia de diseño en vez de ser una anomalía a corregir. Los refactors de esta tanda solo extrajeron helpers (`_hierarchy`, `_dataset`, `_validate_hierarchy`, `_validate_maps_to`) cuando hubo duplicación real de 2+ sitios de uso, evitando construir un "motor de reglas de validación" genérico de forma prematura (NC-2/NC-3). | Al planificar el bucle TDD de una feature nueva, diseñar los primeros casos con generalidad mínima necesaria (parámetros derivados, no constantes) para maximizar la cobertura por "verde directo" en casos posteriores, documentando siempre la razón (D-037/NC-6) sin forzar rojos artificiales. |

| L-038 | Al cerrar los últimos 4 casos del bucle TDD de `onboarding` (19-22), el caso 20 (CA-18, fechas) exponía dos sub-reglas relacionadas bajo un mismo criterio de aceptación (formato de fecha inválido y `period_start > period_end`), pero el test rojo del `tdd_tester` solo ejercitaba la segunda. | Cuando un único CA agrupa varias sub-reglas relacionadas y el test rojo solo cubre una, el `tdd_coder` puede implementar TODAS las sub-reglas del criterio en la misma pasada (no solo la que el test exige), siempre que quede documentado, en vez de dejar la sub-regla no testeada para un caso futuro que quizá nunca llegue. | Se implementaron ambas sub-reglas de CA-18 (`_parse_date` valida formato y calendario real vía `date.fromisoformat`, `_validate_dates` valida el rango) en el caso 20, aunque el test rojo solo cubría el rango invertido. |
| L-039 | El caso 22 (CA-21: el fallo ocurre en `validate` antes de `execute`/`write_outputs`, sin output parcial) se cerró como "verde directo" (D-060) sin escribir ningún código de producción nuevo: el comportamiento ya estaba garantizado por el template method `Flow.run()` (D-050, `load_inputs→validate→execute→write_outputs` en orden fijo, corta en la primera excepción) combinado con las 6 reglas de validación de contenido ya construidas en los casos 16-21. | Un criterio de aceptación sobre el ORDEN/CONTROL DE FLUJO de una abstracción base ya cerrada (`flow_base`, D-050) puede quedar automáticamente satisfecho por cualquier flujo concreto que la herede correctamente, sin necesitar lógica propia; el test de este caso no verifica código nuevo sino que instrumenta los hooks para demostrar la garantía estructural heredada. | Al planificar casos TDD sobre invariantes de una abstracción base ya CONFORME, anticipar que se resolverán como verde directo por herencia de contrato, y que el test sirve como evidencia de regresión de esa garantía heredada, no como impulsor de código nuevo. |

| L-040 | Tras cerrar 5 features de infraestructura/core (`client_scaffold`, `client_new_cli`, `client_context`, `flow_base`, `onboarding`) sin un mecanismo para que el humano dispare/inspeccione un flujo desde la terminal, el humano preguntó explícitamente en qué punto podía "probar algo" sin esperar a los 14 flujos del pipeline. | Construir features de negocio en cadena sin priorizar la superficie de uso humano (CLI de orquestación) puede acumular valor invisible: el rol de "revisor/aprobador" del científico de datos (D-006) necesita un checkpoint tangible por flujo (correr + inspeccionar artefacto), no solo al final del pipeline completo. | Se decidió (D-062) construir a continuación `flow_orchestrator` (`foda run`/`foda status`) antes de seguir agregando flujos de negocio nuevos, dando al humano un checkpoint usable tras cada flujo cerrado. |
| L-041 | En el bucle TDD de `flow_orchestrator`, 13 de sus 15 casos llegaron "verde directo" (D-037): solo los casos 4 (camino feliz de `run`) y 10 (camino feliz de `status`) requirieron ciclo rojo→verde real con código de producción nuevo. Los criterios de aceptación de errores (flujo desconocido, cliente inexistente, `FlowContractError`, delegación en `flow.run`, extensibilidad del registro vía `FakeFlow`, no-creación de `clients/`) ya quedaban satisfechos por el diseño mínimo de esos dos casos base. | Cuando una feature es un "slice vertical de cableado" (orquestador que delega en piezas ya CONFORMES: `resolve_flow`, `ClientContext`, `Flow.run`, `Artifact.exists`) en vez de lógica de negocio nueva, la proporción de casos "verde directo" puede ser aún mayor que en abstracciones base (L-031, 80%) o features de negocio (L-037): aquí llegó al 87% (13/15). Esto valida la calidad del diseño previo del caso 1/caso 10, no una anomalía del bucle. | Al planificar el bucle TDD de un orquestador/adaptador que solo cablea piezas ya cerradas, anticipar una fracción muy alta de casos "verde directo"; documentar siempre la razón concreta (D-037/NC-6) sin forzar rojos artificiales, y confiar en que el diseño mínimo de los primeros 1-2 casos reales suele cubrir el resto del plan. |

| L-042 | El humano validó manualmente `flow_orchestrator` en una terminal real con un cliente propio (`COMPANY_001`), y para poder ejercitar el camino feliz de `foda run --flow onboarding` necesitó crear a mano un `contract_data.json`. Al consultar `system_design.md` se confirmó que ese archivo es, en el sistema real, la SALIDA del flujo `010_discovery`, que aún no existe (`src/foda/flows/` solo tiene `f020_onboarding`). | Un fixture usado durante TDD (D-055, el `contract_data.json` fabricado de `onboarding`) sigue siendo necesario como stand-in manual incluso después de que la feature esté CONFORME y validada, mientras el flujo que realmente lo produce (`discovery`) no se construya; probar manualmente una feature "aguas abajo" de un flujo no construido exige recrear su salida a mano cada vez. | Se documenta como referencia canónica para pruebas manuales futuras el fixture `_contrato_valido` de `tests/cli/test_flow_orchestrator_cli.py`/`tests/flows/test_onboarding.py`; el usuario decidió guardar su copia de ejemplo fuera del repositorio (coherente con `clients/` en `.gitignore`, D-039). Refuerza que `discovery` es un candidato natural para la siguiente feature de negocio. |
| L-043 | Durante la construcción de `ingestion`, `plan_builder` se cortó por límite de sesión justo antes de hacer el commit de `plan.md`/`state.json`, aunque ambos archivos ya habían quedado escritos en disco. | Ante un corte de sesión a mitad de una etapa de la cadena SDD/TDD, conviene verificar primero el filesystem (¿el artefacto y el `state.json` ya quedaron escritos?) antes de asumir que la etapa falló y re-ejecutar el agente desde cero; a menudo solo falta el commit, que la sesión principal puede completar directamente (ejecución durable, E5). | La sesión principal verificó que `plan.md` y `state.json` de `ingestion` ya estaban completos y solo ejecutó el commit faltante (`6d4b81b`), sin re-invocar `plan_builder`. |
| L-044 | En el GATE humano del plan de `ingestion`, el humano cambió la fuente de validación de "solo `map_client_data.json`" a un reparto de responsabilidad entre `contract_data.json` (archivos) y `map_client_data.json` (columnas) — DS-ING-8. Este cambio no podía aplicarse solo al plan: la decisión sobre "cuál es la fuente de verdad de lo esperado" es una decisión de spec, no de plan. | Un cambio pedido en el GATE del plan puede en realidad pertenecer a una etapa anterior de la cadena SDD/TDD (aquí, la spec): antes de aplicar el cambio solo al plan, hay que verificar si la decisión subyacente ya estaba fijada en `spec.md` y, si es así, reabrir esa etapa primero para no dejar spec y plan desincronizados. | Se re-ejecutó `spec_writer` (spec enmendada con DS-ING-8 y CA-05/06/07 anclados al contrato, CA-08/09/10 al mapa) antes de re-ejecutar `plan_builder` sobre la spec ya enmendada, preservando el orden/numeración de los 22 casos ya aprobados. |
| L-045 | Al arrancar el bucle TDD de `ingestion`, `tdd_tester` verificó `state.json` y encontró `plan_builder.awaiting_approval:true`; se negó a arrancar el caso 1 hasta que ese flag pasara a `false`. | El GATE humano de una etapa se hace cumplir mecánicamente vía `state.json` (no solo por convención documental): un agente de la cadena debe negarse a avanzar si el flag de aprobación de la etapa previa sigue en `true`, en cumplimiento directo de NC-6 (sin decisiones silenciosas). | La sesión principal debe registrar explícitamente la aprobación humana del GATE (poner `awaiting_approval:false`/`approved:true` en `state.json`) antes de invocar al primer agente del bucle TDD, no darla por sobreentendida. |
| L-046 | En el caso 2/22 de `ingestion` (CA-20, estructural), un test ingenuo habría dado verde espurio porque la estructura de `Ingestion(Flow)` ya existía del caso 1 (declaración de clase, `requires`/`produces`). El valor del caso 2 estuvo en que `tdd_tester` aisló deliberadamente el sub-aspecto de CA-20 NO cubierto por el caso 1: la declaración explícita del hook `validate` (dependía del heredado implícitamente), logrando un rojo legítimo. | Cuando un caso "estructural" del plan se solapa con un caso previo end-to-end ya cerrado, el `tdd_tester` debe buscar deliberadamente el sub-aspecto concreto que ese caso previo NO ejercitó, en vez de escribir un test genérico contra la estructura ya existente (que nacería verde sin aportar cobertura real). | Al escribir el test rojo de un caso estructural que se solapa con uno previo, identificar explícitamente qué parte del criterio de aceptación queda sin verificar por el diseño ya construido, y anclar el test a esa parte específica. |
| L-047 | El commit del caso 9 de `ingestion` (CA-12, verde directo por D-037/D-060) quedó etiquetado en el mensaje como `test(ingestion): caso 9 en rojo (TDD red)`, cuando en realidad el caso nació verde directo y nunca pasó por un ciclo rojo→verde. | Los mensajes de commit de un caso "verde directo" deben distinguirse explícitamente en su propio texto (p. ej. "verde directo (D-037/D-060)") del patrón `red`/`green`/`refactor` de un ciclo TDD real; reutilizar la plantilla de mensaje de un ciclo con fases sin adaptarla produce un historial de Git que documenta incorrectamente cómo se cerró el caso. | Al cerrar un caso verde directo, verificar que el mensaje de commit use la etiqueta "verde directo" (no "rojo"/"red") antes de confirmarlo, para que el historial de Git sea una fuente de verdad fiel al método usado. |
| L-048 | El caso 16 de `ingestion` (CA-16) exigía un test que agregara `unexpected_file` a una lista top-level de inconsistencias, pero el esquema DS-ING-2 aprobado en el GATE de spec no tenía ningún campo donde alojar esa inconsistencia (un archivo sobrante no pertenece a ningún dataset del contrato). | Un caso TDD puede exponer una contradicción interna latente entre partes de una spec ya aprobada (aquí: vocabulario cerrado de inconsistencias vs. esquema de datos que no cubre todos sus tipos), que solo aflora al escribir el test que la ejercita. La parada NC-6 del `tdd_tester` (detenerse y elevar en vez de decidir en silencio) es el mecanismo correcto para evitar ampliar un esquema aprobado sin autorización. | `tdd_tester` se detuvo y elevó la ambigüedad a la sesión principal; el humano decidió materializarla como una lista top-level `inconsistencies[]` (D-078/DS-ING-9), enmendando `spec.md` antes de reanudar el bucle TDD. |
| L-049 | El caso 19 de `ingestion` (CA-18, inconsistencia parcial) verificó el CONTENIDO del reporte (`bronze_path` de cada archivo), no solo su `status`, y descubrió que `bronze_path` llevaba varios casos anteriores hardcodeado a `None` para todos los archivos sin que ningún test previo lo detectara. | Los tests que solo verifican el `status`/resultado agregado de una operación pueden dejar pasar campos del esquema de salida completamente hardcodeados o incorrectos; verificar explícitamente el contenido de cada campo del contrato de datos (no solo el resultado general) es necesario para detectar ese tipo de defecto. | Al diseñar casos TDD sobre un esquema de reporte con múltiples campos (DS-ING-2), incluir al menos un caso que verifique el valor real de cada campo relevante (aquí `bronze_path`), no solo `status`/`success`. |
| L-050 | Al cerrar `ingestion` con `integration_tester` y `spec_verifier`, la implementación construida durante el bucle TDD unitario (incluida la corrección del ROJO GENUINO del caso 19 y la enmienda D-078/DS-ING-9 del caso 16) pasó sin retrabajo los 7 tests de integración contra el sistema real (`Onboarding` real + `ClientContext` real) y los 21 CA de la spec enmendada, sin defectos ni hallazgos. | Cuando una feature atraviesa dos episodios de fricción real durante el bucle TDD unitario (un rojo genuino por defecto y una enmienda de spec por contradicción), esa fricción ya resuelta dentro del bucle no se traslada a la integración/verificación si se documentó y corrigió correctamente en su momento (NC-1/NC-6); la integración limpia es la confirmación de que ambos episodios quedaron bien cerrados, no una casualidad. | Tratar un cierre de integración/verificación sin hallazgos como señal de que los episodios de fricción del bucle TDD (rojos genuinos, enmiendas de spec) fueron resueltos de raíz, no solo parcheados para pasar el test local. |
| L-051 | Al preparar una prueba humana de `ingestion` (CONFORME desde `verification.md`, D-078), se detectó que `src/foda/orchestrator.py` tiene `FLOWS = {"onboarding": Onboarding}`: `ingestion` no está registrada, por lo que `foda run <cliente> --flow ingestion` falla con "Flujo desconocido" pese a que la feature es CONFORME a nivel unitario e integración. | Una feature puede quedar CONFORME a nivel unitario+integración (tests contra el sistema real) y aun así NO ser ejecutable por la CLI publicada si nadie la registró en el orquestador (`FLOWS`); el veredicto de `spec_verifier` certifica el comportamiento interno del flujo, no su disponibilidad end-to-end para el usuario final vía CLI. | El cableado al orquestador (`FLOWS`) y la prueba humana subsiguiente deben ser pasos explícitos y verificados del ciclo de vida de una feature de flujo, no asumidos como consecuencia automática del cierre CONFORME; se registra T-032 (cablear CLI) y T-033 (prueba humana) como tareas separadas y explícitas. |
| L-052 | Al cablear `ingestion` al orquestador (T-032) y correr la suite completa, `test_dos_ejecuciones..._byte_identicos` (CA-13, determinismo) resultó flaky: fallaba en índices distintos en cada corrida. Causa raíz: `_precios_xlsx_bytes()` genera el xlsx con `openpyxl.Workbook().save()`, que embebe timestamps en `docProps/core.xml`; el test construía DOS fixtures independientes llamando cada uno a `_precios_xlsx_bytes()` por separado, comparando así orígenes de bytes distintos en vez de la misma copia. | Una librería de generación de archivos (aquí `openpyxl`) puede embeber metadatos no deterministas (timestamps) sin que el nombre de la función lo sugiera; un test de determinismo debe generar el artefacto UNA sola vez y reutilizar los mismos bytes en ambas ramas de comparación, no regenerarlo por separado asumiendo que sería idéntico. | Se añadió un parámetro opcional `precios_xlsx: bytes \| None` a `_build_ctx_fixture_completo` (default genera fresco, sin romper otros llamadores); el test CA-13 genera los bytes una vez y los inyecta en ambos `ClientContext`, aislando el determinismo real de la copia en bronze del determinismo (inexistente) de `openpyxl.save()`. |
| L-053 | En la prueba humana de `ingestion` (T-033), los 4 caminos no-felices "blandos" (columna faltante/extra, archivo faltante/inesperado) devolvieron `success:false` en el reporte pero `foda run` salió con código 0 y mensaje "completado"; solo el fallo duro (`FlowContractError` por artefacto ausente) sale con código 1. | Un flujo puede reportar `success:false` en su artefacto de salida sin que la CLI que lo invoca refleje ese fallo en su código de salida; un operador o script que solo mire el exit code de `foda run` (patrón estándar en shells/CI) no se enteraría de que hubo inconsistencias sin abrir manualmente el reporte JSON. | Se acordó con el humano (ADR D-080, punto 4) que `foda run` debe leer el campo `success` del reporte del flujo y salir 0 solo si es `true`, 1 si es `false`; se registra como tarea aislada de alta prioridad (T-035), separable del resto del gate de progresión. |
| L-054 | Al intentar correr `ingestion` vía CLI en la prueba humana (T-033), `openpyxl` no estaba instalado en el `.venv` activo pese a estar declarado en `pyproject.toml` desde la construcción de `ingestion` (TSK-01, D-069); el `.venv` no se había reinstalado tras esa declaración de dependencia. | Declarar una dependencia nueva en `pyproject.toml` durante el bucle TDD de una feature no actualiza automáticamente los entornos virtuales ya existentes de otros colaboradores/sesiones; el error solo se manifiesta al ejercitar en runtime real la ruta de código que la importa (aquí, al arrancar el orquestador que ahora importa `Ingestion`, que a su vez hace `import openpyxl` a nivel de módulo). | Tras cualquier cambio de dependencias en `pyproject.toml`, reinstalar el paquete en modo editable (`pip install -e .`) en cualquier `.venv` existente antes de ejercitar el camino de código que la usa; documentado como recordatorio para futuros colaboradores (extiende L-027). |
| L-055 | En la prueba humana de `ingestion` (T-033) se observó que `data/bronze/` no se limpia entre corridas: si un archivo se ingirió con éxito en una corrida y en una corrida posterior pasa a `missing`/`rejected`, la copia anterior queda en bronze sin que nada la marque como obsoleta. No se manifestó como defecto porque cada camino no-feliz se probó con un cliente `DEMO_*` fresco, pero es un gap de idempotencia real. | Un flujo puede ser CONFORME a su spec (que describe el comportamiento de una sola corrida) y aun así tener un gap de idempotencia entre corridas sucesivas sobre el mismo cliente, si la spec nunca declaró explícitamente qué pasa con artefactos de una corrida anterior que ya no aplican en la corrida actual. | Se anota como riesgo no bloqueante (no bloquea el uso actual, que siempre parte de clientes nuevos en las pruebas); queda pendiente de decidir si se aborda en una banda `stab_n` de `ingestion` o al construir el primer flujo que dependa de la "frescura" de bronze. |
| L-056 | T-035 (reflejar `success` en el exit code de `foda run`, punto 4 de D-080) era una corrección de comportamiento sobre `flow_orchestrator`, una feature YA CONFORME (D-067). En vez de re-invocar la cadena completa de 8 agentes SDD/TDD, se aplicó un ciclo TDD acotado rojo→verde directamente sobre la feature existente (1 test nuevo + cambio mínimo en `_dispatch_run` de `cli.py`). | No toda corrección de comportamiento sobre una feature CONFORME justifica reabrir la cadena de 8 agentes (feature_definer→...→spec_verifier, D-008): cuando el cambio es acotado, quirúrgico (NC-2/NC-3) y está respaldado por un ADR ya aprobado (D-080), un ciclo TDD rojo→verde mínimo sobre el código existente —documentando la traza al ADR— es proporcional (P6/E4) y evita el overhead de volver a recorrer spec/plan/integración/verificación para un cambio de una línea de comportamiento. | Al recibir una tarea de "corrección de comportamiento" sobre una feature ya cerrada CONFORME (no una feature nueva), evaluar primero si un TDD acotado directo sobre el código (con test nuevo + traza al ADR/hallazgo que lo origina) es suficiente, en vez de asumir por defecto que hay que re-invocar la cadena completa de 8 agentes. |
| L-057 | En la prueba humana de `ingestion` (T-033) se detectó que `foda run` con `success:false` salía con código 0 (L-053); esta sesión implementó el fix (T-035) que corrige exactamente ese comportamiento. | Un hallazgo de gobernanza registrado en una lección (L-053) puede resolverse por completo en una sesión posterior mediante la tarea que originó (T-035); vale la pena, al cerrar esa tarea, anotar explícitamente en la lección original o en una nueva que el hallazgo quedó resuelto, para que quien lea `lessons.md` no interprete L-053 como un problema todavía abierto. | L-053 describe el hallazgo original (aún vigente como registro histórico); esta lección (L-057) y la entrada de T-035 en `tasks.md` dejan constancia de que quedó resuelto en 2026-07-09. |
| L-059 | Al resolver el "bloqueo" de T-036 (gate de progresión, diferida por falta de flujo consumidor de un reporte de `ingestion`), la primera hipótesis fue buscar el consumidor en `discovery` (flujo 010, el primero del pipeline), por ser el siguiente flujo sin construir en la hoja de ruta de 14 features. Al releer `990_documents/expected_workflow.md` se confirmó que `discovery` va ANTES de `onboarding` (flujo 020), no después de `ingestion` (flujo 030): el flujo que sigue a `ingestion` es **Profiling (040)**. | El orden numérico de los prefijos de carpeta (`010_discovery`, `020_onboarding`, `030_ingestion`, `040_profiling`, ...) es la fuente de verdad del orden real del pipeline, no la intuición de "el siguiente flujo sin construir en la hoja de ruta"; conviene releer `expected_workflow.md` antes de asumir cuál es el sucesor inmediato de un flujo dado. | Se identificó Profiling (040) como el flujo consumidor real y anfitrión natural de T-036; se decidió con el humano construir un esqueleto mínimo/tracer bullet de `profiling` en vez de asumir que había que esperar a construir `discovery`. |
| L-060 | Para hospedar T-036 (gate de progresión) sin construir el flujo Profiling completo (fuera de alcance por NC-2/NC-4, ya que su lógica de negocio real —salud de datos, comparación contra `client_register.yaml`— no estaba definida), se acordó con el humano construir solo un esqueleto mínimo/tracer bullet: `Profiling(Flow)` con contrato de clase y un `profiling_report.json` mínimo cuyo único campo vinculante es `success`. | Cuando una tarea de gobernanza (aquí, un gate técnico) necesita un anfitrión concreto que aún no existe como feature de negocio madura, se puede construir el esqueleto MÍNIMO de esa feature (solo lo necesario para que el gate tenga algo real que gatear) en vez de posponer la tarea de gobernanza hasta que la feature de negocio esté completa, o de sobre-construir la feature completa solo para desbloquear el gate. | `profiling`/`tracer_bullet` deja explícitamente fuera de alcance la lógica de salud de datos, lectura de `bronze/`, exportables y comparación contra `client_register.yaml` (diferido a `stab_1`); el alcance se acotó a lo estrictamente necesario para hospedar T-036. |
| L-058 | Al implementar T-034 (política de ramas D-079/D-081), el listado de "7 ajustes" documentado en D-079 resultó incompleto: los stages terminales `human_test`/`merge_to_main` también tocan `700_architecture/sdd_tdd_workflow.md` (esquema `state.json` + diagrama de orquestación, fuente única de verdad de la cadena SDD/TDD) y `.claude/agents/spec_verifier.md` (semántica de cierre: CONFORME ya no puede dejar la feature en `completed` directamente). Se detectó al revisar coherencia transversal antes de tocar código, y se consultó al humano vía AskUserQuestion en vez de decidir en silencio. | Al implementar una política de gobernanza documentada como "N ajustes", verificar la coherencia transversal con las fuentes de verdad relacionadas antes de ceñirse literalmente al conteo: un listado enumerado en el momento de acordar el ADR puede quedar incompleto si no se revisan todos los documentos que el cambio toca en la práctica. | Se amplió el alcance de T-034 de 7 a 9 archivos con aprobación explícita del humano, documentando la ampliación en `tasks.md`/`decisions.md` en vez de aplicar solo los 7 originales y dejar `sdd_tdd_workflow.md`/`spec_verifier.md` inconsistentes con el nuevo flujo. Refuerza NC-6 (sin decisiones silenciosas). |

| L-061 | Al cerrar el caso 12 del bucle TDD de `profiling` (wiring CLI del gate), `tdd_coder` encontró que `600_features/profiling/tracer_bullet/state.json` tenía un error de sintaxis JSON preexistente (faltaba una llave de cierre), heredado de una sesión anterior, que lo hacía no parseable pese a que la cadena SDD lo venía usando. | Un `state.json` corrupto puede pasar desapercibido durante varias etapas de la cadena SDD/TDD si ningún agente lo parsea estrictamente en cada invocación (algunos solo lo leen parcialmente por texto/grep); el defecto aflora recién cuando una etapa necesita cargarlo como JSON válido. | `tdd_coder` corrigió la llave de cierre faltante como parte del caso 12 (defecto objetivo, no ambigüedad de diseño, análogo a L-025) y lo documentó en el commit; se recomienda validar `state.json` como JSON parseable en cada cierre de sesión (ya lo hace `session_closer` de forma implícita al leerlo). |
| L-062 | En el avance masivo del bucle TDD de `profiling` (casos 2-17), 8 de los 16 casos restantes (3, 4, 5, 11, 14, 15, 16, 17) llegaron "verde directo" (D-037), pero a diferencia de otras features previas, cada uno tuvo un test específico y no trivial (p. ej. CLI --force con success:true, CLI --flow ingestion sin predecesor corriendo end-to-end, borde documentado de CA-05 con --force y reporte ausente) en vez de una simple repetición de aserciones ya cubiertas. | El patrón "verde directo" (D-037) no exime de escribir un test específico por caso solo porque el comportamiento ya esté cubierto por el diseño mínimo de un caso anterior: NC-5 exige que cada caso del plan tenga su propio test de respaldo, aunque el ciclo rojo→verde con código nuevo no ocurra. Verde-directo describe el estado del código (sin cambios), no una licencia para omitir el test del caso. | Se mantuvo la disciplina de escribir un test dedicado y no trivial para cada uno de los 8 casos verde-directo de `profiling`, documentando en cada commit qué caso/comportamiento previo ya lo cubre, en vez de reutilizar o duplicar el test de un caso anterior. |

| L-063 | Al abrir el primer PR bajo la política D-079/D-081 (`profiling`), se descubrió que GitHub CLI (`gh`) no estaba instalado en el equipo; nunca había hecho falta porque hasta ahora todo el trabajo se empujaba directo a `main` sin PR. | Una pieza de tooling que un ADR de gobernanza asume disponible (aquí, `gh` para `gh pr create`, D-079) puede no estarlo en el equipo real hasta el momento en que se ejerce por primera vez esa parte del flujo; conviene verificar la disponibilidad de la herramienta ANTES de necesitarla en medio de un gate, no asumirla. | Se instaló `gh` v2.96.0 vía winget (`winget install GitHub.cli`) y el usuario se autenticó con `gh auth login --web` (device flow); quedó documentado que `gh` ya está disponible para futuras aperturas de PR, sin necesitar reinstalarlo. |
| L-064 | Al cerrar `profiling` de punta a punta (PR → `human_test` → `merge_to_main`), las 6 pruebas humanas end-to-end (incluyendo construir a mano un fixture con `ingestion_report` fijado a `success:false` para poder ejercitar el gate bloqueado y el `--force`) coincidieron exactamente con el comportamiento esperado en `spec.md`, sin retrabajo de código. | Cuando el bucle TDD unitario y la integración ya cerraron sin fricción (D-obtenido CONFORME), la prueba humana de un flujo con estado dependiente de un predecesor (aquí, el gate de `profiling` sobre `ingestion`) requiere construir deliberadamente fixtures de estado "malo" del predecesor (no solo el camino feliz real) para poder ejercitar las ramas de bloqueo/override; el humano fabricó ese estado a mano fijando el reporte, en vez de asumir que solo el camino feliz real bastaba para validar el gate. | Documentar en las guías de prueba humana futura de flujos con gate de progresión que se debe incluir al menos un fixture con predecesor en `success:false` fabricado a mano, análogo al patrón ya usado para `contract_data.json` (L-042). |
| L-065 | El usuario compartió un artículo externo (LangChain, observabilidad/evaluación de agentes) mostrando interés en trabajar observabilidad y evaluación en el proyecto. El marco conceptual del artículo (runs/traces/threads, eval offline/online/ad-hoc) coincidía casi literalmente con doctrina ya existente en `principles.md` (P8 "Traces over intuition", P3 evaluador externo, E3 rúbrica calibrada, E9 evaluación temprana), envuelto en marketing de una herramienta concreta (LangSmith) que el proyecto no necesita (E4). | La tesis central de ese tipo de artículos ("el comportamiento emerge en runtime, hay que observarlo") aplica al PRODUCTO FODA cuando construya flujos con LLM (Discovery, Exploration — aún no construidos), no al harness SDD/TDD determinista, que ya tiene trazabilidad completa vía persistencia (`800_persistence/`), `state.json` por feature y el historial de Git; adoptar tooling de observabilidad ahora sería sobre-ingeniería sin un flujo LLM real que observar. | Se decidió no adoptar ninguna herramienta de observabilidad todavía; la evaluación real (rúbrica calibrada, D-022) se diseñará cuando se construya el primer flujo con LLM real (Discovery/Exploration, T-020). |
| L-066 | El usuario preguntó cómo proteger `main` con GitHub Actions/ruleset (traído de otro proyecto). Al analizarlo se detectó una tensión sin resolver: proteger `main` exigiendo PR para todo push chocaría con que hoy `session_closer` empuja directo a `main` en el cierre de cada sesión (push de los 5 archivos de `800_persistence/`), incluso en el flujo D-079/D-081 donde el trabajo de feature va por rama pero el propio cierre de sesión que registra ESE cierre puede seguir en `main` tras el merge. | Antes de proteger una rama con reglas de CI/branch protection, hay que verificar explícitamente qué actores automatizados escriben hoy directo a esa rama (aquí, el propio harness vía `session_closer`) y decidir cómo conviven con la nueva regla (bypass de cuenta, mover esa escritura dentro de un PR, o una rama de cierre con auto-merge), en vez de activar la protección y descubrir la fricción después. | El usuario decidió dejarlo así por ahora (sin protección de rama); las 3 opciones de reconciliación quedaron identificadas pero sin decidir, pendientes de una futura sesión de gobernanza si se retoma el tema. |

| L-067 | En el caso 2 del bucle TDD de `profiling` banda `stab_1` (CA-20/17, bloque `health`), `tdd_coder` implementó de una sola vez TODA la lógica del bloque (conteos, `problems_by_type`, `global_score` ponderado, `pareto`, DS-PRF-2..5), en vez del mínimo necesario para pasar el test rojo del caso 2 (que solo exigía las 6 claves presentes con valores de un fixture "todos sanos"). La sesión principal lo detectó al revisar el diff antes del refactor. | Que una decisión de diseño ya esté aprobada en el GATE de spec (aquí, DS-PRF-2..5) no autoriza a `tdd_coder` a implementarla completa de una sola vez: el mínimo del CASO actual sigue siendo el límite de lo que se construye en esa pasada, incluso si el resto de la lógica "ya se sabe" cómo va. Adelantar casos futuros viola NC-2/NC-5 y vacía de sentido al resto del bucle rojo→verde. | Consultado el humano (NC-6), se decidió restaurar el TDD estricto revirtiendo la sobre-implementación (commit `f8d6d5a`) a placeholders mínimos (`global_score:1.0`, conteos en 0, `problems_by_type:{}`, `pareto:[]`) para reconstruir la lógica caso a caso; se recomienda reforzar explícitamente en el prompt de `tdd_coder` la restricción de "solo el mínimo del caso actual, nunca lógica de casos futuros aunque esté ya especificada". |
| L-068 | En el caso 10 del bucle TDD de `profiling` banda `stab_1` (CA-02, fórmula ponderada de `global_score`), `tdd_coder` implementó la fórmula completa de DS-PRF-2 —incluido el clamp inferior `max(0.0, ...)` y el borde `files_declared==0`— de una sola pasada, porque la propia spec la define como una única expresión indivisible. Consecuencia: los casos 11 (CA-03, clamp), 12 (CA-04, borde) y 13 (CA-05, redondeo/determinismo), planificados por `plan.md` como tareas-código separadas (TSK-15/17/19), resultaron todos "verde inmediato" sin código nuevo. | Cuando una spec define un valor mediante una FÓRMULA ÚNICA (no una secuencia de reglas independientes), fragmentar esa fórmula en varias tareas-código distintas en el plan de TDD tiende a generar casos confirmatorios en cascada: la primera implementación real ya satisface las siguientes por construcción matemática, no por adelanto indisciplinado (a diferencia de L-067, aquí no hubo violación de NC-2/NC-5, la fórmula es la unidad mínima correcta según spec §20). No es un error del bucle, pero conviene anticiparlo al planificar. | Al escribir `plan.md` para un criterio de aceptación que es una fórmula matemática única con múltiples cláusulas (clamp, bordes, redondeo), considerar agrupar esas cláusulas en un solo caso TDD real (o documentar explícitamente que las cláusulas restantes nacerán verde-directo), en vez de planificarlas como tareas-código independientes. |
| L-069 | Para preparar el gate humano `human_test` de `profiling` banda `stab_1` (T-039), en vez de derivar a mano los valores esperados de las 7 pruebas del plan (p. ej. el `global_score` exacto ante `missing_column`), la sesión principal ejecutó empíricamente la cadena real Onboarding→Ingestion→Profiling en un cliente scratch aislado (`clients/zzverify`) para observar los valores reales producidos por el código, y los persistió tal cual en `human_test_plan.md`. | Verificar empíricamente (corriendo el sistema real en un entorno scratch) los valores esperados de un plan de prueba humana produce salidas EXACTAS y ya confirmadas contra el comportamiento real del código, en vez de valores derivados a mano de la fórmula/spec que podrían contener un error de cálculo o no coincidir con un detalle de implementación (p. ej. redondeo); además, como `clients/` está en `.gitignore` (D-039), este tipo de verificación puede hacerse creando y borrando clientes scratch sin ensuciar el control de versiones. | Se documentaron los 7 casos de `human_test_plan.md` con los valores observados en la corrida real (no derivados), y se eliminó el cliente scratch `zzverify` tras la verificación; se recomienda repetir esta práctica (verificación empírica en scratch) al preparar futuros gates `human_test` de flujos con cálculos no triviales. |
| L-070 | Al cerrar el gate `human_test` de `profiling` banda `stab_1` (T-039), el humano había ejecutado los casos 1-4 de `human_test_plan.md` pero dejó el caso 5 (`DEMO_e`) a medio sembrar (CSVs sin borrar, `ingestion`/`profiling` sin correr), sin decirlo explícitamente. La sesión principal lo detectó revisando los artefactos reales en disco antes de dar el gate por completo, no confiando en que "el humano dijo que lo probó" bastara. | Antes de cerrar un gate humano terminal (que desbloquea `merge_to_main`), conviene verificar el estado real de los artefactos en disco de cada caso del plan, no solo asumir que el humano ejecutó todos los casos por completo; un caso a medio sembrar puede pasar desapercibido si solo se confía en el reporte verbal. | La sesión principal completó los casos 5-7 faltantes vía CLI antes de considerar el gate `human_test` cerrado, dejando los 7/7 casos con evidencia verificada en la propia sesión, no solo referida por el humano. |
| L-071 | Al diseñar `profiling` `stab_2`/`stab_3`, surgieron dos aprendizajes reutilizables de diseño de auditoría de datos: (1) "defectos con score vs. perfil diagnóstico informativo" (D-098) — no todo hallazgo debe penalizar un score agregado; los ambiguos (outlier, desbalance 95/5, columna constante) se reportan pero no bajan el número, porque no son necesariamente errores; (2) incommensurabilidad de granularidad — sumar conteos de escalas distintas (celdas vs. archivos) directamente a un score plano lo satura arbitrariamente, y la solución es normalizar cada dimensión a [0,1] sobre su propio denominador antes de agregarlas (D-099). | Ambos patrones son generalizables más allá de Profiling: cualquier flujo futuro que produzca un score agregado a partir de señales de granularidad distinta (fila/columna/archivo/celda) debe normalizar por dimensión antes de sumar, y cualquier auditoría que mezcle defectos inequívocos con hallazgos ambiguos debe separar explícitamente cuáles penalizan un score y cuáles solo informan. | Documentado como principio transversal (D-098) y como técnica de diseño (D-099) para consultarlos al construir futuros flujos de auditoría/calidad de datos (p. ej. Cleaning 050). |
| L-072 | Al escribir `spec.md` de `profiling` `stab_2`, el humano reveló durante la revisión (no antes) que el `global_score` dejará de ser meramente informativo y se convertirá en el INPUT de una tarifa contractual (modelo SaaS con sobrecargo). Esa revelación tardía obligó a reabrir una spec YA APROBADA en el GATE para reubicar `profiling_config.yaml` como vendor-owned (D-105) y a documentar dos riesgos de facturación no resueltos (D-106/DS-PRF2-7). Además, `plan_builder` detectó independientemente que `pandas` era una dependencia nueva no declarada (A-020 asumía erróneamente que ya era transitiva de Ingestion). | (1) El contexto de negocio de una feature puede cambiar el peso de una decisión de diseño ya aprobada (aquí: quién es dueño de un archivo de configuración) sin que el criterio de aceptación original estuviera "mal"; conviene mantener el GATE de spec abierto a enmiendas cuando surge contexto de negocio nuevo, en vez de tratar la aprobación previa como inamovible. (2) Un supuesto de planeación (A-020, "pandas es la dependencia adecuada") puede resultar cierto en el "qué" pero falso en un detalle crítico del "cómo" (aquí: si ya estaba declarada formalmente); verificar contra el `pyproject.toml` real, no contra la intuición de que una librería instalada equivale a una dependencia declarada. | Se enmendó `spec.md` con D-105/D-106 antes de arrancar `plan_builder`; `plan.md` añadió TSK-12 (declarar `pandas` en `pyproject.toml`) como tarea explícita. Al iniciar el `spec_writer`/GATE de una feature con implicaciones de negocio nuevas (facturación, contratos), preguntar explícitamente si hay contexto de negocio no dicho que cambie quién es dueño/controla un artefacto de configuración. |

## 4. Lecciones de Producto/Negocio
| ID | Contexto | Lección | Acción |
|---|---|---|---|
| — | _Sin registros aún._ | — | — |
